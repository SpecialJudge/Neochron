# Celechron 魔改版说明

> ⚠️ **本文件是历史记录**：它记的是这个魔改从 Celechron 1.2.0 一路改下来的过程，
> 里面的版本号、应用名、包名（`xyz.nosig.celechron.mod`）都是**当时**的状态。
> **当前状态**请看 [README.md](README.md)：应用名 **Neochron**、包名
> `io.github.specialjudge.neochron`、仓库 `SpecialJudge/Neochron`。

## 一句话

把 Celechron 的「任务/日程」改造成钉钉待办详情页的形态：完成待办按钮、大标题、描述、截止时间带提醒铃铛、优先级、子待办、附件、本地评论，并新增实时倒计时。*（「时间安排」已在后续迭代中按要求移除）*

## 工程信息

| 项 | 值 |
|---|---|
| 源码位置 | `D:\学习资料\celechron魔改\Celechron`（另有 ASCII 联接 `D:\celechron-mod\Celechron`，用于规避 flutter analyze 的中文路径 bug） |
| 上游仓库 | https://github.com/Celechron/Celechron |
| 基准版本 | tag `1.2.0`（与你手机上装的 v1.2.0 一致） |
| 工作分支 | `mod-1.2.0` |
| 魔改后版本号 | `1.2.1+2` |
| 应用包名 | `xyz.nosig.celechron.mod`（**与官方版共存**，互不影响数据） |
| 应用名 | Celechron魔改 |
| Flutter SDK | 3.47.2 / Dart 3.13.2（装在 `D:\flutter`） |

### 数据层：为多端同步打地基（最新一轮）

多端同步最难的不是网络，而是「怎么知道该保留哪一条」。这一轮只做数据层，不做网络。

| # | 改动 |
|---|---|
| 1 | **删除墓碑**：新增 `lib/model/tombstone.dart`（`TaskTombstone{uid, deletedAt}`）与独立 Hive box `dbTombstones`。删除待办不再只是从列表里移除，会先留一条墓碑——否则另一台设备会把已删除的待办又推回来。所有删除入口（`updateDeadlineList` 清理 deleted、删除已完成、删除已过期）都走 `_removeWithTombstone`；`fixedlegacy` 副本属本地派生数据，不留墓碑 |
| 2 | **JSON 编解码**：新增 `lib/utils/task_json.dart`。刻意**用稳定字符串键 + 枚举名**而不是 Hive 字段序号（序号在版本间会漂移，跨设备会读错字段）；解析时未知枚举回退默认值、缺必要时间字段的脏数据整条跳过 |
| 3 | **数据包 + 合并**：新增 `lib/utils/data_sync.dart`。`DataBundle`（format/version/exportedAt + tasks + tombstones + tags + tagColors + settings）可 encode/decode；`DataMerge.merge` 规则：同 uid 比 `updatedAt` 新者胜；墓碑时间晚于待办更新时间则该条保持删除；删除后又编辑过的会复活；墓碑作用于两边（本地待办也会被远端墓碑删掉）；挂掉的日程其《过去日程》副本一并清理。合并是幂等的 |
| 4 | **导出 / 导入**：新增 `lib/utils/data_backup.dart` + 设置页「数据」分区。导出 → 写 JSON 到应用文档目录并拉起系统分享面板（可存到坚果云）；导入 → 选文件 → **先在内存试算合并结果并弹窗展示「新增/更新/删除/保留」条数** → 确认后**先在本地写一份 `before-import.json`** 再落库 |

新增测试 `test/data_sync_test.dart`（15 个用例）：字段往返、嵌套结构还原、枚举按名字序列化、未知枚举回退、脏数据跳过、整包往返、非本应用文件拒绝、合并的 9 种情形（新增 / 新者胜 / 本地更新不被覆盖 / 墓碑阻止复活 / 删后编辑复活 / 本地被远端墓碑删 / 副本清理 / 双向新增 / 幂等）。

> 本轮**不包含**任何网络同步逻辑，也不含桌面端。下一步才是把 `DataBundle` 整份 PUT/GET 到坚果云 WebDAV。

### 五项优化（上一轮）

| # | 改动 |
|---|---|
| 1 | 所有「⋯」菜单的卡片**水平顶到屏幕边缘**（原来左右各留 40 的边距） |
| 2 | 闹钟配色预设：**天依蓝 `#66CCFF`**（[萌娘百科](https://moegirl.icu/%E5%A4%A9%E4%BE%9D%E8%93%9D) / [百度百科](https://baike.baidu.com/item/%E5%A4%A9%E4%BE%9D%E8%93%9D/24194679) 收录的标准色）、**爱莉粉 `#FFA6C9`**、**清新绿 `#6FD8A6`**、**静谧紫 `#B39DDB`**；全部浅色 + **毛玻璃**（`BackdropFilter` 卡片 + 背景光斑）。设置 → 「闹钟配色」可选，**每个预设带迷你预览**，**点一下即可打开真实闹钟页预览**（预览模式不响铃、不震动，底部只有「返回」）。选择存 `optionsBox.alarmTheme` |
| 3 | 删掉原来的后台定时刷新（`workmanager` 每 15 分钟的 `backgroundScholarFetch`，只保留取消逻辑）；改为**每天第一次打开时自动刷新一次**课表数据（记录 `optionsBox.lastAutoRefresh`，成功才写日期，失败下次再试） |
| 4 | **有子待办的待办必须在子待办全完成之后才能完成**：未完成时点完成会弹二次确认「还有 N 个子待办未完成，确定要一起完成吗？」，确认后把所有子待办一起勾上；取消则不完成。覆盖卡片打勾 / 左滑完成 / 卡片弹窗 / 详情页 / 日历打钩 5 个入口 |
| 5 | 周期性待办恢复「完成即生成下一次」：自动生成的判断从「仅 DDL」放宽到**所有类型**（日程类重复待办完成时也会生成下一期） |

新增：`lib/design/alarm_theme.dart`、`lib/design/alarm_theme_picker.dart`、`lib/utils/task_complete.dart`；测试新增 `test/task_complete_test.dart`（4 个用例，覆盖无子待办 / 全完成 / 未完成确认 / 取消）。

### 提醒功能升级（上一轮）

| # | 改动 |
|---|---|
| 1 | 详情页删掉**「时段设置」**（「不在这个时段中安排待办」整块）。模型字段 `blockArrangements` 保留（时间规划算法还在用），只是界面不再出现 |
| 2 | 提醒通知换成新渠道 `task_reminder_v2`（`Importance.max` + 高优先级 → **横幅弹出 + 响铃**，类似钉钉），并带两个按钮：**延迟提醒**（推迟 10 分钟）/ **划掉** |
| 3 | 设置页新增 **「待办提醒方式」**：`通知`（横幅+响铃）/ `闹钟`。存 `optionsBox.reminderMode` |
| 4 | **闹钟模式**：全屏闹钟页（大时钟 + 任务名 + 截止时间 + 描述），原生播放**手机默认闹钟铃声并循环**（`RingtoneManager.TYPE_ALARM` + `MediaPlayer`，`USAGE_ALARM`），配 `HapticFeedback` 震动；两个按钮 **延迟提醒（10 分钟）** / **划掉**。通知侧用 `fullScreenIntent` + `AndroidNotificationCategory.alarm`，所以应用在后台/锁屏时也会拉起 |
| 5 | 延迟/划掉的状态记录在 `TaskReminder` 内（`_snoozed`），签名里带上，避免每秒同步把延迟覆盖掉 |

新增文件：`lib/utils/alarm_player.dart`、`lib/utils/task_alarm_center.dart`、`lib/page/task/task_alarm_page.dart`；原生 `MainActivity.kt` 加了 `celechron/alarm` 通道。

### 标签筛选优化（上一轮）

| # | 改动 |
|---|---|
| 1 | 标签筛选改为**多选**：`selectedTag`（单选）→ `selectedTags`（`RxSet<String>`），点一下加进来、再点一下移出去；筛选逻辑为**命中任意一个选中标签即保留**（OR）。「全部分类」= 清空所有标签选择 |
| 2 | 标签胶囊去掉后面的数量数字（`标签名 0` → `标签名`） |
| 3 | 标签管理里删掉某个标签后，自动从筛选选择中同步移除，避免筛住出不来 |

> 按你的要求，本轮**未做任何数据恢复/修改**，待办与标签保持你自己清理后的状态。

### 细节修正 3（上一轮）

| # | 修正 |
|---|---|
| 1 | 「全部分类」= **默认选中所有标签**：点它清掉标签筛选，回到全部待办（之前被误做成无功能，导致筛住出不来） |
| 2 | 分类标签行与下面筛选行的距离收紧（标签行下内边距 6→0，筛选行上内边距 10→4） |
| 2b | 但两行胶囊之间留出明确间隙（筛选行下 3 + 标签行上 3），不再贴在一起；分类行高度改为跟随系统字号（`textScalerOf`），大字号时不会顶到下面一行 |
| 2c | 两行胶囊整体上移：分类行容器留白 28→14、筛选行上内边距 4→2 |
| 3 | 全应用界面文案「任务」统一改为**「待办」**：页面标题、底部标签、没有待办、⋯ 菜单各项、表单占位符、提醒渠道名等 |
| 4 | 所有「⋯」菜单改为**钉钉风格**：圆角卡片 + 竖排「图标 + 文字」行 + 分隔线 + 独立的「取消」块（原来是 iOS 底部动作面板）；详情页为「星标 / 删除待办 / 取消」，待办页为「删除已完成待办 / 删除已过期待办 / 暂停所有待办 / 继续所有待办 / 取消」 |

> 安全加固：`setTagLibrary` 默认拒绝写入空列表（只有用户在标签管理里主动删光才允许），避免标签库被意外清空。

### 细节修正 2（上一轮）

| # | 修正 |
|---|---|
| 1 | 分类行去掉灰色背景（原来在导航栏 `bottom` 里铺了 `systemGroupedBackground`，看着像一条灰带） |
| 2 | 「全部分类」按钮保留但**不再有任何功能**（分类面板已删除）；标签过滤只由第二行的标签胶囊承担 |
| 3 | 标签管理拖动时不再出现方框/阴影（`proxyDecorator` 直接返回原样式） |
| 4 | 筛选面板的「开始日期 / 结束日期」改用**月历点选**（`showDateTimeSheet`），不再是滚轮 |
| 5 | 去掉筛选生效后多出来的「重置」胶囊 |
| 6 | 排序面板补上 `Obx`：之前点排序项界面不刷新，看起来「灰底固定创建时间、蓝箭头固定↓」；现在**点哪行灰底移到哪行、点哪个箭头哪个变蓝** |

### 细节修正 1（上一轮）

| # | 修正 |
|---|---|
| 1 | 分类标签页从列表里移进导航栏的 `bottom`（`NavigationBarBottomMode.always`），紧贴「任务」大标题下方，间距收紧；四个分类改为**两端对齐 + 等距分布**，间隔一致 |
| 2 | 筛选胶囊整体缩小（字号 15→14、内边距 14/9→12/6），未选中为浅灰无描边、选中为蓝字 + 蓝描边，更贴近参考截图 |
| 3 | 「全部标签」改为 **「标签管理」**：点开弹出管理面板，可**新建 / 删除 / 拖动排序 / 给标签选颜色**（10 色盘，存 `optionsBox.tagColors`）；标签胶囊显示各自的颜色圆点 |
| 4 | 详情页点「添加子待办」改为弹小窗口二选一：**选择现有待办**（列出其它待办供挑选）/ **新建子待办**（完整新建窗口）/ 取消 |

### 待办页分类 / 排序 / 筛选 / 星标（上一轮）

按钉钉「待办」页截图 1:1 复刻：

| 截图 | 实现 |
|---|---|
| 图一 | 任务页顶部四个标签页：**待我处理**（未完成，带数量徽标）/ **优先处理**（未完成且优先级为高或紧急）/ **我已处理**（已完成）/ **星标**（按要求把「我创建的」改成了星标） |
| 图一 | 第二行筛选胶囊：**全部分类 ▾** / **创建时间 ▾**（显示当前排序项）/ **筛选**（有生效筛选时高亮） |
| 图二 | 点「全部分类」弹出两栏面板：左列=全部分类 + 所有标签（各带数量），右列=当前分类及其数量 |
| 图三 | 点排序项弹出排序面板：截止时间 / 创建时间 / 更新时间 / 优先级，每行 ↑↓，当前项整行高亮、方向箭头变蓝 |
| 图四 | 点「筛选」弹出筛选面板：完成状态（未完成/已完成）、优先级（紧急/高/普通/低，各自配色）、截止时间（已逾期/今天/明天/未来七天/未安排 + 开始日期→结束日期）、创建人（本地版无协作成员，点击给出提示），底部 **重置 / 确定** |
| 图五 | 筛选行下方一行显示**所有标签**（标签库 + 任务里用过的），每个标签带数量，点一下即按该标签过滤 |
| 图六 | 详情页右上角 ⋯ 菜单：**星标 / 取消星标**、**删除待办**、取消（**不含「投诉」**） |

数据层新增 `starred` / `createdAt` / `updatedAt` 三个字段（Hive 23–25，老数据读出来为空值、不丢数据）。

### 统一为「待办」（上一轮）

| 改动 | 说明 |
|---|---|
| ① 删掉时间安排 | 详情页的「预期用时 / 已经用时 / 允许插入休息时间」整段移除；任务卡片上的「xx% 已完成：预期…」与进度条也一并移除。**完成只由打钩决定**，不再按累计用时自动完成 |
| ② 合并 DDL 与日程 | 界面不再有类型选择：新建页去掉「归属于」、详情页去掉 DDL/日程 切换、任务页把「待办 / 日程列表」合成一个**待办**列表（已完成的合到「已完成」）。内部仍按「有没有开始时间」区分：在「其他日期 → 开始时间」填了开始时间（早于截止）就按带时段的任务处理，否则就是普通待办。旧数据自动归一化（早期 DDL 被写成「截止前 1 分钟」的开始时间会被清掉），**原有日程与待办数据全部保留** |
| ③ 主日历显示任务小点 | 月视图的标记从「课程/考试/日程」扩展为「课程/考试/日程/待办」：待办未完成用其主题色、已完成用绿色 |
| ④ 删除详情页⋯菜单 | 详情页右上角只保留 ✓；删除任务仍可用底部的「删除任务」按钮 |
| ⑤ 新建页可填地点 | 新建待办页在「添加描述」下方新增「添加地点」输入框，保存后写进任务的 `location`（日历当日卡片也会显示地点） |
| ⑧ 子待办用完整新建窗口 | 详情页点「添加子待办」直接打开**和新建待办一样的整页窗口**（标题/描述/地点/时间/优先级/标签/附件都能填）；点已有子待办同样用它来编辑。子待办模型相应扩展了 `description / endTime / priority / tags / attachments / location`，行下方显示概要（截止·优先级·标签·附件·地点）。窗口标题为「新建子待办」/「编辑子待办」，高度只占屏幕 86%（上方留出空隙 + 顶部圆角），不遮住整屏 |
| ⑥ 其它应用分享进来即可新建 | 注册为系统分享目标（`ACTION_SEND` / `SEND_MULTIPLE`，`*/*`）。收到分享后：文件复制进附件目录 → 切到「任务」页 → 弹出新建窗口（分享的文本自动填入标题，图片/文件作为附件）。原生侧在 `MainActivity.kt` 用 MethodChannel + EventChannel 实现，冷启动与前台都支持 |

### 六项增强（上一轮）

| 需求 | 实现 |
|---|---|
| ① 任务可删除 | 详情页导航栏加 **⋯ 菜单**（标记完成 / 删除任务）；**修掉上游 bug**：原来从列表点进详情页后点「删除任务」是空操作（调用处把 `status == deleted` 的结果忽略了），现在会真正删除。*（⋯ 菜单已在最新一轮按要求移除，删除改由底部按钮承担）* |
| ② 标签库 | 标签存进 `optionsBox`，标签选择器分「标签库」（一键复用）+「新建标签」两段；新建的自动入库 |
| ③ 日历显示安排 | 所有日期选择弹窗的月历上都画事件圆点（课程=蓝、考试=粉、日程=橙、待办按完成状态），与主日历一致 |
| ④ 图片附件显示 | 图片附件显示缩略图；点击进入全屏预览页（双指缩放 + 分享），非图片仍走系统分享 |
| ⑤ 待办显示在日历 | 日历月视图给当天到期的待办画圆点，当日列表里也列出待办卡片（标题 / 截止 / 子待办进度） |
| ⑥ 直接打钩完成 | 日历当日列表的每条任务、任务页的日程卡片，都能直接点圆圈打钩完成 / 取消完成，不用进编辑页 |

### 反馈后的三个小修复

| 反馈 | 处理 |
|---|---|
| 标签输入框被键盘遮挡 | 所有底部弹窗（标签 / 日期 / 重复 / 其他日期面板）都加上了 `viewInsets.bottom` 内边距，键盘弹出时整块内容上移，输入框与「+」按钮完整可见 |
| 非主日历看不到具体安排 | 日期弹窗里**点某一天**，会在那一格正下方弹出附着的小卡片，列出当天安排（彩色圆点 + 标题 + 时间），点空白处关闭；无安排的日期不弹 |
| 任务页仍没有快捷勾选框 | 任务页所有卡片（待办 + 日程）左侧都加了圆圈勾选框，点一下即完成/取消，待办会自动同步用时与流程 |

## 功能对照

钉钉是两套界面：**新建待办**（极简快填）和**待办详情**（全量编辑）。本魔改同样拆成两个页面。

### 时间点弹窗（新建页与详情页共用）

| 钉钉弹窗 | 本魔改实现 |
|---|---|
| 月历选日期 | `showDateTimeSheet`：`2026年9月` + ‹ › 翻月、`日一二三四五六` 表头、今天灰底、选中蓝圆点、取消/确定；`withTime` 为真时下方带「时 / 分」滚轮 |
| 重复规则列表 | `showRepeatSheet`：不重复 / 每天重复 / 每周重复 / 每月重复 / 每年重复 / 每周工作日（跳过双休日）/ 自定义重复 ›，当前项打勾 |
| 自定义重复面板 | 重复周期标题 + 「每 N 天/周/月/年」+ 结束重复（无限重复 / 终止于某一天 + 日期）+ 取消/确定 |
| **其他日期面板** | `showTaskTimePanel`：把**截止时间 / 提醒时间 / 设置重复**三件事收在「其他日期」里，每行点开各自的弹窗，改动即时生效 |

重复规则落在模型上：每天=days/1、每周=days/7、每 N 周=days/7N、每月=month/N、每年=year/N、每周工作日=新增的 `weekday` 类型（自动跳过双休日）。「无限重复」用哨兵日期 `2099-12-31` 表示。

**重复的待办会真的滚动**：待办完成后自动生成下一次（用 `fromUid` 串起来，避免每秒重复生成），完成后旧的那条留在「已完成」。
> 注：这条逻辑一度被误判为 bug 而临时移除，经用户确认属预期行为，已恢复。

### 新建待办（`TaskCreatePage`，点列表右上角 + 进入）

| 钉钉截图元素 | 本魔改实现 |
|---|---|
| ✕ / 新建待办 / 新建 | 「新建」按钮在标题为空时置灰不可点 |
| 归属于 浙江大学 | *已移除*（DDL 与日程合并为统一待办） |
| 写下你的待办事项 | 大号标题输入框 |
| 添加描述 | 多行描述 |
| 我 执行 | 本地单用户，静态展示（点击提示无协作成员） |
| 今天 / 明天 / 其他日期 | 快捷截止日期：今天/明天带日期数字徽标（一键设成当天 23:59）；**其他日期打开时间面板**（截止时间 / 提醒时间 / 设置重复） |
| 下方摘要行 | 只读显示「截止 … · 每天重复 · 提醒 …」，不占功能位 |
| 优先级：普通 | 低 / 普通 / 高 / 紧急 |
| 添加待办标签 | 本地标签（新增字段），可加可删 |
| 添加附件 | file_picker 选择文件并复制到应用目录 |

### 待办详情（`TaskEditPage`，点列表卡片进入）

| 钉钉截图元素 | 本魔改实现 |
|---|---|
| 完成待办（圆角胶囊） | 点击切换完成/未完成，完成即把已用时置为预期用时 |
| 大标题 | 内联编辑任务标题（原 `summary`） |
| 添加描述 | 多行描述（原 `description`） |
| 截止时间 + 铃铛 + ✕ | 截止时间选择器；铃铛开启提醒（默认截止前 30 分钟）/再次点击改提醒时间；✕ 关闭提醒 |
| 优先级：普通 | 低 / 普通 / 高 / 紧急 四级，待办列表按「优先级 → 截止时间」排序 |
| 待办标签 | 同新建页，可增删 |
| 添加子待办 | 可勾选子待办 + 进度条 + 完成计数，点标题可改名 |
| 添加附件 | 同上（与新建页共用 `attachment_helper.dart`） |
| 评论 | 本地评论/备注列表（带时间），可删除 |
| ~~（原功能）时间安排~~ | **已移除**（界面不再显示用时/休息） |
| （原功能）时段设置 | 重复、重复周期、重复截止日期、不在日程中打扰 —— 保留（设了开始时间才显示） |
| （新增）倒计时 | 「距截止剩 X 天 Y 小时」每秒刷新，超时变红 |
| 参与人 | 未实现（无账号/服务器，做不了协作） |
| AI 拆解子待办 | 未实现（需外部 LLM API） |

## 改动文件清单

| 文件 | 改动 |
|---|---|
| `lib/model/task.dart` | 新增 `TaskPriority`、`SubTask`(typeId 14)、`TaskAttachment`(15)、`TaskComment`(16)；Task 增加字段 16–22（子待办/优先级/提醒开关/提醒时间/附件/评论/标签）与 `subtaskDoneCount`、`subtaskProgress`、`reminderTargetTime`、`remainingTime` |
| `lib/database/adapters/deadline_adapter.dart` | 新增 4 个适配器；`DeadlineAdapter` 写入字段数 16 → 23，读取对老数据做空值兜底（向后兼容） |
| `lib/database/database_helper.dart` | 注册新适配器 |
| `lib/utils/task_reminder.dart` | **新增**：任务提醒调度，签名缓存去重、精确闹钟失败自动降级为非精确 |
| `lib/utils/attachment_helper.dart` | **新增**：附件选择/复制/打开与大小格式化，两个页面共用 |
| `lib/design/tag_picker.dart` | **新增**：标签库 + 新建标签的选择弹窗 |
| `lib/design/image_preview.dart` | **新增**：附件缩略图 + 全屏图片预览页 |
| `lib/utils/calendar_events.dart` | **新增**：供日期弹窗查询某天安排的桥接函数 |
| `lib/design/date_picker_sheet.dart` | **新增**：钉钉风月历选择弹窗（月历 + 可选时间滚轮 + 事件圆点） |
| `lib/design/repeat_sheet.dart` | **新增**：重复规则列表 + 自定义重复面板，以及 `RepeatSetting` 模型映射 |
| `lib/design/task_time_panel.dart` | **新增**：「其他日期」面板（截止时间 / 提醒时间 / 设置重复） |
| `lib/design/task_priority_color.dart` | **新增**：优先级配色 |
| `lib/page/task/task_create_page.dart` | **新增**：钉钉风「新建待办」页（快捷日期、标签、执行人占位） |
| `lib/page/task/task_edit_page.dart` | **整页重写**为钉钉风详情页（类名与返回契约不变，调用点无需改动） |
| `lib/page/task/task_controller.dart` | 待办列表按优先级排序；每次刷新同步提醒通知；**重复的待办完成后自动生成下一次** |
| `lib/page/task/task_view.dart` | 列表卡片显示优先级标记与子待办进度 |
| `pubspec.yaml` | 加 `file_picker: ^8.0.0`；版本号 1.2.1+2 |
| `test/task_model_test.dart` | **新增**：5 个单元测试（新字段读写往返、子待办进度、提醒回落、深拷贝、老数据兼容） |
| `android/app/build.gradle` | applicationId 改为 `xyz.nosig.celechron.mod`；release 引用 proguard 规则；Kotlin compose 插件升 2.2.20 |
| `android/app/proguard-rules.pro` | **新增**：保留 Gson 泛型签名与 flutter_local_notifications 类，修 R8 压缩导致的提醒失效 |
| `android/app/src/main/AndroidManifest.xml` | 加通知/精确闹钟权限与定时通知 receiver；应用名 Celechron魔改 |
| `android/settings.gradle`、`android/build.gradle` | Kotlin 2.2.0 → 2.2.20 |
| `android/gradle/wrapper/gradle-wrapper.properties` | Gradle 8.13 → 8.14.3 |

## 构建

```powershell
pwsh -File D:\学习资料\celechron魔改\build_apk.ps1
```

产物：`Celechron\build\app\outputs\flutter-apk\app-release.apk`（当前 25.2 MB）

手动构建：

```powershell
cd D:\celechron-mod\Celechron
D:\flutter\bin\flutter.bat build apk --release --target-platform android-arm64 --no-tree-shake-icons
```

### 构建链上的三处必要调整

| 问题 | 现象 | 处理 |
|---|---|---|
| Gradle 版本过低 | `Gradle version (8.13.0) is lower than Flutter's minimum supported version of 8.14.0` | `gradle-wrapper.properties` 升到 `gradle-8.14.3-all.zip`（腾讯镜像） |
| Kotlin 版本过低 | `Kotlin version (2.2.0) is lower than Flutter's minimum supported version of 2.2.20` | `settings.gradle` / `build.gradle` / `app/build.gradle` 三处统一升到 `2.2.20` |
| `font-subset.exe` 被拦截 | 本机 Windows Application Control 策略阻止未签名程序，构建停在图标字体裁剪 | 加 `--no-tree-shake-icons` 跳过（图标字体全量打包，多几百 KB） |
| R8 压缩破坏插件 | `IllegalStateException: TypeToken must be created with a type argument`，`cancel()`/`zonedSchedule()` 全部失败 | 新增 `android/app/proguard-rules.pro` 保留 Gson 泛型签名与插件类 |

## 验证结果（已在你的华为 OCE-AN50 上实测）

| 验证项 | 结果 |
|---|---|
| 安装 | ✅ `adb install` 成功，与官方版 `xyz.nosig.celechron` 共存 |
| 启动 | ✅ 无崩溃，首页/任务页正常 |
| 详情页渲染 | ✅ 完成待办、标题、描述、截止+铃铛+✕、优先级、标签、子待办、附件、倒计时、评论全部就位 |
| 新建待办页 | ✅ ✕/新建待办/新建（标题为空置灰）、归属于、大标题、描述、我 执行、今天/明天/其他日期、优先级、标签、附件 |
| 快捷日期 | ✅ 点「明天」→ 截止变 9-09 23:59，卡片与详情页同步 |
| 标签 | ✅ 新建页添加 `tag1` → 详情页标签行正常显示 |
| 新建流程 | ✅ 填标题 → 「新建」可用 → 保存 → 列表出现「进行中」任务 |
| 日历弹窗 | ✅ 月历翻月、今天灰底、选中蓝圆点、取消/确定；截止时间弹窗带「时/分」滚轮 |
| 重复规则列表 | ✅ 七个选项与钉钉一致，当前项打勾，自定义重复带 › |
| 自定义重复面板 | ✅ 「每 2 天」「无限重复 / 终止于某一天」「请选择日期」全部可用 |
| 重复待办滚动 | ✅ 完成 9-09 的「每 2 天」待办 → 自动生成 9-10 的下一次（旧条目留在已完成） |
| 其他日期面板 | ✅ 截止时间 / 提醒时间 / 设置重复 三行齐全，各自弹窗联动，面板与创建页摘要同步刷新 |
| ① 删除任务 | ✅ 详情页 ⋯ → 删除任务，列表里立即消失（修复了上游的无效删除） |
| ② 标签库 | ✅ 建了标签「蜡笔」→ 再次打开标签选择器，「标签库」里可直接复用 |
| ③ 日历圆点 | ✅ 日期弹窗月历上出现蓝点（待办）与绿点（已完成） |
| ④ 图片附件 | ✅ 推入测试图 → 附件行显示缩略图 → 点击进入全屏预览（✕/分享） |
| ⑤ 待办进日历 | ✅ 日历当日列表出现「擦了1蜡笔 截止 2026-09-08 23:59」 |
| ⑥ 直接打钩 | ✅ 日历里点圆圈 → 任务变已完成；任务页日程卡片点圆圈 → 绿色对勾 |
| 反馈修复 1 | ✅ 标签弹窗呼出键盘后，输入框与「+」按钮都在键盘上方完整可见 |
| 反馈修复 2 | ✅ 日期弹窗点 9月8日 → 附着卡片显示「9 月 8 日 · 1 项安排 / 藏 / 截止 23:59」 |
| 反馈修复 3 | ✅ 任务页待办卡片点圆圈 → 直接进「已完成」 |
| 重复待办（已恢复） | ✅ 一度被误判为 bug 而移除，经确认属预期行为后已恢复原逻辑 |
| 优先级 | ✅ 选「紧急」→ 保存 → 列表卡片显示红色旗帜「紧急」 |
| 子待办 | ✅ 添加条目 → 显示「子待办 0/1」→ 保存 → 列表卡片同步显示 |
| 倒计时 | ✅ 「距截止剩 3 天 23 小时」实时刷新，超时变红 |
| 截止提醒 | ✅ 系统闹钟出现 `RTC_WAKEUP 2026-09-09 18:18` 精确闹钟（`exactAllowReason=permission`），无插件异常 |
| 删除任务 | ✅ 闹钟同步撤销（`reason=alarm_cancelled`） |
| 数据层 | ✅ `flutter test` 5/5 通过：新字段 Hive 读写往返、子待办进度、提醒回落、深拷贝、老数据兼容 |

## 安装

```powershell
C:\Android\Sdk\platform-tools\adb.exe install -r D:\celechron-mod-release.apk
```

包名不同，可与官方版共存；官方版数据不受影响，魔改版从空数据开始。

## 顺手修掉的上游 bug

1. **按 N 月/年重复会少推一天**：上游用「含时刻的时间差」取整天数（`nex.difference(startTime).inDays`），
   非零点任务每次重复都少一天（9/15 09:00 的「每月重复」变成 11/14 而不是 11/15）。改为用 `dateOnly(startTime)` 计算。
2. **详情页「删除任务」无效**：`TaskEditPage` 返回 `status == deleted` 的结果后，
   列表页与日历页的调用处都写成 `if (res.status != TaskStatus.deleted)` 直接忽略，于是删不掉。
   现在两处都会正确把它标记为已删除并刷新。

## 已知限制

1. **提醒依赖通知权限**：首次开启提醒时系统会询问通知权限；精确闹钟需要「闹钟与提醒」权限，被拒绝时自动降级为非精确调度（可能延迟几分钟）。
2. **提醒时间不会被自动前移**：只在你手动改过提醒时间、且该时间已失效（已过或晚于截止）时才按「截止前 30 分钟」重算；否则尊重你设的值。
3. **时区写死 Asia/Shanghai**：与仓库原有 `calendar_to_system.dart` 的做法一致。
4. **附件是本地副本**：选中的文件会复制进应用私有目录，删除任务不会自动清理文件。
5. **评论非协作**：纯本地数据，没有多人同步与 @ 提醒；「参与人」与「AI 拆解子待办」未实现。
6. **分析器在中文路径下会崩**：flutter_tools 的 LSP 消息在非 ASCII 路径下被截断，所以 `flutter analyze` 请用 `D:\celechron-mod\Celechron` 这个联接路径。
7. **release 包用 debug 签名**：与官方版签名不同，因此不能覆盖安装官方版（本方案改为共存包名，规避此问题）。

### 回滚：恢复上游的后台刷新逻辑（1.3 合并之后）

早前按需求做过两处「权宜改动」，1.3 跟上之后已按上游恢复：

| 早前的改动 | 现状 |
|---|---|
| 删掉 workmanager 后台周期刷新，改成「每天第一次打开才刷新课表」 | **已恢复上游逻辑**：启动即走统一入口 `_refreshRestoredScholar`，后台按设置里的开关注册 15 分钟周期任务 |
| 「推送成绩变动 / 推送作业截止提醒」两个开关失效 | **已恢复可用**（它们本来就依赖上面那个 worker） |

理由：本魔改的初衷是弥补 1.2 更新不及时带来的问题；合并到 1.3 之后上游自己是新的，
就不需要这类改动了。现在 `lib/main.dart` 与 `lib/page/option/option_controller.dart`
与上游完全一致，跟版冲突面进一步缩小。