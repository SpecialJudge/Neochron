# ⛔ 这份清单已经废弃，不要再往里加东西

> **状态：已归档（2026-09-25 由用户决定）。本文件只读，不再维护。**
>
> - **现在在用的清单是 [BACKLOG.md](BACKLOG.md)**，新东西一律加在那里。
> - 本文件里的条目**没有被否决，也没有被清理**，只是**不再是工作入口**：
>   它们是 Elychron 时代（到 v1.4.2 计划为止）的完整原始记录，有考古价值。
> - 本文件里的编号（`#13` `#20` `#24` `#28` `#29.x` 等）**仍然有效**：
>   代码与文档里的历史注释指向的就是它们，所以**这个文件不能删**。
>   但这批编号**不会被新清单续用**：新清单重新从 #1 开始编号。
> - 想捡起这里某一条：**在新清单 [BACKLOG.md](BACKLOG.md) 里重新登记一次**，
>   注明「出自本文件 #NN」并附链接，**不要直接改本文件**。
>
> 已经做完的事在 [MOD_NOTES.md](../MOD_NOTES.md)；跟上游合并的流程在
> [UPSTREAM.md](UPSTREAM.md)。

---

# 待办清单（以后再做，别忘了）

> 这份文件只记录**讨论过、有意做、但当下先不做**的事情。
> 每项都写清楚「为什么先不做」和「什么条件下该做」，避免以后想不起来当时的判断。
>
> 已经做完的事在 [MOD_NOTES.md](../MOD_NOTES.md)；跟上游合并的流程在
> [UPSTREAM.md](UPSTREAM.md)。

---

# ★ v1.4.2 计划（2026-09-17 起）—— 1.4.1 之后要做的事都堆到这里

> 用户 2026-09-17 拍板：「其他没做的部分也堆到 v1.4.2」。
> 1.4.1 只收**修 bug + 小改进**，任何"要新设计、要逐页真机验证、要等外部数据"的一律排到这里，
> 免得发布被无限拖住。

> **★ 发布口径（2026-09-17 用户澄清）**：
> 1. 「教程还没写完」指的是**教程内容**（还没写几篇），**不是**"手把手引导"那套架构；
> 2. **v1.4.1 与 v1.4.2 分两次发布** —— 1.4.1 等**教程内容**补齐就发；
>    手把手引导仍然排在 v1.4.2（评估见本页后面）。
> 3. 成品包已从项目目录清理（只留 v1.4.0 正式版归档在 `releases/`）；
>    `build_apk.ps1` 已改成构建后自动归档到 `releases/` 并打印 SHA-256。
> 4. **教程内容由用户手写**（`docs/GUIDE_V1.4.1.md`，正文里用【图片】占位并注明期望画面），
>    交给 AI 负责"截图 + 裁剪 + 写成 `lib/tutorial/modules/` 里的教程"。
>
> **★ 代码文案风格（2026-09-17 用户要求，以后一律照办）**：
> 1. **代码里不用直角引号**（「」）—— 注释与界面文案都不许出现，统一删掉；
> 2. **尽量不用破折号**（——）—— 需要停顿/解释的场合用逗号或冒号；
> 3. 已经全仓清理过一遍：`lib/` 与 `test/` 共 151 个文件、删掉 1690 个直角引号、
>    340 处 `——` 换成 `，`。**新写的代码别再加回来。**

## A0. ✅ 把一次刷新的"教务请求条数"砍下来（2026-09-17 深夜**已实施**）

详细记录见 `WHATS_NEW_1.4.1.md` 16.8。做了四件事：
历史学年只吃缓存（高年级 16 次 → 0 次）、**"已知为空"的学期当天不再问**
（真机：一次刷新省 4 个请求）、探针学年只探第一个学期、
**探针学年被限流不再拖垮整个课表**（真机：失败 1 → 0，课表从"没连上"变"走缓存"）。
外加后台 15 秒/请求的慢节奏 + 启动刷新 5 分钟节流。

**还可以继续做的（没做，收益/风险都要再评）**：
- 当前学年仍然是 4 个学期都查（真机日志：1|秋 22 条、1|冬 21 条、2|春/2|夏 各 0 条 ——
  后两个已被"已知为空"覆盖，实际每天只多问 2 次）；
- 成绩 / 主修 / 考试 / 作业 / 实践 这 5 个模块**仍然每次刷新都查**：它们的变化频率其实不同
  （成绩一天一次足够、作业可能要勤一点），按频率分档还能再省几个请求。

## A0-old. ★ ~~下一轮优先做~~（已完成，留档）

**问题**：教务对"一个时间窗内的请求条数"限流（HTTP 921）。现在一次刷新会打 13~21 个请求：

- 课表按 **学年 × 学期** 逐个查（`1|秋``1|冬``2|春``2|夏`），学年范围是"入学年 → 当前年 + 探针年"：
  26 级 = 2×4 = **8 次**，23 级 = 4×4 = **16 次**；
- 再加成绩 / 主修 / 考试 / 作业 / 实践 各一次。

**改法（未实施）**：**历史学年不必每次刷新都查** —— 过去学年的课表/成绩基本不变，
只在「本地没有这份数据」或「距上次 ≥7 天」时拉一次；当前学年 + 探针学年照旧每次查。
预期把一次刷新压到 6~9 个请求，落回限流阈值以内。

⚠️ 这条会碰**刷新主链路**（数据缺失会让历史学期消失），**必须配真机验证**再上。
相关背景见 `WHATS_NEW_1.4.1.md` 第十六节（尤其 16.7 那条"串行化没解决问题"的诚实记录）。

## A. 已经在排队、只等条件成熟的

| 项 | 事情 | 卡在哪 |
|---|---|---|
| 13.1 | **手把手引导（在真实页面上高亮按钮 + 气泡说明）** | **工程量偏大**，见下面的评估 —— 用户已同意挪到 v1.4.2 |
| 23 | 海宁校区自定义节次（"课表时间段问题还是很严重"） | **等用户给一份节次对照表**（几点上第几节）；没有数据无从下手 |
| 29.8 | 有成绩时点"没排进课表"的课的卡片 | 代码已堵住（显示「课表里没有这门课」），但**我们手上没有"有成绩"的真数据**可验证 |
| 29.11 | AI 子待办 5→7 的**端到端**验证 | 要真调一次 AI；改的是常量，等用户下次用 AI 时留意 |
| 29.4 / 20 | **专注的「该休息了」在后台/锁屏不响** ★ 用户点名 | 现在是页面 tick 触发的前台通知，锁屏后定时器被系统挂起 → 要像待办提醒那样预排进系统（`zonedSchedule`） |
| 25 | 课前提醒的**投递方式** | 算法早就写好了（`class_reminder_rule.dart`，30/10 分钟），只差"投到哪"（系统日历提醒 vs App 通知） |

## B. 用户提过、还没排期的

- **29.5** 每节课只要专注过就**自动生成复习**待办（设置里可选）—— 要设计：去重、算不算已完成、挂在哪天
- **29.7** 翻页**再快一点**（1.4.1 只调了"防误触阈值"，动画时长还没动）
- **29.9** 长按登录 → 看**哪些接口/子站是通的**（和现有「诊断与测试」有重叠，考虑合并）
- **27 / 28** 桌面小组件：等"点了没反应"有解再解封；解封时把那四个小遗留一起做
- **24** 课程实体框架的剩余部分（框架与课程详情已完成，剩下的见该条目）
- **21** P5 数据清理：把已删除的「时间规划」残留字段真正迁掉
- **22** 专注统计补「按标签分布」+ 中断率（1.4.1 已补「按课程」，标签与中断率还没）
- **9** 备份文件的自动清理
- **19** 导航时间计算服务（高德）—— 让提醒提前量变成动态的
- **7 / 17** AI 的后续能力与远程模型清单

## C. 更大的、要单独开一轮的

- **1 / 2 / 8** 局域网面板：附件同步、手机端快速入口（入口现在是关的）
- **3** WebDAV / 坚果云跨网络同步（用户 2026-09-16 明确"接受坚果云这种更可靠的方法"）
- **4** Windows 桌面客户端
- **15** 上架国内应用商店
- **11** CI 的取舍
- 教程内容扩充：课表 / 专注 / AI / 同步 各一篇（**图片管线已经做好，见 `docs/TUTORIAL.md`**）

## ★ 手把手引导（页面内高亮教学）的工程量评估（2026-09-17）

**结论：偏大，建议按用户拍板放到 v1.4.2。** 理由逐条：

1. **要拿到"目标控件在屏幕上的真实位置"**：得给目标挂 `GlobalKey`、拿 `RenderBox.localToGlobal`，
   而且目标可能在 `ListView` 里还没构建（要先滚动到它）、页面还在加载（要重试）；
2. **位置必须实时跟随**：滚动、旋转、键盘弹出、下拉刷新、数据刷新都会让高亮框跑偏；
3. **遮罩要"挖洞"**：`CustomPainter` + `Path.combine(PathOperation.difference)`（或 `BackdropFilter`），
   还要处理深色模式、刘海/挖孔、手势条安全区；
4. **跨页跳转**：引导要从"待办页 → 详情页 → 设置页"这样一路走，每一步都要等目标页就绪；
5. **必须能优雅失败**：目标控件改名/挪位置/被隐藏时，这一步应当**跳过**而不是卡住或崩 ——
   而"找不到就跳过"意味着一套降级策略与日志；
6. **打断处理**：返回键、切后台、被杀掉、用户中途退出，都要能恢复或干净结束；
7. **和现有体系打通**：进度/已看过/不再提示要走 `TutorialStore`，不要另起一套；
8. **几乎没法自动化测**：几何与时序主要靠真机逐页走一遍，**每加一个功能就得重新走一遍**。

估：**约 600 ~ 1000 行 + 逐页真机验证**，比自己写一篇"一步一页"的教程（现在的形态）高一档。
**1.4.1 里能做的近似物已经有了**：教程的 `TutorialActionStep` 就是"讲完一步 → 一键跳到那个页面自己试"，
先把这条用好（成本几乎为零）。

---

## 当前进度（2026-09-11 更新）

**P1「四种时间语义」已完成并真机验证** —— 活动 / 截止 / 提醒 / 备忘全部落地：

- 数据层 `TaskType` 追加 `remind`(3) / `memo`(4)，**没有新增任何 Hive 字段**
- 创建页与详情页都有四个类型胶囊；卡片、日历、筛选全部按类型说话
- 提醒锚点修正：活动锚「开始」、截止锚「截止」、提醒就是那一刻、备忘不提醒
- 设置里有「默认提醒提前量」（默认 30 分钟）；活动结束自动归档到「我已处理」
- AI 会划分类型（`kind` 字段 + 白名单 + 一致性校验，改动一律写进 warnings）

详见 [TIME_MANAGEMENT_DESIGN.md](TIME_MANAGEMENT_DESIGN.md) 的「P1 进度」表。

**P2「子待办行程表」也已完成** —— 子待办带时间/地点、详情页时间轴（进行中高亮、
已过去未完成标红）、每一步单独提醒（只走通知）、点通知进详情页并高亮那一步、
卡片显示「下一步」、AI 拆解升级成带时间的结构化步骤。

**P3「专注计时」已完成** —— 默认 60 / 15 可调，可暂停 / 跳过休息，工作↔休息自动
交替，休息开始可选提醒；会话落 `dbFocus`（typeId 20），专注时长累加到
`Task.timeSpent`；入口在详情页「开始专注」与待办页右上角的计时器图标（自由专注）。

**下一步是 P4（专注统计页）**：周/月总时长、日均曲线、按任务/标签分布
（"这周写代码 12 小时"）、轮数与中断率 —— 数据都已经躺在 `dbFocus` 里了。

### P2 留下的两件事 —— ✅ 都已完成（2026-09-12）

- ~~AI 补全行程~~：**已删**（2026-09-12）。用户指出它没有用武之地：
  ① 子待办是手动加的 → 用户自己会写时间；② 子待办是 AI 加的时候 → AI 本来就会带时间。
  我当初设想的第三种场景（用户手写步骤、但原文里其实有时间）太窄，不值得为它
  多一个按钮、一段提示词和一次 API 调用。**删掉的东西**：`fillItineraryFor`、
  `AiItineraryFill`、`applyItineraryFill`、详情页入口、4 个测试，以及只为补全模式
  加的解析参数（`expectedCount` / `keepOrder`）。
- **预览里交互式改步骤**：`AiTaskDraft` 是不可变对象，做「删单条 / 改时间」要加
  copyWith 与局部状态；现在填进去后每一步都能在详情页里点开改，够用。
  **触发条件**：AI 经常拆出某一条不想要的步骤。

---

## 接下来能做什么（2026-09-12 排序）

> 用户问「还有哪些事可以做」时，从这里看。分三档：**想做/该做**、**等你用出感觉再说**、**可有可无**。

### A. 该做的（有明确价值，随时可以开工）

| # | 事情 | 为什么 |
|---|---|---|
| **23** | **公开发布 Elychron v1** | 功能已经够了，卡的是工程/合规收尾。清单见 `RELEASE.md`。只有两件必须仓库主本人做（keystore、仓库改 public）|
| 20 | **专注休息提醒在后台/锁屏不响** | 现在的「该休息了」是页面 tick 到点触发的前台通知；锁屏后 Dart 定时器会被系统挂起，提醒就丢了。要像待办提醒那样**预先把通知排进系统**（`zonedSchedule`）✓ 这是自己实现时就知道的一个真实缺口 |
| 21 | **P5 数据清理：把已删除的「时间规划」残留字段迁掉** | `Task.timeNeeded/isBreakable/blockArrangements`、`SubTask` 上的同类字段、`optionsBox` 里的 `workTime/restTime/allowTime` 现在都是死字段（**只保留着不删**是为了 Hive 序号安全）。做一次真正的迁移就能把它们清掉 |
| 22 | **专注统计补「按标签分布」+ 中断率** | P4 的剩余部分。数据已经在 `dbFocus` 里，主要是 `Task.tags` 的关联与展示 |

### B. 等你用出感觉再说（触发条件写在各自条目里）

- **1 局域网面板美化**、**2 附件同步**、**3 WebDAV 跨网同步**
- **7** AI 的自然语言改期 / 周报摘要 / 自动打标签
- ~~P2 留下的两件事~~：AI 补全行程（**已删**，无用武之地）、预览里交互式改步骤（随预览一起删掉，改步骤能力搬到了新建页）
- **8** 局域网同步的快速入口、**9** 备份文件自动清理
- **19** 高德导航时间计算（提醒提前量动态化）—— Key 已备好，等 P1 之后排期，现在 P1 已完成

### C. 可有可无 / 长期

- **4 Windows 桌面客户端**（工作量比现在大一个量级）
- **5 外观统一 + 壁纸色系**、**13 换图标与 Logo**
- **10 两处整页重写的搬迁**（观察项，上游改那两页时才评估）
- **11 CI 的取舍**、**15 上架国内应用商店**（需要 APP 备案）
- **12 标签库历史问题**（已结案，只在复现时再看）

---

### 23. 公开发布 Elychron v1（CC98 分享）★ 发行这条线

- **现状**：功能层面已经远超发布门槛（四种时间语义、子待办行程表、专注计时、
  AI 整理、局域网同步都是上游没有的）；卡的是**工程与合规收尾**。
- **完整清单在 [`RELEASE.md`](RELEASE.md)**，这里只记状态：
  - **P0（不发不行）**：① 更新检查还指着 `api.celechron.top` ✗
    ② release 用的是 **debug 签名** ✗ ③ 图标与 `assets/logo.png` 还是上游美术 ✗
    ④ ~~仓库私有 → GPLv3 要求源码可获取~~ ✅ **已完成**：新仓库
    [Elyyyyyyyyxer/Elychron](https://github.com/Elyyyyyyyyxer/Elychron) 已建（public），
    历史里的旧身份已用 `git filter-repo` 全量切割（元数据 65 个提交 + 内容 3 个提交），
    旧仓库待仓库主删除
  - **P1（建议）**：版本号改 `1.4.0-elychron.1`、~~修「专注锁屏提醒」~~ ✅（已排进系统并
    用 `dumpsys alarm` 验证）、README + 截图、真机回归、PRIVACY 复核
  - **P2**：发布后再说
- **只有仓库主能做的两件**：删除旧仓库；生成并**备份** keystore
  （⚠️ 证书 DN 用中性信息，别填真实姓名/学校邮箱 —— 它写进 APK 后谁都能看到）。
- **为什么值得现在做**：Elychron 与上游的区分度已经足够（名字、四类语义、专注、
  AI、电脑端），再拖只会让「发行收尾」越积越多。
- **触发条件**：想发的时候 —— 按 `RELEASE.md` 第九节的 checklist 走一遍即可。

## 一、功能

### 1. 局域网面板美化 + 更像 PC 客户端

- **现状**：面板（`lib/mod/lan_panel.dart`）是能用的素净样式，功能上只支持
  看待办 / 打钩 / 删除 / 新建 / 导出 / 导入。
- **以后要做**：视觉重新设计；支持编辑描述、子待办、标签、优先级、地点；
  也许加个看板/日历视角。
- **为什么先不做**：功能先跑通更重要；面板是纯前端单文件，改样式随时能改，不欠技术债。
- **触发条件**：局域网同步实际用起来、觉得「能用但难看」的时候。

### 2. 附件也跟着同步

- **现状**：`DataBundle` 只同步待办与标签，**不同步附件文件**。电脑面板上看到的
  附件条目在手机上有，但文件本身不会传。
- **以后要做**：附件按内容 sha1 命名，用同一个 HTTP 服务提供下载/上传端点，
  PC 端按需懒下载（不一次性全拉）。
- **为什么先不做**：附件是大文件，传输/断点/清理策略要单独设计，
  先让「文字待办」同步站稳。
- **触发条件**：用户反馈「照片/文件只能在手机上看」的时候。

### 3. WebDAV 作为可选的跨网络同步

- **现状**：只做了局域网直连（必须同一 Wi-Fi）。
- **以后要做**：`WebDavTransport`，让用户填**自己的**坚果云应用密码，
  实现真正的「在外面也能同步」。
- **重要澄清**：用户填自己的账号密码是**完全合规**的标准 WebDAV 客户端做法；
  不行的只是把**开发者自己的**账号密钥内置进 APK 给所有人共用。
- **为什么先不做**：对师生推广来说「注册坚果云 + 生成应用密码」门槛太高，
  不适合做默认路径；但作为**可选高级功能**很有价值。
- **触发条件**：局域网同步满足不了「不在同一 Wi-Fi 也要同步」的需求时。
- **工程上很便宜**：数据层是传输无关的（`DataBundle` + `DataMerge`），
  加一个 transport 是加法，不是重写。

### 4. Windows 桌面客户端（形态 B）

- **现状**：只做了形态 A（手机当服务器 + 浏览器面板）。
- **以后要做**：`flutter build windows` 出真正的桌面端，手机⇄电脑 都是 App。
- **主要障碍（已实测）**：
  - `workmanager`（上游自定义 fork，只支持 Android/iOS）**11 处**
  - `flutter_local_notifications`（不支持 Windows）**18 处**
  - 好消息：上游已有 `PlatformFeatures` 抽象（11 处）、`Platform.is` 23 处，
    基础不是从零开始
  - 已确认支持 Windows 的：`flutter_secure_storage`、`path_provider`、
    `file_picker`、`share_plus`、`qr_flutter`、`hive`、`table_calendar`
- **为什么先不做**：工作量比形态 A 大一个量级，而形态 A 已经能解决「电脑上看待办」。
- **触发条件**：真的需要在电脑上离屏使用（比如桌面上常驻提醒）时。

### 5. 外观统一 + 壁纸 / 色系

- **现状**：闹钟页有四套配色（`lib/design/alarm_theme.dart`，天依蓝/爱莉粉/清新绿/
  静谧紫），其它页面各写各的颜色。
- **以后要做**：抽一层统一 AppTheme；壁纸；色系跟随；闹钟配色与全局配色打通。
- **为什么先不做**：纯视觉，不影响功能；而且改起来会碰到大量上游文件
  （与我们「少碰上游文件」的接缝原则冲突），值得先把接缝压缩做完再动。
- **触发条件**：功能告一段落、想打磨观感的时候。

### 6. ~~排程算法改进（「接下来」页）~~（**已作废**）

> 排程功能在 P1 里被**整体删除**（`lib/algorithm/arrange.dart`、「接下来」页、
> `Option.workTime/restTime/allowTime`、`Task.updateTimeSpent()` 全删了）。
> 「什么时候做」这件事现在交给**四种时间语义**（自己定时间）+ **专注计时**
> （自己记实际花了多久），不再由算法排班。本项不再需要。

### 7. AI 智能化的后续能力

- **已经做完的**（见下面第 16 项）：分享消息/截图 → 草稿、拆子待办、
  **划分四种时间语义**、**行程型子待办带时间地点**、标签复用。
- **还能做的**：
  - **自然语言改期**：「这周四之前」直接把已有待办改期（对话式修改，不是新建）
  - **每周回顾摘要**：把这一周的完成/逾期/专注时长写成人话
  - **按描述自动打标签**：现在只在新建时给标签，改已有待办时不打
  - **AI 补全行程**：给清单型子待办补时间/地点（见「P2 留下的两件事」）
- **模型名会变**：DeepSeek 的模型名随发布变动（`deepseek-chat` → `deepseek-flash`
  就是这么变的）。`AiConfig` 里有一张过时名字的迁移表，官方再改名时更新那张表即可。
- **触发条件**：真的在用 AI 了、并且发现上面某件事每次都得手动做。

### 8. 手机端「局域网同步」的快速入口

- **现状**：入口在 设置 → 数据 → 局域网同步（三层）。
- **以后要做**：待办页右上角 ⋯ 菜单里加一个快捷入口 / 状态指示。
- **为什么先不做**：设置页里能找到，不急；且待办页是在持续跟上游改的文件，
  加东西要谨慎（接缝原则）。
- **触发条件**：发现自己每天都得点三层进去的时候。

### 9. 备份文件的自动清理

- **现状**：每次导出/导入都会往 `文档/celechron_backup/` 里写文件
  （`before-import.json` 每次覆盖，导出文件是新的时间戳文件），**不会自动清理**。
- **以后要做**：限制保留份数（比如最近 10 份），旧的自动删。
- **为什么先不做**：文件很小（几十 KB），短期内不会占满。
- **触发条件**：备份目录超过 20 份或用户反馈占空间时。

---

## 二、工程 / 接缝

### 10. 两处「整页重写」的搬迁（观察项）

- **现状**：`lib/page/task/task_view.dart`（417 行）与
  `lib/page/task/task_edit_page.dart`（1205 行）是整页重写的，改动量最大。
- **要不要搬到 `lib/mod/`**：**先不搬**。理由：它们**至今零冲突**
  （1.3 没动这两个文件），而搬出去会让上游对这两页的修复不再自动合并进来。
- **触发条件**：**上游改了这两个文件、并且产生冲突时**，再评估搬迁。
  平时每次跟版留意一眼即可。

### 11. CI 的取舍（可选）

- **现状**：`.github/workflows/code_check.yml` 已换成魔改版自己的检查
  （只对 error 敏感 + 跑全部单测，2026-09-11 是 **254 个**）。
- **可选做法**：如果在意私有仓库的 Actions 配额（免费 2000 分钟/月），
  可以整个删掉 `.github/workflows/`。
- **触发条件**：收到配额告警，或者觉得每次推送都跑 CI 没必要。

### 12. ~~标签库曾被清空的历史问题~~（已查明并修复）

> **已结案**：根因不是"被清空"，而是**标签库从来没有被这些来源写入过**——
> 只有「标签管理页编辑」「选择器里新建标签」「导入数据」三条路径会写库，
> 而 **AI 生成待办时直接给任务设标签**、局域网同步合并、以及旧数据里的标签
> 都只存在于待办身上。表现就是：筛选器看得到（它取的是「标签库 ∪ 待办标签」），
> 标签管理页和标签选择器却是空的（它们只读标签库）。
>
> 修复：`lib/mod/tag_harvest.dart` 做标签库自愈——TaskController 初始化时
> 把待办（含子待办）身上的标签补进库；AI 应用草稿时也会记住用过的标签。
> 这样 AI 复用已有标签的提示词也才真正有料可用。

- **现状**：早期发生过两次标签库被清空，当时的处置是加防护
  （`setTagLibrary` 拒绝空写入、标签管理页保存时与加载值做比较）并恢复了数据。
  **根因没有查明**，之后也没再复现。
- **以后要做**：如果再复现，抓 logcat + Hive 状态查一次。
- **为什么先不做**：无复现路径，盲改反而有风险。
- **触发条件**：**再次复现时**。

---

## 三、分发 / 合规

### 13. 应用图标与 Logo —— ✅ 已完成（2026-09-11）

- 图标与 `assets/logo.png` 已换成自己的（`ed00918 feat(ui): 应用图标与主题色系更换为爱莉希雅粉`），
  已确认**与上游美术不同**、且该提交不在上游历史里。
- 主题色 `0xFFFF699A`（爱莉希雅粉）与之配套。
- ⚠️ 唯一提醒：这套美术若是网上找的**同人图/官方设定图**，公开发布时属于第三方 IP
  （非商业学生项目实践上风险很低，但不是零）；如果是自己画的就是完全干净的。
- 触发条件：已完成。
- **触发条件**：正式对外分发前（GitHub Release 可以先不管，上架商店必须换）。

### 14. 名字再确认一次 —— **已确定：Elychron**（2026-09-11）

- **现状**：显示名已统一为 **`Elychron`**（Android `android:label`、iOS
  `CFBundleDisplayName` / `CFBundleName`、macOS `PRODUCT_NAME`、桌面端窗口标题、
  关于页、局域网面板、通知与诊断日志文案、README / PRIVACY 全部改过）。
- **仓库与身份（2026-09-12）**：新仓库
  [Elyyyyyyyyxer/Elychron](https://github.com/Elyyyyyyyyxer/Elychron) 已建，旧身份
  `Eidosiny` 已从**元数据 / 历史内容 / 提交信息 / 工作区**四个层面全部切割
  （`git filter-repo`，改写了 65 个提交的元数据 + 3 个提交的内容）。
- **为什么换**：`Telechron` 是 20 世纪美国时钟品牌，在「钟表」类目有历史商标
  注册记录；`Elychron` 是自己造的词，没有历史包袱。
- **刻意没改的内部标识**（改了会出真问题，且用户看不到）：
  - `applicationId xyz.nosig.celechron.mod` —— 改了等于换一个 App，
    Hive 里的待办/成绩/设置**全部读不到**；
  - Dart 包名 `celechron`（`package:celechron/...`）—— 改了要动 600+ 处 import，
    而且以后跟上游合并会全线冲突；
  - Android `android:scheme="celechron"` 深链、Kotlin MethodChannel 名、
    Hive box 名、`api.celechron.top` 上游接口 —— 都是**接线用的身份**，不是显示名。
- **触发条件**：已完成。

### 15. 上架国内应用商店

- **现状**：只走 GitHub Release（点对点发 APK 没有备案检查点）。
- **以后要做**：需要**你自己的** APP 备案（上游的备案号与主体绑定、不可转让、
  已从关于页移除）+ 通常还要软著。
- **为什么先不做**：备案要时间和主体资质，先让应用本身成熟。
- **触发条件**：想让浙大师生在应用商店里直接搜到的时候。

---

## 四、AI 智能化（正在做，进度记在这里）

### 16. 接入 DeepSeek API —— **已完成**（2026-09-11）

- key 存系统密钥库、设置里有「测试连接」、默认关闭且明确告知文本会发给模型服务商 ✓
- 分享消息 → 草稿 ✓ 分享截图 → 视觉识别 ✓ 详情页「AI 拆成子待办」 ✓
- **P1/P2 之后它还能划分四种时间语义、并给出带时间地点的行程步骤** ✓
- 剩下的想法都归到第 7 项（自然语言改期、周报摘要、自动打标签）。
- 待办：`PRIVACY.md` 的措辞已经跟上（AI 默认关闭 + 只发给用户自己配置的服务商）✓

### 17. 远程模型清单（`model_manifest.json`）

- **现状**：模型名已经做到自动解析（`GET /models` + 别名优先 + 失败自愈，
  见 `lib/mod/ai/model_resolver.dart`），**日常改名已经不需要发版**。
- **以后要做**：在仓库里放一个静态 `model_manifest.json`，由 GitHub Action
  （或手动）维护，应用每 N 天拉一次。它能处理 `/models` **解决不了**的事情：
  1. 官方同时提供便宜档与贵档时，**哪个更适合本应用**（`/models` 只给名字，不给语义）
  2. 某个名字被**静默转路由**（比如 `deepseek-v4-pro` 从 2026-09-14 起被转到
     V4.1 Flash）——这种"名字还在但行为变了"的情况只能靠人写进去
  3. 已知废弃名 → 新名的映射表（比代码里那张硬编码的迁移表更好维护）
  4. 能力标记：是否支持 JSON 模式 / 多模态 / 上下文长度（影响功能开关）
- **代价**：需要有人维护这份文件；应用多一次"只拉静态文件"的请求（不含任何用户数据）。
- **触发条件**：
  - 官方出现"名字没变但行为/价格变了"的情况时
  - 或者我们发现要在代码里临时加特例才能兼容某个模型时
  - 或者要开始按模型能力切换功能（比如只对多模态模型开放"截图识别"）时
- **接口建议**：`GET https://raw.githubusercontent.com/<repo>/main/model_manifest.json`，
  结构 `{ "updatedAt": "...", "recommended": "deepseek-flash", "avoid": [...], "renames": {...} }`，
  拉取失败就完全忽略（本地解析结果照常用）。


### 18. ~~给「排程死循环」补回归测试~~（**已作废**）

> 排程功能（`lib/algorithm/arrange.dart`）已整体删除，对应的 `test/arrange_test.dart`
> 也一并删除。本项不再需要。**不过那次踩的坑留下了资产**：`lib/mod/loop_guard.dart`
> 现在用在日程滚动生成等其他循环里，仍然有价值。

### 19. 导航时间计算服务（高德）—— 让提醒提前量变成动态的

- **动机**：现在的提前量是一刀切 30 分钟 ✗。可「去龙湖西溪天街集合」要骑车 25 分钟，
  而「交个作业」只是点开网页 1 分钟 ✓ —— 同一个 30 分钟，对前者太晚、对后者太早。
- **设想**：对有**地点**的活动/截止，按「当前位置 → 地点」算路网通行时间，然后
  `提醒时间 = 锚点 − 通行时间 − 缓冲（默认 10 分钟）`；地点没填或算不出就退回默认提前量 ✓
- **前提**：
  - 需要定位权限（首次使用时请求 ✓）
  - 需要用户自备**高德 Web 服务 Key** ✓ —— **绝不要硬编码进仓库** ✗ 按 DeepSeek Key 的既有做法
    存 ``FlutterSecureStorage``、在设置里让用户自己填 ✓
  - 高德 Key 用户已提供 ✓ 完整值存在本地未入库文件 ``.local-secrets.md``（已 gitignore ✓）
  - 计划用的接口：路径规划 v5（driving / bicycling / walking ✓）
- **为什么先不做**：依赖定位权限与第三方配额，且必须先把四种时间语义落地
  （没有「活动」类型就没有稳定的"开始前"锚点 ✓）。等 P1 完成后再排期 ✓

### ~~18. 给「排程死循环」补回归测试~~（**已作废**）

> 排程功能（`lib/algorithm/arrange.dart`）已整体删除，对应的 `test/arrange_test.dart`
> 也一并删除。本项不再需要。**不过那次踩的坑留下了资产**：`lib/mod/loop_guard.dart`
> 现在用在日程滚动生成等其他循环里，仍然有价值。

---

## 五、专注（P3/P4）留下的

### 20. 专注的「休息提醒」在后台 / 锁屏时不会响 ★ 建议尽早修

- **现状**：休息提醒是在页面每秒 tick 到「工作段走完」的那一刻，当场调
  `TaskReminder.showFocusRestNotice()` 弹一条通知。App 在前台时没问题，
  但**锁屏或切到后台后，Dart 定时器会被系统挂起**，那一秒根本不会到来 ——
  等用户回到前台时，它才补算并立刻弹出（已经晚了）。
- **要做的事**：像待办提醒一样**预先把通知排进系统**：
  - 工作段开始时 `zonedSchedule` 一条「该休息了」，时间 = 工作段结束时刻
  - 暂停 / 跳过休息 / 提前结束时 `cancel` 掉它
  - 通知 id 用固定值（一次专注只有一条）
  - 后台补算到点后仍有可能重复弹一次，用「已经排过系统通知」的标记去重
- **为什么值得做**：专注计时最典型的用法就是**锁屏把手机扣在桌上**，
  而现在的提醒恰好在这种用法下失效。
- **代价**：小。`TaskReminder` 里已经有现成的插件、时区、精确闹钟权限与渠道。

### 21. P5：把「时间规划」的残留字段真正迁掉

- **现状**：P1 删功能时**只删代码、不删字段**，因为 Hive 按**序号**读写，
  删字段会让后面所有字段的序号前移、读错老数据。留下的死字段：
  - `Task.timeNeeded(4)` / `isBreakable(8)` / `blockArrangements(14)`
    —— `timeSpent(3)` **已经复用**为专注累计，不能删 ✓
  - `SubTask` 上的同类字段（3/4 位）
  - `optionsBox` 里的 `workTime` / `restTime` / `allowTime`
- **要做的事**：写一次迁移：读旧数据 → 用新的 adapter（字段序号已经理顺）
  重写一遍 → 备份原 box 后再替换。**迁移必须可回滚**（先导出备份）。
- **触发条件**：有整块时间、且愿意为此做一次完整的回归时。
  在此之前维持现状是**安全**的 —— 死字段只占几十字节，不影响任何行为。

### 22. 专注统计补「按标签分布」与中断率

- **现状**：P4 做了今天/本周/本月、最近七天、按专注对象分布、逐条明细。
- **要做的事**：
  - **按标签**：一条待办可能有多个标签，要定口径（一条会话计入它关联待办的所有标签？
    还是只算第一个？），再展示「这周写代码 12 小时」这类结论
  - **中断率**：`FocusSession.completed == false` 的比例 + 「平均每轮多长」
- **为什么先不做**：标签维度的口径没有真实使用反馈时容易做错（多标签会重复计数）。

---

## 六、课表与课程（2026-09-14 记）

### 23. 秋/冬（春/夏）课程的半学期归属混乱 ★ 等当事人回话（2026-09-15：他说已经正常了）

- **2026-09-15 晚当事人回话**：装了我们给的 `1.4.0-debug`（build 7/8）之后说
  **「debug版好像恢复正常了，没有出现我说的情况了」**。
  注意那个包**就是从 1.4.1 工作分支构建的**（只是版本号临时写成 debug），所以他看到的
  "恢复"其实是 1.4.1 的改动生效 —— 最可能是 `ClassHalfRule`（研究生/自定义重复那条），
  也可能是这次刷新终于拿到新数据（zdbk 一直 HTTP 921 限流，课表长期来自缓存）。
  他附的反馈文本是空的（只有脱敏日志，还截断了），所以**只有"现在正常了"这一句可用**。
  → 不再继续追；**保留诊断代码**，他若再遇到就看 `操作=halfDiag` 两行。
- **现象**（由别的同学反馈，我们尚未复现）：**冬学期的课和秋学期的课出现在同一天**。
- **已排除**：不是校历日期算错。真实校历 `2026-2027-1` 的两半是**相邻不重叠**的
  （秋 `2026-09-14 → 2026-11-08`，冬 `2026-11-09 → 2027-01-03`），可用
  `curl http://calendar.celechron.top/2026-2027-1.json` 复核。
- **已定位到的可疑机制**（三处叠加，但**数据不足以下结论**）：
  1. App 分别用四个季节查教务：`ugrs_spider.dart` 里
     `for (var season in ['1|秋', '1|冬', '2|春', '2|夏'])`；
  2. 教务行里的 `xxq` **经常读不出来**（`test/calendar_config_parser_test.dart` 的实测注释：
     "会缺失或只给数字码"），这时按**查询用的季节猜**（`session.dart` 的
     `_halfFlagsFrom(requestedSeason)` 兜底）；
  3. 同一门课出现"秋 + 冬"两个 session 时按**或**合并
     （`course.dart` 的 `currentSession.firstHalf || session.firstHalf`）。
  → 于是一门"只有冬学期上"的课，若被秋查询也返回且 `xxq` 读不出，就会被并成"两半都上"。
- **⚠️ 为什么不能凭猜改**：`xxq` 缺失时"这门课到底算哪一半"**数据里真的没有**；
  改错方向的直接后果是**整张课表被滤空**（`session.dart` 的注释记录了当初就是
  因为这个才加的兜底）。必须拿到真实证据再动。
- **2026-09-14 拿到当事人证据后定位完成 —— 其实是两条完全不同的路**：

  | | 当事人 1 | 当事人 2 |
  |---|---|---|
  | 课程 | 物理光学实验（**研究生课** OPT3011M，周一 6-9 节）| 能源工程伦理与管理（**本科课**）|
  | 现象 | 「日程→接下来」里出现，只有冬学期的课跑到秋学期 | 秋一周的某天里出现，并与秋课重叠 |
  | 数据路径 | 研究生院 → `customRepeat`（按 `zc` 周次展开）| 教务 → 半学期标记 + 单双周 |
  | **根因** | 自定义周次展开**只看周次数字** | `xxq` 读不出时**按查询季节猜** + 合并时 OR |
  | 状态 | ✅ **已修**（`lib/mod/class_half_rule.dart`，9 条测试）| ⏳ 待一条真实 `xxq` 值 |

- **当事人 1 的根因（已修）**：`Semester._buildPeriods` 里按周上课的课走
  `(week - 1) ~/ 8` 判断属于哪一半 —— **第 1-8 周一律算上半学期**。
  但研究生院给的 `zc` 是**该课程所在半学期内部**的周次：只有冬学期的课给的也是「1-8 周」
  → 被排进秋天。**课表（两周表格）正常、只有「接下来」不对**，正是因为课表走的是
  不按日期展开的另一条路 —— 当事人 1 的原话正好印证了这一点。
  修法：**单半学期 + 周次全 ≤ 8 → 按它自己那一半为基准**；
  跨两半的长学期课、全局编号（≥9 周）**行为完全不变**（不把原本正确的排课改坏）。
- **当事人 2 的根因（待一条数据）**：教务行的 `xxq` 读不出时，`Session.fromZdbk`
  按**这次查询用的季节猜**；而 App 用 `1|秋` 与 `1|冬` **各查一次**，
  于是同一门课被分别猜成"秋"和"冬"，再被 `course.dart` 按**或**合并 → **两半都上**。
  只要 `xxq` 读不出，任何"按查询季节猜"的方案都会摆错位置。
  - ⚠️ `zdbk.dart` 里 `xqm=$semester` 把 `1|秋` 这种**带竖线的内部标记原样发给了教务**
    —— **上游源码是同一行**，不是我们引入的。但如果教务只认 `1`/`2`，
    那"四次季节查询"实际只等于两次，`|秋`/`|冬` 从来没起过筛选作用（只能用于打标签），
    兜底猜测也就必然在"秋/冬"之间摇摆。
  - **需要的那条数据**：这门课在教务响应里的**原始 `xxq` 值**。
    已加**只读诊断**（`zdbk.dart` 的 `_parseSessions`，`operation: halfDiag`）：
    每次刷新记一行「请求季节 + 两半都上的课程 + xxq 读不出的条数与原文」，
    当事人用「设置 → 数据 → 复制反馈信息」发出来即可定位。
- **⚠️ 为什么不能凭猜改**：`xxq` 缺失时"这门课到底算哪一半"**数据里真的没有**；
  改错方向的直接后果是**整张课表被滤空**（`session.dart` 的注释记录了当初就是
  因为这个才加的兜底）。必须拿到真实证据再动。
- **顺带确认过的两件事（都不是这个 bug 的原因）**：
  - 研究生课不是"永远两半都上"：`grs_new.dart` 按学期码分流，只有 `pkxq` 为 15/16（长学期）才两半都上；
  - 手机重启后闹钟会自动重排：清单里注册了
    `ScheduledNotificationBootReceiver` + `BOOT_COMPLETED`。

### 24. 课程实体框架：让课程"像待办一样"但**不进待办列表** ★ 框架设计

- **需求**（用户口述）：课程之后要能 **加评论 / 被专注 / 挂附件**；
  其中"自由专注若正处于上课时段，应自动计入该课程"；
  但**不想让它显示在待办列表里**。
- **✅ 用户已拍板（2026-09-14）**：
  1. **评论与附件挂在「课程总体」**（不做当次级）；但**某个待办也可以挂到课程上** ——
     这样"课程级"与"当次级"两种分级都能用（待办本身就是当次级载体）；
  2. **专注归属按「开始时间」判定**属于哪节课（不是按重叠比例），并且**做成开关**；
  3. 实现路径选 **C（折中）**：资料/评论用独立轻量结构，"提醒"与"专注归属"复用现有管线。
- **为什么值得先设计再动手**：现在的待办提醒管线（通知 / 全屏闹钟 / 延迟 / 划掉 /
  专注归属）全都绑在 `Task` 上。直接复用 `Task` 最省事，但会带来两个问题：
  ① 待办列表被课程刷屏；② `TaskType` 是 Hive **枚举按序号**存的，插值会读错老数据。
- **建议的三层模型**（待评审）：
  1. **课程（Session）**：跨周的稳定实体，`id` 就是课程代码 → 挂**长期资料**
     （课件、笔记、评论），问"这门课我记过什么"；
  2. **每一次课（Period，某天某节）**：一次性 → 挂**当次相关**
     （这次要带的东西、这次课的专注时长）；
  3. **提醒与专注归属**：由上面两层**派生**出来的调度项，不参与待办列表渲染。
- **实现路径（三条，代价不同）**：
  - **A. 复用 `Task` + 新 `TaskType.course`（追加在枚举末尾，绝不插队）**
    ＋ 一个 `showInTaskList` 开关：立刻白拿提醒/专注/附件全部能力，
    代价是待办列表、筛选、日历、AI、同步等处都要认这个开关（漏一处就漏出来）。
  - **B. 独立结构（`CourseNote` / `CourseItem`）**：干净，但提醒、附件、专注归属
    都要**重新实现一遍**（含闹钟可靠性那一整套踩过的坑）。
  - **C. 折中（我倾向）**：课程资料/评论用**独立轻量结构**（存储简单、不会被待办逻辑影响），
    而"提醒"和"专注"这两件**需要复用管线**的事，走派生出来的调度项；
    专注侧只需给 `FocusSession` **追加一个课程 id 字段**（Hive 只追加、不改序号）。
- **专注归属的口径要定**（没有唯一正确答案，需用户拍板）：
  自由专注的时段与某节课**完全包含**才算该课？还是**只要重叠达到某比例**
  （例如 ≥ 50%）？还是**只要有任何重叠**就记一次？
  建议：**重叠 ≥ 一半**才算，并且**允许多条专注归属同一节课**（累加时长）。
- **前置依赖**：先接好「课前提醒」（见 #25），因为它会决定课程是否已经有"调度项"这层抽象。

#### 实施进度（2026-09-15 深夜开工）

⚠️ **先纠正一件事**：用户以为"脚手架已经搭好了"，实际**一行代码都没写** ——
本条目此前只是设计。所以这是从零开始。

**已完成（数据层，521 测试全绿）**：

- `lib/model/course_mount.dart`：`CourseMount`（键 = 课程代码，含附件 + 评论）。
  **不新增 typeId、不写 adapter** —— 直接存 Map，与 `CourseIdMap` 同一招（零 schema 风险）。
  读取时**逐项容错**：某项读不动就丢那一项，绝不让课程详情页崩。
- `lib/mod/course_mount_store.dart`：`courseMount()` / `saveCourseMount()`（空挂载自动删记录）
  + 纯函数 `tasksForCourse()`（跳过已删除/已作废，**保留已完成**，是否折叠交给界面）。
- `Task.courseId`（`@HiveField(26)`，只追加）：课程挂载的"挂待办"走它，
  关系只存一处、不冗余。**加字段同时改了四处**：字段、构造函数、`copy`/`copyWith`、
  `database/adapters/deadline_adapter.dart`（少一处就会出现"编辑一次链接就没了"）。
- 测试 `test/course_mount_test.dart`（6 条）：序列化往返、空挂载、脏数据容错、筛选口径。

**下一步（界面层）**：

1. 课程详情页（`page/scholar/course_detail/course_detail_view.dart`）加三个区块：
   **资料**（附件，复用待办那套"选文件 → 复制进应用目录 → TaskAttachment"）、
   **评论**、**关联待办**（列出 + "给这门课新建一条待办"）。
2. 附件要能预览/删除/改文件名（沿用待办侧已有的交互）。
3. 专注归属（第三条决定）：给 `FocusSession` 追加课程 id + "自由专注落在上课时段则计入该课"
   的开关（按**开始时间**判定）。

### 25. 课前提醒（算法已完成，待接投递） ★ 算法已在 `lib/model/class_reminder_rule.dart`

- **现状**：**功能还不存在** —— 课程同步到系统日历时**根本没设提醒**
  （`calendar_to_system.dart` 的 `_createEventFromPeriod` 只设了标题/时间/地点/忙碌状态）。
- **算法已完成并有 20 条测试**（用户口述的四条规则）：
  - 前面没课 → 提前 **20** 分钟；前面有课（连着上）→ 提前 **10** 分钟；
  - 午饭/晚饭**视作一节空课**；"前面有没有课"按**空余时间是否大于课间时长**判断。
  - **课间时长默认 15**（配置项）：本校实际只出现 5（小课间）/ 25（大课间）/ 60（饭点）
    三种间隔，所以**阈值取 6~25 之间任何值结果都一样**，15 落在区间正中，很稳。
  - 饭点默认 `12:25–13:25` 与 `17:50–18:50`（取自校历第 5→6、第 11→12 节之间的空档）。
- **待定的两件事**：
  1. **投递方式**：① 写进**系统日历事件**的提醒（不耗电、App 被杀也能响、只对开了同步的人生效）
     vs ② **App 自己发通知**（不开同步也能收到，但依赖 App 存活）。
     —— 用户倾向"**当成待办处理**"（复用现有通知/闹钟管线，它已经验证过能响）。
  2. **课间时长是否做成设置项**：跨学校/改排课时会需要，代价很小。
- **交付前必须回答**：这些"上课"条目**要不要出现在待办列表里**？
  用户明确说**不要**（见 #24 的框架设计）→ 所以要么走 #24 的派生调度层，
  要么在待办列表里过滤掉 `TaskType.course`。

### 26. 后台被清掉后提醒不响 —— 能做的上限在哪

- **用户的疑问**（合理）："为什么被杀后台就不会响呢？明明钉钉之类的应用都可以。"
- **查证结论**（写下来免得以后重复解释）：
  | 情形 | 会不会响 | 原因 |
  |---|---|---|
  | 进程被内存回收 | ✅ 会 | 用 `AlarmManager` 精确定时，系统到点拉起 App 投递 |
  | 手机重启 | ✅ 会 | 清单里注册了 `ScheduledNotificationBootReceiver` + `BOOT_COMPLETED` |
  | **被强行停止／被厂商"清理后台"** | ❌ 不会 | Android 硬规则：**force-stop 会取消该应用全部闹钟**并标记为已停止，直到用户手动打开一次 |
- **钉钉为什么"怎么都活着"**：靠**厂商推送通道**（华为 Push Kit 等，由系统级推送进程接收，
  与 App 是否被杀无关）+ **系统白名单**（自启动 / 后台运行 / 电池不优化）。
  **我们两样都没有**（没有服务器，也没有厂商开发者资质）。
- **我们能做到的三件事**（前两件已实现）：
  1. 引导用户加白名单（设置 → 闹钟可靠性面板已在讲；**App 无权自己开**）；
  2. **每次打开 App 都重排闹钟**（已实现）→ "打开一次 = 修复一次"；
  3. 被杀期间错过的提醒，重开 App 后**只补弹一次、不重复弹**（已修，见 `alarm_dedupe_test.dart`）。
- **要不要做前台常驻服务？**（通知栏常驻一条"Elychron 运行中"）
  - 好处：系统不会轻易杀我们，提醒可靠性明显提升；
  - 代价：**耗电** + 通知栏长期有条东西；
  - **当前判断：先只做白名单引导**，不为闹钟牺牲电池。等收到"确实经常不响"的反馈再评估。

### 27. 桌面小组件（PR #4，已合并）遗留的四个小改进

> 合并时**没改**这些（尊重贡献者的实现），都记在这里。前两条是观感/正确性，后两条是代码整洁。

1. **时间文案会停旧**（最值得做的一条）：小组件上的「今天 10:00 截止」「已逾期」是
   **App 推快照时算好的**，而 `updatePeriodMillis=0` 又没有周期刷新 ——
   所以 App 不开的时候，小组件会一直显示旧文案、逾期色也不会变。
   做法：把**原始时间戳**传下去，让小组件自己算文案与逾期；或者给小组件加周期刷新
   （两者一起做最稳）。
2. **从小组件勾选会连带勾掉所有子待办**（且没有确认框）：桌面上没有可以询问的界面，
   这是刻意的取舍，但**要写进教程/更新日志**，否则用户会以为子待办丢了。
3. `MainActivity` 里 `CoroutineScope(Dispatchers.Main).launch` 每次调用新建一个作用域
   → 改用 `lifecycleScope` 更干净。
4. 「小组件点 + 新建」用了 `Future.delayed(260ms)` 等底部标签切换完成 ——
   能工作但脆，将来可以改成等切换真正结束。

### 28. ★ 小组件「点了没反应」已定位：需要后台白名单（2026-09-15 深夜）

**根因**：App 被系统"电池优化/后台限制"管着时，**Glance 的会话任务（WorkManager）在后台
根本不会被调度** → 数据更新了、小组件画面永不重画。App 在前台时任务能立刻跑，
所以现象就是"进 App 就正常、在桌面点就没反应"。

**证据**（`adb logcat -s ElychronWidget`，探针本轮已加）：
`onAction queued=true ✓` → `update/updateAll ✓` → 但**没有任何 `SessionWorker`** ✗；
`adb shell dumpsys deviceidle whitelist +xyz.nosig.celechron.mod` 之后
→ `Worker result SUCCESS for Work [... tags={ androidx.glance.session.SessionWorker }]` ✓
→ 画面当场正确 ✓。

**这和"闹钟不响"是同一个根因**（见上面第 26 条）—— 我们已经有个「闹钟可靠性」面板
在引导白名单。

剩下要做的：

1. ~~**把这条补进「闹钟可靠性」面板**~~ —— ✅ **已做**（2026-09-16），但**没有点名小组件**：
   小组件现在是尘封状态（`TodoWidgetMessenger.enabled = false`、清单里的 receiver 已注释掉），
   面板里提它，用户会去找一个根本不存在的东西。所以补进去的是一句**普适**的话：
   「这条白名单管的不只是闹钟：后台刷新、后台同步这类"App 不在前台也要干活"的事情同样受它限制」。
   **等小组件哪天解封，再把这句加回去**："桌面小组件的勾选同样需要它，否则点了要等下次打开 App 才刷新"。
2. 更新日志 / 教程里说明同样的事。
3. （可选）让小组件**自己检测**是否在白名单里，不在就提示"点此允许后台运行"。
4. 本轮为缩短重画链路把 `LazyColumn` 换成了普通 `Column` → **失去滚动**
   （超出高度会被裁）。白名单这条稳定后可以再评估要不要加回来
   （`verticalScroll` 需要 API 31+，我们 minSdk 28）。
5. 第 27 条里的其余遗留仍然有效（尤其是"从小组件勾选会连带勾掉子待办"要写进教程）。

### 29. 用户 2026-09-16 自己列的「Elychron优化方向」（★ 原文照抄 + 逐条现状）

> 来源：用户手机里那条**同名待办**（我把原文抄下来，免得散落在聊天里以后没人记得）。
> 每一条后面标上**我们的现状与去向**。⚠️ 里面有几条是**我还没复现/还没查过的**，
> 都如实标成"待查"，**不要当成已定位的问题**。

**原文（照抄，不改口径）：**

> 接受坚果云这种更可靠的方法；课程挂载待办、文件、评论；小组件还是不行，关闭入口；
> 专注模式还是有问题，后台不会发通知！！；每节课只要专注过就自动生成复习（设置中可选）；
> 课表时间段问题还是很严重！！！；翻页速度快一点；全版本点击成绩卡片任意位置会卡死；
> 应该提供一个长按登录可以查看哪些网页链接成功的功能；暂停状态下应当可以切换到其他页面；
> 子待办数量上限增加

**逐条对照：**

| # | 原文 | 现状 / 去向 |
|---|---|---|
| 29.1 | 接受坚果云这种更可靠的方法 | **已采纳**：跨网络同步走 WebDAV/坚果云这条（见 #3）。目前只有预留配置项，功能未实现 |
| 29.2 | 课程挂载待办、文件、评论 | ✅ **1.4.1 已做**（数据层 + 界面层 + 专注归属，见 `WHATS_NEW_1.4.1.md` 11.12） |
| 29.3 | 小组件还是不行，关闭入口 | ✅ **已按此执行**：小组件尘封、入口关闭（见 11.9 与 #28） |
| 29.4 | 专注模式后台不会发通知 | ★ **真问题**，与 #20「专注的休息提醒在后台/锁屏时不会响」同源。**用户点名 → 优先级上调** |
| 29.5 | 每节课只要专注过就自动生成复习（设置中可选） | 新需求：按课程/节次自动生成"复习"待办。**待设计**（要不要去重、算不算已完成、挂在哪天） |
| 29.6 | 课表时间段问题还是很严重 | **#23 同源**（海宁校区自定义节次 / 半学期归属）。**仍在等课表数据**：需要当事人给出"几点上第几节"的完整对照表 |
| 29.7 | 翻页速度快一点 | 1.4.1 只把**翻页阈值**调高了（防误翻），"更快"是另一件事（**动画时长**）。待做 |
| 29.8 | 全版本点击成绩卡片任意位置会卡死 | ✅ **1.4.1 已修**（2026-09-16）：真机复现出 `RangeError` + GetX 的 Obx 中毒 → 主线程 100% 空转 → ANR。详见 `WHATS_NEW_1.4.1.md` 第十三节。**剩下没验证的**：有成绩时点"没排进课表"的课的卡片（代码已堵，缺真数据） |
| 29.9 | 长按登录可以查看哪些网页链接成功 | 新功能（诊断向）：长按登录态 → 列出各子站/接口的连通结果。与现有「诊断与测试」有重叠，**待评估是否合并** |
| 29.10 | 暂停状态下应当可以切换到其他页面 | ✅ **1.4.1 已做**（2026-09-16）：暂停时按返回**不结束**这次专注，专注首页出现「有一次专注还没结束」→「继续」原样接回（见 13.2） |
| 29.11 | 子待办数量上限增加 | ✅ **1.4.1 已做**：上限在 `AiTaskDraft.maxSubtaskCount`，**5 → 7**（只影响 AI 拆解；手动加子待办本来就没上限）。见 13.3 |

