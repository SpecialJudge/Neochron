# Elychron 1.4.1 相对 1.4.0 的变化（完整清单）

> **这份文档的用途**：给"我们自己"看的完整变更清单，免得发布前漏东西。
> 对外文案（Release 说明、CC98 帖子）在 `RELEASE_NOTES.md`、`CC98_POST.md` 里，
> 这里**只求全、求准**，不求好读。
>
> **生成依据**：`git log v1.4.0-elychron.1..HEAD` —— 21 条提交、54 个文件、**+3625 / −693 行**，
> 截至 commit `3f5fb4f`。测试 **395 条全绿**（1.4.0 时为 355 条左右）。
> 分界标签：`v1.4.0-elychron.1` → `dd539e6`。

---

## 一、新增功能

### 1. iCal（.ics）文件导入

- **入口**：设置 → 数据 → 导入 iCal 文件
- **做什么**：把别人的 .ics（课程表、会议邀请、系统日历导出）变成 Elychron 待办。
  **只进待办，不写系统日历**（按用户要求）
- **字段映射**：`SUMMARY`→标题、`DTSTART`+`DTEND`→**活动**、只有 `DTSTART`→**提醒**、
  只有 `DTEND`→**截止**、都没有→**备忘**；`LOCATION`→地点、`DESCRIPTION`→描述
- **去重**：按 iCal 的 `UID` 生成稳定本地 uid（`ical-<hash>`），**重复导入同一文件不会产生重复待办**
- **导入前确认**：列出识别到的条数与前 5 条标题；导入后报告"新增 N 条 / 跳过 M 条"
- 解析器 `lib/mod/ical_import.dart` 是**纯函数**，16 条单测覆盖：折叠行、`TZID`、
  UTC 的 `Z` 换本地时区、全天 `VALUE=DATE`、转义 `\,` `\;` `\n`、空文件/非 iCal 文本
- ⚠️ **踩过的坑（已修，勿改回去）**：按扩展名过滤（`allowedExtensions: ['ics']`）会被
  Android 映射成 MIME 过滤（`text/calendar`），而很多 .ics **没有登记 MIME**
  （浏览器直下、聊天转发、adb 推的）→ 文件在选择器里**根本看不见**。
  现在改成"选任意文件 + 读出来校验内容"

### 2. 分享内容可以添加到「已有待办」

- 以前分享进来只能新建待办；现在先问一句去向：
  **新建待办** / **添加到已有待办**
- 选已有待办时：按「进行中优先、截止时间近的靠前」列出（最多 15 条，每行显示类型与时间）
- 追加语义：附件往后加、描述换行追加（不覆盖），并**更新 `updatedAt`**
  —— 否则以后多端合并会把它判成旧数据

### 3. 专注时自动免打扰

- 开关：设置 → 专注时自动免打扰（**默认开**）
- 打开专注页即切系统「完全静音」，离开时**还原成原来的档位**（不是固定还原成"全部允许"）
- **没授权时**点开关直接跳系统授权页，副标题写明状态
- ⚠️ **关键设计**：免打扰是系统级状态，App 被系统杀掉时结束回调不会执行。
  所以开启前的档位会**落盘**，下次启动发现"改过却没还原"就立刻还原
  （见 `DoNotDisturb.restoreIfStale`）—— 不做这个，用户手机会被**永久静音**
- 真机验证过：专注中 `filter=4`（静音）、退出后 `filter=1`、被杀后重开自动还原

### 4. 退出登录后仍预填账号密码

- 用户的原始诉求："主动退出登录之后，依然能够预填充登录账号密码"
- 做法：登录成功时**另存一份** `mod_last_username` / `mod_last_password`，
  **退出登录不删**（上游 `removeScholar()` 删的是 `username`/`password` 两个键，我们不动它）
- 登录页打开时读回来预填；**启动时回填**一次（老用户也能用，已登录用户的凭据补记一份）
- 存的是系统密钥库（Keystore / Keychain），不是明文数据库

### 5. 登录态失效如实显示 + 一键重登

- 用户反馈："软件保持了登录状态，但实际上已经连不上了"
- 根因：`isLogan` 一旦为 true 就不会因为会话过期回落 → 界面一直显示「已登录」，什么都没提示
- 现在：刷新时检测**认证/会话类**错误（未登录 / ticket / CAS / 401 / 用户名或密码…）→
  置 `Scholar.sessionInvalid` → 设置页那一行变成
  **「登录已失效」** + 副标题「连不上学校服务器，请点右侧重新登录」+ 右侧「重新登录」，
  点整行直接打开登录页
- 判据做了**双向测试**：认证类要认出来，而"教务网超时"这种**单模块抽风不能误判**成登录失效

### 6. 一键「复制反馈信息」

- 设置 → 数据 → 复制反馈信息：**机型 / 系统版本 / App 版本 + 最近 120 行脱敏日志 + 反馈模板**
  一次进剪贴板，直接粘到 QQ 群或论坛帖
- 日志里的密码、Cookie、学号自动隐藏

### 7. 更新检查加 Gitee 兜底

- 原来只读 GitHub，国内常常连不上 → 现在按 **GitHub → Gitee** 顺序尝试，谁先回应用谁
- **「去下载」打开实际回应的那个源**，不会再"提示有新版本却打不开下载页"

---

## 二、修复

### 1. 闹钟关掉后一直重复弹（重要）

- **现象**（用户反馈）："后台被清掉了闹钟就不会触发，但是一打开软件，闹钟弹窗会一直触发，
  关了还会一直重复弹闹钟"
- **根因**：去重记录写在 `TaskAlarmCoordinator` 里，且写成
  `if (TaskAlarmCenter.current.value == null) _fired.clear();`
  —— 用户一点掉弹窗（`current` 变 null），下一秒 tick 就**清空记录**，
  而那条待办的提醒时刻仍在"同一分钟内" → 再弹，循环不止
- **修法**：去重改为**按「提醒时刻」**（`uid@毫秒`），并且**不再因为弹窗被关闭而清空**；
  改时间或"延迟提醒"到新时刻算新的一次；前台 tick 与通知路径共用同一套去重
- 顺带修：**排定失败被静默吞掉且永不重试** —— 原来 `syncAll` 的 `finally` 里也记签名，
  一次失败就再也不重试（症状正是"闹钟不响且查不出原因"）。现在失败不记签名（下次重试），
  并把排定结果写进诊断日志
- 8 条回归测试（含"关掉弹窗后不会再弹"这条直接对应反馈的用例）

### 2. 「接下来」的倒计时停在旧值

- 卡片上的「还有 N 分钟」原来只在页面碰巧重建时才重算，实测出现状态栏 13:16
  而卡片还写着「还有 24 分钟」（13:01 的旧值）
- 现在每 20 秒自动重算，且只在看「接下来」时刷新

### 3. 课程/考试信息弹层重做

- 原来用 iOS 风格 ActionSheet，里面有一条显示日期/地点但**内容是空白的选项**
- 换成钉钉风格弹层（大标题 + 图标信息行 + 粉色「知道了」）

### 4. 登录判据应只看统一身份认证

- 原来是「6 个子站全为 null 才算登录成功」→ **任何一个子站抽风都让人登不进来**
  （实测：教务网故障导致首次登录直接失败，而身份认证其实已通过）
- 现在身份通过即算登录成功，子站失败降级为「部分模块暂不可用」

### 5. 刷新很卡、要退出重进才能刷好（用户反馈）

- **排查过程**（逐项排除，结论写在代码注释里）：
  抓取已经是 `Future.wait` **并行**；单请求超时 8 秒；`maxRetries = 1`；
  刷新锁是文件锁 + 心跳 + 30 秒过期恢复 —— **架构都没问题**
- **真凶**：「异步刷新」**默认关闭** → 界面要等所有模块全刷完（最坏近 20 秒）才一次性更新，
  看着就像卡死；用户等不及就退出重进（重进读缓存所以"立刻就好"）
- **修法**：改为**默认开启** + 对老用户做**一次性迁移**（标记键，幂等）；
  设置里的开关保留，用户自己关掉后不会再被打开
- 迁移时踩到的坑：只改数据库不够，**界面读的是数据库打开时创建的 Rx** →
  不同步的话设置页一直显示"关着"，用户以为没生效（已补 Rx 同步）

### 6. 界面报错"一长串非常吓人"（用户点名）

- 用户原话："统一认证账号登录时如果输错了会产生很长很长的报错表"
- 根因：界面直接贴 `异常.toString()`，而 `ExceptionWithMessage` 会拼成
  `消息 + 分隔符 + 详情`（详情里是 URL、状态码、响应片段），一屏放不下
- **修法**：新增 `lib/mod/friendly_error.dart` —— **界面只给人话，细节去诊断日志**：
  砍掉详情段、只取第一行、去异常类名前缀、按关键词映射常见故障、兜底截断到 42 字
- 已接入：登录失败、导出/导入失败、复制反馈失败、AI 整理失败、选图失败

---

## 三、体验一致性

### 1. 弹窗全面统一成「钉钉风格」（零残留）

- 新增共用组件 `lib/design/dingtalk_sheet.dart`：圆角顶 + 大标题 + 说明 +
  选项行（可带颜色与对勾）+ 底部整宽按钮（粉色主操作 / 灰色取消）
- 共替换 **10 处** `CupertinoActionSheet`：
  默认提醒提前量、专注时长、闹钟可靠性、导出课程表（两个弹层）、
  重复单位、设置优先级（新建页/详情页）、同步日历选项、选择学期、外观模式、应用错误面板
- 结果：**全仓库代码里 `CupertinoActionSheet` 残留 = 0**（已用扫描验证）
- 顺手改进：优先级保留各自颜色语义；外观模式补上"当前选中项"的对勾；
  闹钟可靠性面板的**主按钮是动态选出来的**（哪一项没做好就把它设成粉色主按钮）

### 2. 主题色统一（为将来的"换主题色"留好接口）

- 新增 `lib/design/app_accent.dart` 作为**唯一来源**，替换掉散在 8 个文件里的 16 处硬编码粉色
- 暴露 `primary / primaryLight / primaryDeep / primaryText / onPrimary / soft()` +
  `configure(color)` / `resetToDefault()`
- ⚠️ 校验时注意：全仓库搜 `0xFFFF699A` 现在仍会命中 **2 处，都是对的** ——
  一处是 `app_accent.dart` 顶部注释（记录这次改造的历史），一处是
  `static const Color defaultPrimary = Color(0xFFFF699A);` 本身。**别把它当成漏改**
- ⚠️ **故意不用 `const`**：const 常量在"运行时换色"时每个 const 上下文都要改；
  现在用 getter，将来加设置项只需接 UI + 持久化，**调用点一行都不用改**
- 5 条测试锁住这个 API

### 3. 文案精简（用户手动修改）

- 去掉大量「」引号、措辞更正式（"丢掉"→"丢弃"）、AI 提示词调整
- ⚠️ 注意：这改动让 2 条**契约测试**失败（`ai_kind_test.dart` 断言警告文案含
  `已按「截止」处理`）—— 这正是契约测试的意义（保证"替用户改过什么都不许静默"）。
  断言已同步

---

## 四、工程 / 测试 / 文档

### 新增测试文件（5 个，均为纯逻辑单测）

| 文件 | 锁住什么 |
|---|---|
| `test/alarm_dedupe_test.dart` | 闹钟去重（含"关掉弹窗不再弹"的回归） |
| `test/ical_import_test.dart` | iCal 解析/映射/去重（16 条） |
| `test/friendly_error_test.dart` | 报错压缩规则（9 条） |
| `test/app_accent_test.dart` | 主题色 API 契约 |
| `test/login_criteria_test.dart` | 登录判据 + 登录态失效识别（追加用例） |

### 文档

- `README.md` **重写**：与发布推文对齐（下载与安装 / 为什么有改版 / 更新内容 / 对照表 /
  数据迁移 / 已知问题 / 后续方向 / 构建 / 隐私 / 反馈 / 许可）
- 新增 `docs/CC98_POST.md`（论坛帖子文案）
- `docs/RELEASE_NOTES.md` / `MIGRATION.md` / `FEATURES.md` / `RELEASE.md` 同步更新
- 隐私：测试夹具不再使用与真实学号前缀相同的示例学号（`3260100000` → `3200000000`）

### 代码整洁

- 新增文件都带"为什么这么做"的注释（尤其是踩过的坑，避免以后被"优化"回去）

---

## 五、发布前仍要处理的事（别忘了）

- [x] ⚠️ **APK 已重建、SHA-256 已重算并填回文档**（2026-09-16）：
      `versionCode=9` / `versionName=1.4.1-elychron.1` / 25.3 MB，
      SHA-256 `b835218c842dfa1e6fe9745d0695cacdf964bdf4de0f76a927e9c745cb6914ec`，签名当场验过；构建产物见 10.2
- [x] ⚠️ **版本号已改回 `1.4.1-elychron.1`**（build **9**，见 `pubspec.yaml` 与
      `lib/worker/fuse.dart`）。为什么是 9 不是原来那个 6：发出去的临时调试包是
      build 7/8，**发布版的 versionCode 必须更大**，否则安卓会当成降级直接拒绝安装
      （我们自己的手机和当事人手机上都装着调试包）
- [x] 两行临时的 `print('[halfDiag…]')` 已删（诊断本身保留，见「设置 → 诊断与测试」）
- [x] **2026-09-16 的「App 打不开」事故已修复，数据已抢救**（全过程见第十二节）：
      字段计数写错 → 开机自愈 + 锁/密钥库保险丝 → 兼容读取 → **32 条待办全部恢复**
- [x] **启动路标收进开关**（`_bootProbesEnabled`，发布版默认不打；异常日志照旧原样打）
- [x] 写坏的「自己扫帧修复」死代码已删（教训留成注释，见 12.4）
- [x] 「闹钟可靠性」面板补上「白名单不只管闹钟」这句普适说明（见 12.5）
- [x] `docs/V1.4.1_README.md` 已按「问题引用 → 逐条对应」的结构写完整
      （12 条真实反馈全部对上了）—— **只剩下载链接与 SHA-256 待填**（要等重建发布包）
- [x] iCal 导入测试留下的 **4 条待办**（新生开学典礼 / 交实验报告 / 国庆假期 / 线上组会）——
      **2026-09-16 夜在手机上逐条核对：已经不在**（「待我处理」7 条全部认得出是用户自己的东西，
      「我已处理」里也没有这 4 条）。⚠️ **不排除是这次数据事故里丢的**（抢救只恢复到最后一个好帧），
      也可能是用户自己删的 —— 所以**不用再清理了**，但以后动手机前先确认，
      **别把用户自己的东西当成测试数据删掉**
- [x] **重建的正式包已在真机上过一遍**（2026-09-16 夜）：装上去开机正常、首帧正常，
      「接下来」拿到的是真实数据（待办 + 课表 + 倒计时都对），启动路标按预期**一声不响**
- [ ] Gitee 发行版还没建（需要与 GitHub 同 tag、同 APK）—— **等用户发话再发**
- [ ] 网盘备用下载链接待补，README/Release 说明里的「国内备用下载」段落待填
- [ ] Gitee 用的 `.zip`（给网盘/直链）还没按新包重新打
- [ ] 数据抢救留下的备份先别删（`dbdeadline.hive.damaged-*` / `.unreadable-*`），
      等正常用几天确认没缺东西再清

## 六、本版**没做**的事（避免以后误以为做了）

- **#3 周视图**（把一周的课程 + 日程混排进时间块）：用户明确说**不做**
- **#2 AI 联网**（给 AI 网络工具自动探索网页生成待办，来自 GitHub issue #2）：用户明确说**不做**
- **桌面小组件「最近待办」（PR #4）**：代码在树里，但**入口已经关掉**
  （清单里不再注册接收器，桌面小组件列表里不会出现它）—— 它刷新依赖系统后台调度，
  在电池优化限制下会「点了没反应」，原因见 11.9 与 `docs/BACKLOG-DEPRECATED.md` #28，等有解再解封
- 局域网同步：入口仍是关闭状态（`kLanSyncEnabled = false`）
- WebDAV / Windows 客户端 / 附件同步：见 `docs/BACKLOG-DEPRECATED.md`，均未动

## 七、外部反馈状态

- **GitHub issue #2**（`DongLanQwQ0`）：[Suggestion] AI生成待办能力加强 —— 建议给 AI
  网络工具、能访问网页链接自动探索并补全字段。**未回复**（用户已决定不做）
- **上游 Celechron issue #174**：我们的评论（空课表兜底方案 + 告知本改版）发出后
  **过了一天仍无人回复**；`Celechron/Celechron` 那边目前没有新动静
- **本科课「秋冬半学期混淆」的报错人（2026-09-15 晚）**：
  - 他说：**「我去，debug版好像恢复正常了，没有出现我说的情况了」** —— 那条
    *能源工程伦理与管理* 不再跑到秋学期里了。
  - 需要说清楚的是：那个"debug 版"（`1.4.0-debug`，build 7/8）**就是从 1.4.1 的工作分支
    构建的**，只是版本号临时改成了 debug —— 所以他看到的"恢复"其实是 **1.4.1 的改动**
    起了作用。最可能的那个改动是 `ClassHalfRule`（研究生/自定义重复那条半学期归属的修复）；
    另一种可能是这次刷新终于拿到了新的教务数据（之前 zdbk 一直返回 HTTP **921 限流**，
    课表长期来自缓存/智慧研工），把本地那份错的覆盖掉了。
  - 他附的反馈文本是**空的**（问题描述/复现步骤/期望/实际 全空，只有脱敏日志，
    而且日志还在 `2026-09-15T` 处被截断了）—— 所以除了"现在正常了"这句话，
    这份报告本身没有可分析的信息。
  - 设备：**OPPO PJR110 ｜ Android 16（SDK 36）** —— 和我们手上的 HUAWEI OCE-AN50（Android 12）
    不是同一台；Android 16 对后台/闹钟的限制更严，**以后碰"闹钟不响"这类问题要记得问机型**。
  - 待跟进：让他**明确确认**那一门课现在是不是还在秋学期里出现；如果以后再出现，
    直接看 `操作=halfDiag` 那两行（诊断还在包里）。


---

# 八、2026-09-14 ~ 09-15 补记（在第七节之后又做的）

> 这一节是后面的增量，写法同上：**每条都写清"为什么"**。

## 8.1 待办：删掉「暂停/继续所有待办」，新增批量编辑

- **删除**：⋯ 菜单里的「暂停所有待办 / 继续所有待办」——属于早期的"时间规划"概念，
  那个概念已经不存在（用户点名要求删）。
- **新增「批量编辑」**（同一个 ⋯ 菜单）：进模式后每张卡片左边出现勾选框，
  **点卡片 = 选中/取消**（不是打开详情），底部浮出操作栏：**删除 / 完成 / 未完成**。
  - 批量模式下**屏蔽左右滑动**（否则选着选着手一滑就删掉一条）；
  - **活动与日程不参与"完成"**（与单条右滑规则一致），选中全是活动时会明说；
  - 多条有未完成子待办时**只确认一次**（不是每条弹一次）；
  - 删除复用控制器：会**记墓碑**，多端合并时才知道是"删掉"而不是"新加"。
- 实现放在 `lib/mod/task_batch_edit.dart`（我们自己的文件），上游 `task_view.dart` 只留 4 处
  `// ===== MOD =====` 接缝。8 条单测。

## 8.2 教程模块（框架）

- 新增 `lib/tutorial/`：注册表 + `sealed` 步骤类型（5 种：文字/清单/图片/对比/行动）+
  播放器 + 教程中心 + 三个入口（中心 / 「?」按钮 / 首用自动弹一次）。
- **加一篇教程 = 新建一个文件 + 注册表加一行**，内容只写数据、样式集中在渲染器。
  扩展指南见 `docs/TUTORIAL.md`。
- 行为规则都有测试：中途退出只记进度不算看过、`contentVersion` +1 会重新提示、
  「不再提示」优先于自动弹、教程改短不越界、坏数据不崩。
- 入口挂在设置页（`modTutorialSection`）。真机走过全链路（含"缺图占位"）。

## 8.3 登录：修掉「老是显示登录已失效」的误报

- **现象**：设置页频繁显示「登录已失效」，而用户其实好好的。
- **根因**：判据太宽 —— 把**模块自己的会话**当成了用户登录失效：
  `学在浙大：未登录`、`智慧研工未登录或会话已失效` 是各子站自己的登录，
  设计上就是可容忍降级（代码里本来就有 `isDegradedRefreshText` 标记，当时没用上）；
  再加上关键词里有 `cas` / `401` 这种**短子串**，而错误详情几乎都带 URL（含 `/cas/`）。
- **修法**：只认两件事 —— ① **统一身份认证（CAS）这一路失败**；
  ② 措辞明确的凭据/会话失效。并**排除 `isDegradedRefreshText` 标记的降级**。
  关键词表里明确注释"别再加回 `cas`/`401`/`未登录`"。
- 6 条**误报回归测试**（用真实消息原文）。

## 8.4 课前提醒：算法 + 课间阈值

- **功能此前不存在**：课程同步到系统日历的事件**根本没设提醒**。
- 算法在 `lib/model/class_reminder_rule.dart`（纯函数，20 条测试）：
  - 前一节没课 → 提前 **30** 分钟（最初 20，用户后来改为 30，见 8.7）；
  - 前一节有课（连着上）→ 提前 **10** 分钟；
  - 午饭/晚饭**视作一节空课**；"前面有没有课"按**空余是否大于课间时长**判断。
- **课间阈值默认 15 分钟**（可配置）：本校真实节次表的间隔只有
  **5（小课间）/ 25（大课间）/ 60（饭点）** 三种，所以阈值取 6~25 之间任何值结果都一样。
- 饭点默认 `12:25–13:25`、`17:50–18:50`（第 5→6、第 11→12 节之间的空档）。
- **投递方式仍未接**（待定：系统日历提醒 vs App 通知；用户倾向"当成待办处理"）。

## 8.5 课表：修掉「只有冬学期的课显示到了秋学期」（研究生那条）

- **两个当事人两条完全不同的路**：
  | | 当事人 1 | 当事人 2 |
  |---|---|---|
  | 课程 | 物理光学实验（**研究生课**）| 能源工程伦理与管理（**本科课**）|
  | 路径 | 研究生院 → `customRepeat` 按周次展开 | 教务/智慧研工 → 半学期标记 |
- **研究生这条已修**：`_buildPeriods` 里按周上课的课原本**只看周次数字**
  （`(week-1)~/8`，第 1-8 周一律算上半学期），而研究生院给的 `zc` 是
  **该课程所在半学期内部**的周次（只有冬学期的课也给 `1-8周`）→ 被排进秋天。
  修法：`lib/mod/class_half_rule.dart` —— **单半学期 + 周次全 ≤ 8 → 按它自己那一半为基准**；
  跨两半的长学期课、全局编号（≥9 周）**行为不变**。9 条测试。
- **本科那条待数据**：教务行的 `xxq`（学期类型）读不出时按"查询季节"猜，
  而 `1|秋`/`1|冬` 各查一次 → 同一门课被猜成两半 → 合并时按"或" → 两半都上。
  **智慧研工那条路**对缺 `xxq` 同样处理成"两半都上"。
  → 已加**只读诊断**（`halfDiag` / `halfDiagEta`），等当事人反馈原文。

## 8.6 排查用的临时构建（**发布前必须还原**）

- `pubspec.yaml` 与 `lib/worker/fuse.dart` 里的版本号被临时改成 **`1.4.0-debug`（build 7/8）**，
  一共产了 **7 个 `Elychron-v1.4.0-debug*-arm64.apk`**（都仍保留半学期诊断、都用**正式签名密钥**
  签的，所以能直接覆盖安装保留数据）：debug 给当事人收集数据；debug2 多覆盖智慧研工路径；
  debug3~debug7 是这一晚真机边验边改的迭代包（③④ / 免打扰两修 / 第九节四条 / 9.6 四条 /
  卡片堆叠第三版）。
  - **当前手机里装的是 `debug7`**：
    SHA-256 `66742004adce8c8e9e9ca1f4af83f43aaafaaf9978aadefe1919105b323daf5c`，25.3 MB。
  - 这些包**只是排查与验证用的**，正式发布前都要丢掉（见下面那条 ⚠️）。
- ⚠️ **发布前必须做**：版本号改回 `1.4.1-elychron.1`（build 6 或重新 +1），重新构建正式包。
- 临时加过的东西：`zdbk.dart` / `eta.dart` 里各有一行 `print('[halfDiag…]')`（便于 adb 验证）、
  诊断上报。诊断本身**可以留着**（只读、有用），但 `print` 建议删掉。

## 8.7 用户新提的四项（2026-09-15 已完成）

1. **文件选择分类**：附件选择处先问一句来源 ——
   **「从图库选择图片」**（`FileType.image`，系统给相册，能看缩略图）
   / **「从文件管理选择」**（不限类型，交给手机自带文件管理）。
   刻意**不加扩展名过滤**（会被映射成 MIME 过滤，出现"文件明明在却看不见"，iCal 导入踩过这个坑）。
2. **动画卡顿（闹钟页等）**：闹钟页的毛玻璃用的是 `sigma 24 / 40` 的**实时**高斯模糊，
   而原来**每秒刷新的时钟文字在模糊层内部** → 每秒重算一次大面积模糊，必然掉帧。
   修法：把「静态毛玻璃层」与「会变的内容」拆成**兄弟节点**，并给模糊层包
   `RepaintBoundary`（只光栅化一次）；装饰光斑同理。**观感不变**。
3. **课前提醒提前量**：前面没课时 **20 → 30 分钟**（连着上课仍是 10 分钟）。
### 8.7 补充：④ 的证据链

**右滑完成的真 bug**：`confirmDismiss` 正是在**手指抬起那一刻**被调用的，而原代码在里面 `await confirmCompleteTask(...)`（有未完成子待办时才弹确认框）→ 弹框刚出现就被同一个抬手事件误触关闭 → 返回 false → 什么都没发生。这正好解释了"**没有子待办的能滑、有子待办的滑不动**"。修法：不在该回调里 await，**先让卡片弹回、下一帧再走完成流程**（复用 `_toggleDone`）。

## 8.8 又一批四项（2026-09-15 第二轮）

1. **专注切休息时不关免打扰 → 已修** ✅
   - 现象（用户反馈）："切休息模式时不会自动关掉免打扰，导致无通知，我也不知道我要休息了"。
   - 原因：免打扰只在**进出专注页**时开关（进=静音、走=还原），中间的**段切换没管它**。
   - 修法：段落切换处（`focus_page.dart` 的 `_lastPhase` 比较）按当前阶段同步 ——
     工作段静音、休息段还原；`dispose` 里再兜一次（幂等）。
2. **专注期间被外部分享打断 → 已修** ✅
   - 现象："如果在专注期间通过外部分享进入 Elychron，会强行打断并提示未正常退出"。
   - 原因：分享流程会切到待办页并弹新建面板，直接压在专注页上；会话因此停在"未完成"，
     下次进专注页就提示"上次没有正常结束"。
   - 修法：`home_mod_hooks._handleShared` 里**先判断是否有未结算的专注会话**；
     是则把分享内容**攒起来**（每 3 秒看一次是否结束），专注结束后再自动弹出来处理 —— **全程不打断**。
3. **「接下来」多个进行中的项目只显示一个 → 已修** ✅
   - 现象（用户反馈）："同时有两个正在进行的项目⋯⋯只会显示一个进行中"。
   - 原因：界面只认 `items.first` 当顶层大卡；第二条进行中的条目掉进「之后还有」，
     而那一行**还故意不显示「进行中」**（`_row` 里的旧条件）→ 用户根本看不出它在进行。
   - 修法：把「顶层大卡 / 折叠的其它进行中 / 之后还有」三段划分做成**纯逻辑**
     （`model/upcoming.dart` 的 `layoutUpcoming`，13 条单测）。顶层默认**课程优先**
     （`defaultTopRunningIndex`：有 `PeriodType.classes` 就给它，否则按开始时间最早）；
     其余进行中的条目在顶层卡片下方**折叠成一叠小卡**（逐级内缩 + 「同时还有 N 个进行中 ·
     点一下置顶」），点任意一张把它换到顶层，原来那张落回折叠堆（点错了能点回来）。
     - 置顶按 `dedupeKey` 记**不按下标**：这一页每 20 秒跟着心跳重算，下标随时会变。
     - 置顶**只在那条还进行中时有效**：课程/日程的 uid 整套复用（同一门课每天同一个 uid），
       按"还在不在列表里"判断会让置顶一直留到下次开课（好几天后莫名冒到顶层）。
     - 进行中的条目**不再同时出现在「之后还有」**，避免同一条显示两遍。
   - ✅ 已真机验证（见 8.10）：折叠堆的样子、计数、点一下置顶、扛得住心跳。

4. **日程页上滑折叠日历（只留一周）→ 已做** ✅（农历那部分**没做**，见下）
   - 做法：折叠的呈现**直接用 `TableCalendar` 自带的 `CalendarFormat.month ⇄ .week`**，
     不手画一周条 —— 它自己就是 `AnimatedSize` 包着的（`formatAnimationDuration` 默认
     200ms），高度变化天然平滑；且选中态、今天、小圆点标记在周视图下全都照旧。
     手画一条「一周条」要把选中/今天/标记/横向翻周全部重写一遍，风险大得多。
   - 手势：`NotificationListener<ScrollNotification>`（`calendar_view.dart` 的日历分支）→
     `CalendarController.handleDayListScroll` → 判定在 `mod/calendar_fold.dart`
     （`CalendarFoldGesture`，13 条单测）。上滑列表 → 折成一周；**回到顶部继续下拉** → 展开整月。
   - 三个必须踩住的坑（都写进注释和单测了）：
     1. **只认竖向通知**：`TableCalendar` 内部是横向翻页的 `PageView`，它的通知同样会冒泡
        出来，而那时 `metrics.pixels` 是"第几页"的偏移 —— 拿它判断会让日历莫名折叠。
     2. **只认"手指直接带着动"的通知**（`dragDetails != null`）：折叠后列表因为日历变矮而
        **变高**、内容可能不再溢出，位置会自动弹回 0；那种自动回弹若被当成"用户下拉"，
        就会立刻展开 → **一折一展死循环**。
     3. **当天列表改成"永远可拖"**（`AlwaysScrollableScrollPhysics.applyTo(当前平台物理)`）：
        当天只有一两条时列表本来滚不动，手势就没有载体。用 `applyTo` 接在平台物理外面
        （本 App 是 CupertinoApp → `BouncingScrollPhysics`），只多出"永远可拖"，回弹手感不变。
   - **折叠状态不粘住**：每次进「日历」这一面都恢复整月（`onInit` 里 `ever(viewMode, ...)`）。
     折叠是一次性的浏览动作，翻去「接下来」再翻回来只剩一行星期的话，很容易以为月视图坏了
     —— 而展开的手势（回到列表顶部继续下拉）不是一眼能看出来的。
   - ❌ **图二里的农历没做**：仓库里没有农历换算（`TimeHelper.chineseDayRelation` 只是
     "今天/明天"这种相对说法，不是农历）。要加得引一个农历库或自己塞一张换算表，
     属于独立小功能，等用户明确要了再做。**其余（星期 + 日期 + 小圆点标记）与图二一致。**
   - ✅ 已真机验证（见 8.10）：上滑折成一周、顶部下拉展开，两向都正常、不抖。
     **手感类的（阈值 28px 是否偏灵敏）仍建议用户自己上手再定。**

## 8.9 本轮工程记录（2026-09-15 第三轮）

- 新增单测 **35 条**：
  - `test/upcoming_test.dart` +9（多个进行中的三段划分、点选置顶、置顶失效）
  - `test/calendar_fold_test.dart` 新建 **20**（折叠 / 展开 / 两种必须滤掉的通知 / 边界 /
    **通知→判定参数的映射**（拿真的 `ScrollNotification` 对象测，含"自动回弹不会把日历又展开"的端到端一条））
  - `test/upcoming_view_test.dart` 新建 **6**（widget 测试：折叠堆画出来的样子、
    写明数量、**点一下真的能换到顶层**、点错了能点回来、**360pt 手机宽度下不溢出**）
- 全套测试 **497 条全绿**；`flutter analyze` **0 error / 26 warning（与基线一致）/ 50 info**。
- 改动文件：`lib/model/upcoming.dart`、`lib/page/calendar/upcoming_view.dart`
  （改为 `StatefulWidget`）、`lib/page/calendar/calendar_view.dart`、
  `lib/page/calendar/calendar_controller.dart`、`lib/mod/calendar_fold.dart`（新建）。
- 这两项当时手机还断着，先用 widget 测试把"画出来的样子 + 点击交互"钉住；
  **后来真机回来了，已补验（见 8.10）**。

## 8.10 真机验证（2026-09-15 晚，用户手机 HUAWEI OCE-AN50 / Android 12）

装上含本轮全部改动的包（`Elychron-v1.4.0-debug3`，正式密钥签名、覆盖安装保数据）后实测：

| 项 | 结果 |
|---|---|
| ④ 日程页上滑折叠日历 | ✅ 上滑 → 折成一行（星期一～日 + 日期 + 小圆点标记都在，与图二一致，只差农历）；回到列表顶部下拉 → 展开整月。**两向都正常、没有来回抖**（当初最担心的"折展死循环"没出现）。 |
| ③ 接下来页多个进行中 | ✅ 造了两件同时进行中的事：顶层一张大卡 + 「同时还有 1 个进行中 · 点一下置顶」+ 一张折叠小卡（写明"进行中 · 地点"）。**点折叠卡 → 真的换到顶层**，原来那张落回折叠堆；而且能扛过 20 秒心跳（置顶不会被重算冲掉）。 |
| ③ 课程优先（默认口径） | ✅ 三件同时进行中（两件日程 17:30/18:00 + 一节课 18:50）时，**顶层自动是那节课**
（卡片右上角写着「课程」，另两件折叠在下面）。⚠️ 见下面的"一次异常"。 |
| 专注切休息/暂停时关免打扰 | ✅ 工作中 `mInterruptionFilter=4`（静音）；**暂停 → 1（还原，能收通知）**；继续 → 4；结束 → 1。**顺带抓出两个真 bug 并修掉**，见下。 |
| 右滑完成（有未完成子待办） | ✅ 造了一条带未完成子待办的待办，右滑 → **确认框正常弹出**（「还有子待办没完成 … 确定要一起完成吗？」）→ 点「全部完成」→ 待办进「我已处理」且子待办 1/1。**这条正是用户报的"有子待办的滑不动"，修好了。** |
| 分享进 App | ✅ 分享一段文字 → 弹出「分享到 Elychron」面板（新建待办 / 添加到已有待办），选「新建待办」→ 新建页标题已自动填好。 |
| 左滑删除的二次确认 | ✅ 弹「删除这条待办？「…」删除后无法撤销」，取消/删除都正常。 |

### 8.10.1 真机抓出来的两个免打扰 bug（都已修）

1. **按钮换段不走免打扰同步**：暂停/继续、跳过休息这三个按钮里有
   `_lastPhase = _engine.phase;`（**手动对齐**），于是 `_onTick` 里"段变了"的判断
   永远不成立 —— 免打扰同步只挂在那个判断上，这条路就被跳过了。
   实测：工作中点「暂停」，`mInterruptionFilter` 仍然是 **4**（本该还原成 1，让用户能收到通知）。
   修法：两个按钮里补调 `_syncDoNotDisturbForPhase()`；另外在 `_onTick` 里**再加一道
   与段无关的对齐**（只看「当前该不该静音」，`_lastSilenced != _engine.isWorking` 才动手），
   这样以后任何路径换段都会在 1 秒内被纠正，不会再有人漏调。
2. **`enableForFocus()` 会把还原记录清掉**：它原来是"当前已经是完全静音 → 清掉保存的档位"。
   问题是**我们自己刚设的静音**也满足这个条件 —— 上面那道对齐一旦二次调用它，
   记录就没了，结束时 `restore()` 变成空操作，**用户手机会一直静音**（最不能出的事故）。
   修法：已静音时直接返回，**绝不清记录**（用户自己开的免打扰本来就没有记录可清）。
   → 这一条是"为了兜底而加的第二道同步"逼出来的隐患，真机复验时确认：结束专注后
   `mInterruptionFilter` 回到 **1**，手机没被留在静音。

### 8.10.2 一次没解释通的异常（诚实记下来）

课程 18:50 开始之后的第一次观察（18:51）：三件进行中都在，但**顶层是那两件日程里更早的
"A"，不是课程** —— 与"课程优先"不符，且折叠堆数量（2）和顺序都对得上"顶层=A"。

- 排除了「用户手动置顶」：那个手势只在**折叠堆里的小卡**上，而当时只有一件折叠
  （课程没开始时 running 只有 A、B，顶层是 A，折叠堆里只有 B）→ **不可能置顶到 A**。
- 排除了「代码没生效」：点一下折叠堆里的课程，它立刻到顶层且卡片标着「课程」；
  重启 App（清掉内存态）后，顶层**直接就是课程**、A/B 折叠在下面 —— 默认口径是对的。
- 单元测试（9 条）和 widget 测试（6 条）也都覆盖这个口径并通过。
- **没能复现**，也没找到代码路径能解释那一次。最可能是当时那一份 `items` 正处于
  异步刷新的中间态（课表那一侧被替换的瞬间）。**留在这里待观察**：如果再出现，
  第一件事是看 `buildUpcoming` 出来的 `kind` 是不是 course。

---

# 九、2026-09-15 第四轮：用户看完真机后的四条建议

> 这四条是用户在真机上用过 8.10 那个包之后提的（含一条真 bug）。
> 写法和上面一致：**每条都写清"为什么"**，踩过的坑也留在里面。

## 9.1 横向翻页阈值调高（"老是划错"）

- 原实现：月份翻页走 `TableCalendar` 内部 `PageView` 的 `PageScrollPhysics` ——
  手指一带就翻，还可能一口气翻好几页；库也没暴露这个 physics。
- 现在：把内置横向手势关掉（`availableGestures: AvailableGestures.verticalSwipe`，
  竖向"整月 ⇄ 一周"保留），自己套一层带阈值的 `ModSwipePager`
  （`lib/mod/calendar_paging.dart`）：**划够 90 逻辑像素**（约屏幕宽度的 1/4）才翻；
  快速轻扫（≥40px 且 ≥900px/s）也算。翻页本身用纯函数 `shiftedFocusedDay`
  （月视图整月挪、周视图整周挪，**日期按目标月长度收口**，1 月 31 日往后 → 2 月 28 日）。
- ⚠️ **两个只有真机才会暴露的坑（都踩过了）**：
  1. **必须用 `GestureDetector` 而不是 `Listener`**：首页 5 个标签是外层一个 `PageView`，
     它也在抢横向滑动。`Listener` 只是旁听、不参与手势仲裁，长距离横划会被外层抢走 ——
     实测**真的会误切到「待办」标签**。用横向拖拽识别器把这一片占住，外层就拿不到了。
  2. **`adb input swipe` 的坐标是设备像素，Flutter 指针坐标是逻辑像素**（1080÷3=360）。
     所以 `input swipe ... 220px` 在代码里只有 73 逻辑像素 —— 够不着 90 的阈值，
     看上去就像"手势完全没生效"。我在这个换算上绕了好几圈，最后靠**把计数画在屏幕上**
     （`DIAG down=1 up=1 dx=-73`）才定位到：手势一直是好的，是我测试划得太短。

## 9.2 日历折叠提示（无文字）

- 展开整月时是一条短横「—」（意思是"能往上收"），折成一周时是 V 形「⌄」（"能拉下来"），
  两者用 `AnimatedSwitcher` 换（200ms，与日历自身的折叠动画对齐）。
- **点它也能折叠/展开** —— 只做提示不可点的话，用户多半会去点它却点不动。

## 9.3 「接下来」的折叠改成"一叠卡片"（用户亲手纠正 + 概念图）

- 我第一版做成了"每条并排列出"，用户纠正：折叠的意思是**像一叠卡片一样重合**，
  并在概念图里强调那图只是表达"一叠"这个意思、**别照抄**。
- 现在：顶层大卡下面露出几条**卡边**（同色同阴影的矮圆角条，逐级内缩一点），**不写字**。
- 交互按用户要求分成两处：
  - 点**最上面那张卡面** → 看这条的信息（原有行为）；
  - 点**下面的重叠部分** → 弹出「同时进行中」列表（顶层那条标"当前 ✓"），
    选一条把它放到顶层。

## 9.4 横幅通知的「划掉」点了没反应（真 bug）

- 用户原话：「横幅通知点划掉没有反应，只能点击延迟」。
- 原因（读插件源码确认）：`showsUserInterface: false` 的按钮**不会把 App 拉到前台**，
  响应只会送到**后台 isolate**（插件为它单独起一个引擎执行
  `onDidReceiveBackgroundNotificationResponse`）—— 而后台 isolate 碰不到前台界面，
  于是通知取消了、**全屏闹钟照样响**。「延迟提醒」是 `true`（会把 App 拉起来），
  所以一直能用 —— 这个"只有一条好用"的现象正好印证了机制。
- 修法：
  1. 闹钟模式的「划掉」改成 `showsUserInterface: true`，让它走前台回调
     （`TaskReminder.actionsFor(mode)`，有单测钉住）；通知模式没有全屏闹钟要停，
     保持 `false`（点它不必把 App 弹出来）。
  2. **闹钟页原来根本不监听 `TaskAlarmCenter`** —— 就算清掉中心，页面也不会关、
     铃声一直响（铃声是在 `dispose` 里停的）。现在它监听中心，被别处清掉就自己关掉；
     并加了"只 pop 一次"的守卫（否则 `clear()` + 自己 pop 会把下面那一页也弹掉）。
  3. 顺手注册了后台按钮的入口函数（`@pragma('vm:entry-point')`），
     免得那种按钮点下去静默消失、插件还白起一个引擎。
- ⚠️ 这条**只做了代码级 + 单测级验证**（真机上要等一条真的闹钟到点；我这套 adb 自动化
  造不出"精确 2 分钟后响的提醒"）。一分钟自测法：待办里新建一条**提醒型**待办、
  时间设成 2 分钟后，等横幅弹出 → 点「划掉」应当立刻停响并收起。

## 9.5 验证与工程记录

- 真机实测（HUAWEI OCE-AN50，含上面全部改动）：
  - 横向翻页 ✅ 167 逻辑像素的划动翻月、73 逻辑像素不翻、不再误切标签；
  - 折叠提示 ✅ 「—」⇄「⌄」切换 + 点击折叠/展开；
  - 一叠卡片 ✅ 三条卡边（对应 3 个折叠项）、点边弹出列表、选中置顶生效；
  - 顺带再次确认"课程优先"（顶层自动是那节课）与点日期选中都正常。
- 新增测试 **14 条**：`test/calendar_paging_test.dart` 10 条（翻页纯逻辑 7 +
  阈值/竖向不误触 3）、`test/notification_actions_test.dart` 4 条（按钮声明）；
  `test/upcoming_view_test.dart` 改成新交互（7 条）。
- 全套 **511 条全绿**；`flutter analyze` **0 error / 26 warning（与基线一致）/ 50 info**。

---

# 十、1.4.1 发布准备（2026-09-15 夜）

## 10.1 合并 PR #4：桌面「最近待办」小组件（作者 LisirX）

- **来龙去脉**：仓库里有两个社区 PR（都来自 `LisirX` 的 fork）。我把两个分支都拉下来
  **实跑**了一遍（analyze / test / 真编 APK）：
  - **#4 桌面小组件**：analyze 0 error、352 测试全绿、**release APK 真编得出来** → 接。
  - **#1 全局 Markdown 加粗**：分支本身也是干净的（351 测试全绿），但它的基线是
    `350bcb0`，之后 main 走了 **52 个提交**、它要改的 13 个文件我们都动过 ✗；
    更麻烦的是它在 `upcoming_view.dart` 里改的还是**我们早就删掉的 `CupertinoActionSheet`**
    —— 硬合会把旧代码带回来。**用户决定不做**（收益不大、工作量不小），PR 由用户自己回复。
- **为什么 #4 接得下**：
  1. **不是引入新技术栈**：Glance 依赖与 `MainActivity` 的 `updateAll` 用法 main 里本来就有
     （`ECardWidget.kt` 就是一个 Glance 小组件），这是沿着已有模式扩展；
  2. **架构选对了**：小组件**不碰数据库**。Flutter 侧推一份精简快照到 SharedPreferences，
     勾选动作进队列，由 App 启动/回前台时对账落库（幂等、有测试、对
     `MissingPluginException` 有兜底）—— 跨进程直接读 Hive 是这类需求最常见的坑，它避开了。
- **合并冲突只有一个文件**：`lib/mod/home_mod_hooks.dart`（双方都在 `start()` 末尾追加初始化），
  按"两边都保留"解决：PR 的小组件冷启动检查放在 `start()` 最末。
- 合并后：analyze 0 error / 25 warning、测试 **516 全绿**、release APK 构建通过。

### ⚠️ 10.1.1 一个坑：Kotlin 增量编译缓存坏了会报假错

合并后第一次构建失败，报的是
`Unresolved reference 'saveTodoWidgetSnapshot' / 'pendingTodoWidgetCompletions' / …`
—— 看着像"文件没进编译"，但文件在、也在 git 里、gradle 源集也包含 `src/main/kotlin` ✗。
翻**完整构建日志**（别只看过滤后的几行）才看到真因：

```
e: Daemon compilation failed: null
Caused by: java.lang.AssertionError: java.lang.Exception:
Could not close incremental caches in D:\celechron-mod\Celechron\...
```

**Kotlin 的增量编译缓存坏了会退化成一堆"未解析引用"的假错误。**
解法：删掉 `android/.gradle`（必要时连 `android/app/build`）再编 ✓。
（起因大概是：先编了 PR4 分支，又切回 main 合并，缓存对不上了。）

## 10.2 发布物

- 版本：`pubspec.yaml` = `1.4.1-elychron.1+9`；`lib/worker/fuse.dart` =
  `appVersionName '1.4.1-elychron.1'` + `appBuildNumber 9`（两处必须一起改）
- 正式包：`Elychron-v1.4.1-elychron.1-arm64.apk`
  - `versionCode=9` / `versionName=1.4.1-elychron.1` / 25.3 MB（arm64）
  - **SHA-256** `8fc16ab62aea5a6fad2b74e10c8406ff6e7ac3e58c34df2a6487e85759d37720`
  - 签名证书 SHA-256 `b2cc4256…a771c8`（与 1.4.0 同一个正式密钥 ✓）
  - ⚠️ **上面这一版是"事故之前"构建的，已经作废**（它不含第十二节的修复）
- 正式包（**2026-09-16 重建，当前这一版**）：`Elychron-v1.4.1-elychron.1-arm64.apk`
  - `versionCode=9` / `versionName=1.4.1-elychron.1` / **25.4 MB（26,679,900 字节）**
  - **SHA-256** `d4bdc64d5e6d4ce70730aaab820f809c4d150708ff27b4cced5428bd53460ce3`
  - 上一版（同一天早些时候，只有前十七条修复）：`db2e8a5a…460ce3` 已作废，以这一版为准（2026-09-17 深夜用 `build_apk.ps1` 一键出的包：analyze 0 error、
    583 测试全绿、签名指纹仍是 b2cc4256…a771c8）
  - 历史：`8fc16ab6…`（事故前）→ `b835218c…`（09-16 第二次）→ `a914715d…`（09-16 第三次）
    → `9689f618…`（09-17 教程配图那次）→ **`525c9817…`（2026-09-17 第四次：装上第十五节的四条修复）**
  - ⚠️ v1.4.1 **还没发布**，之后还会重打，发布时以`build_apk.ps1`打印的那一串为准
  - ⚠️ 这是**第三次重建**（2026-09-17）：又装进了第十四节的**教程配图**（两张真机截图）
    与全屏看图，所以 26,679,900 字节 / `a914715d…` 那一版**同样作废**，以这一版为准
  - 签名证书 SHA-256 `b2cc4256…a771c8` —— 用 `apksigner verify --print-certs` 当场核对过 ✓
  - 构建产物落在 `build/app/outputs/flutter-apk/app-release.apk`，发布时改名为上面那个文件名
- 已装到用户手机验证：`dumpsys` 里 versionName/versionCode 正确、
  **小组件接收器已注册**（`TodoWidgetReceiver` + `APPWIDGET_UPDATE`）、启动无崩溃。
- 仍未做：Gitee 发行版、网盘备用链接、`.zip` 重打、`docs/V1.4.1_README.md`。

## 9.6 第四轮微调（用户上手后又提的四条）

1. **折叠提示的判定区加大、并且可以滑动**：
   - 判定区从"72×26 的小块"改成**整条**（左右到底 × 高 34），图形本身仍然很小；
     真机实测：在离中心 1/5 屏幕宽的地方点，照样折叠 ✓（以前点那儿没反应）。
   - 新增**上下滑**：往上滑收起、往下拉展开（与当天列表的方向一致）。
     滑动**不设"每帧位移阈值"** —— `details.delta` 是逻辑像素，慢划一帧只有 1~2px，
     卡阈值就会"划了没反应"；只看方向、靠 `setCalendarFormat` 幂等防重复触发。
2. **V 更扁**：换成自绘的浅 V（宽 22 × 高 5，线宽 2，圆头）。
   之前用的 `CupertinoIcons.chevron_down` 在 14 号下又窄又高，像个尖；折叠提示要的是
   一道"往下拉"的浅角。
3. **点击折叠提示的动画更快**：`formatAnimationDuration` 200ms → **140ms**。
4. **卡片堆叠的观感重做**（用户："都是一条圆角线，根本没有堆叠的感觉"，随后
   干脆说"别按我的设计来了，按你自己的审美改好看点"）。前后做了三版：
   - 第 1 版（用户概念图）：几条留缝、各带一圈向上阴影的圆角条 —— 失败。
     根因：**留缝**破坏了"一件物体"的整体感；每层长得一模一样也读不出深度；
     给每条描一圈边只会更像控件。
   - 第 2 版：不留缝 + 每层整体变暗 + 越深越窄 —— 好一些，但仍像"灰条纹"，
     因为**整层染灰**是"条纹"的语言，不是"纸"的语言。
   - 第 3 版（定稿）：照着纸的物理线索来 ——
     1. **不留缝**：第一层直接贴住顶层卡，让卡自己的投影落在它身上；
     2. 每层**与卡片同色、不描边**，只在**自己的上沿**有一道由深到无的渐变
        （`Color.alphaBlend` 把黑色按比例混进卡面色）—— 那正是"上面那张压下来的
        影子"，相邻两层就自然形成锯齿状的分层；
     3. 越深越窄（≈均匀缩小 5px/层，最多 20px），只有最下面那张补一道落地下阴影；
     4. 层高随层数递减（1 层 13px → 5 层以上 7px），**整叠总高封顶 ~32px**，
        堆六条也不会把当天列表顶下去。

> 这四条都上了真机看、并按看到的再调（纯观感的东西只能这么办）。
> 真机确认：偏离中心点提示能折叠 ✓；V 上往下划能展开 ✓；卡堆三层贴紧、越深越暗 ✓。

---

# 十一、2026-09-15 深夜：用户上手后的第二轮问题

## 11.1 活动类待办无法右滑完成 / 右滑恢复（已修 + 真机双向验证）

- **真因不在回调里，在 `Dismissible` 的手势方向上**：

  ```dart
  direction: batch ? none : (!deadline.isEvent ? horizontal : endToStart)
  ```

  上游对活动型只给了 `endToStart`（只能从右往左 = 删除），
  **右滑手势根本传不到 `confirmDismiss`**。我第一次只改了回调里那句
  `if (!deadline.isEvent)`，所以真机上毫无变化 —— 教训：改交互前先把整段结构读完。
- 一并修的两处连带问题：
  1. 活动型右滑的**完成背景原来是 `null`**（就算能滑也没有任何提示）；
  2. 活动型卡片的状态标签**只看时间轴** → 已完成的活动在「我已处理」里还写着
     「进行中」，看着像右滑没生效（真机实测撞到过）→ 改成完成状态优先。
- 口径上也该放开：详情页（`task_edit_page` 的完成按钮）本来就不分类型。
- **其他三种类型（截止 / 提醒 / 备忘）没有这个问题** —— 用户问过这点。
- 真机验证（用用户列表里的活动型 `test report 3`）：
  右滑 → 待我处理 6→5、我已处理 17→18 ✓；到「我已处理」里再右滑 → **恢复**，
  计数回到 6/17 ✓（顺手把用户数据还原了）。

### 11.1.1 另外两处"排除活动型"（**已一并对齐**）

- `mod/task_batch_edit.dart`：原 `splitCompletable` 会把活动型从批量完成里剔除
  （连"全是活动"时的提示语都在讲这条规则）→ **函数已删**，批量完成直接走
  `resolve` 的结果，四种类型一视同仁；钉住旧规则的那两个单测也随之删除
  （新规则就是"不过滤"，没有可测的分支）。
- `page/task/task_controller.dart` 的 `removeCompletedDeadline`：
  「清除已完成」原来跳过活动型 → 现在一起清。理由：**既然能完成，就得能一起清掉**，
  否则对活动型是条死路。
- 这一改一共影响三个入口：**列表右滑、批量完成、清除已完成** —— 口径现在统一了。

## 11.2 两个子页面底部被系统导航栏遮住（已修）

- 症状：学业-课表、设置-AI智能助手 划到底时，最后一块内容被底部导航栏压掉一半。
- 原因：两个页面的滚动视图都没把**底部内衬**算进内容里。
- 修法（同一套写法）：
  - `mod/ai/ai_settings_page.dart`（ListView）：`padding.bottom = 12 + MediaQuery.padding.bottom`；
  - `page/scholar/course_schedule/course_schedule_view.dart`（CustomScrollView）：
    末尾加一个 `SliverToBoxAdapter`，高度同为 `16 + MediaQuery.padding.bottom`。
- 为什么用 `MediaQuery.padding.bottom`：它在**全屏页面**等于系统导航栏高度，
  在**标签页**里则是「标签栏 + 系统导航栏」（首页 `home_page.dart` 会把标签栏高度注入
  MediaQuery），所以同一个表达式两种场景都对。
- 用户已确认这两个页面现在没问题 ✓。
- ⚠️ 同款毛病的还有 7 个页面（课程列表 / 课程详情 / 考试列表 / 成绩详情 /
  学业首页 / 局域网同步 等）→ **本轮一起修**（见 11.5）。

## 11.3 桌面小组件的两个反馈（用户实测）

- **勾选是生效的** ✓：用户点了小组件里的方框，进 App 后那些待办确实完成了。
  抓到的日志也印证链路是通的（Glance 的 `InvisibleActionTrampolineActivity` 起来过、
  `androidx.glance.session.SessionWorker` 连续 `result SUCCESS`）。
- ✗ 但**小组件自己的画面没跟着变** —— 问题在"刷新链路"，不在勾选动作本身。
- ✗ 而且**时间文案会停在推快照的那一刻**（`updatePeriodMillis=0`，没有周期刷新）。
- → 用户明确要求"周期刷新很需要"，见 11.4。

## 11.4 小组件改造：让文案与状态自己活起来（已完成）

- **快照带上原始时间戳与类型**（`at` + `kind`），小组件**自己算**
  「今天 10:00 截止」「已逾期」这类文案 —— 原来这些是 App 推快照时算好的，
  App 不开就永远停在旧值。Dart 侧保留算好的 `time` 作为**兜底**
  （万一读到旧格式快照，不至于显示空白）。
- **周期刷新**：`updatePeriodMillis` 从 `0` 改成 **30 分钟**（系统允许的最小值）；
  再给接收器补上 `TIME_SET` / `TIMEZONE_CHANGED` / `DATE_CHANGED` ——
  跨零点、改时间、换时区都会立刻重算文案。
- 这两条一起上，"小组件像是死的"那种观感就没了。

## 11.5 其余 7 个页面的底部内衬（已完成）

逐页补上同一套内衬：课程列表 / 课程详情 / 考试列表 / 成绩详情 / 加权 GPA /
学业首页 / 局域网同步。
- 其中 CustomScrollView 的页面统一在 slivers 末尾加一个 `SliverToBoxAdapter` 垫片；
  两个 ListView 的页面（AI 设置、局域网同步）改的是 `padding.bottom`。
- ⚠️ 踩了一个小坑：`exam_list_view.dart` 的最后一个 sliver 原本**没有尾逗号**，
  往它后面插新元素必须先补逗号，否则报 `Expected to find ','`（已修）。

## 11.6 ⚠️ 用户反馈的「更新后登录态诡异」—— 待查（老毛病）

用户原话（**不是本轮引入**）：

> 基本上每次推送更新的时候登录态会变成一种诡异的样子，显示"已登录"但是没有学号，
> 同时学业页面刷新不出来。退出登录后再次重新登陆时不会保存上次的账号密码
> （我们之前特地写过这个功能），重新登陆之后一切恢复正常。

已知线索：
- 这与 11.7 的"学业页整页空白"是同一个现场：刷新项报 **未登录** → 学业页缺数据 → 崩。
- 「已登录但没有学号」很像**凭据还在、会话已经不能用**：App 认为自己登录着
  （所以不提示重新登录），而各子站已经取不到数据。
- "退出重登后一切正常"说明重走一遍密码登录就能自愈 → 那为什么不自动重登？
  要么**存的密码取不到了**（更新后密钥库/加密存储的问题），要么**重登流程没被触发**。
- 用户还提到**重登时不预填上次的账号密码**（那功能是我们专门做的）→ 说明
  「记住账号」这条链在"退出 → 重登"路径上失效 —— 这两件事很可能是同一个根因。
- 下一步：① 先给 ErrorWidget 补堆栈日志（现在只打一行消息，查不到崩在哪）；
  ② 「已登录但取不到数据」时给一个明确的重新登录入口，而不是让用户自己摸索；
  ③ 排查「记住的账号密码」在退出后为什么取不到。

## 11.7 学业页整页空白 + 每 20 秒一次 Flutter 崩溃（已定位并修复，见 11.8）

- 日志里每 20 秒一条 `Elychron ErrorWidget: Null check operator used on a null value`
  —— 刷新心跳每 20 秒推一次状态，而**数据缺失时某个 widget 的 build 会空值崩**
  （上游代码的脆弱点：缺数据应该给一句"部分数据暂不可用"，而不是崩）。
- 直接触发条件：用户那次刷新的某一项失败（弹窗写「刷新遇到问题：0 项降级，1 项失败 /
  失败：未登录」），而「查看详情」**只说"未登录"、不说是哪个模块** —— 这句可读性也该改。

## 11.8 11.7 的崩溃定位到了（已修）：`Scholar.isGrs` 里的空断言

- 抓到日志后堆栈很清楚：

  ```
  #0  Scholar.isGrs (package:celechron/model/scholar.dart:55)
  #1  ScholarPage.build.<anonymous closure> (package:celechron/page/scholar/scholar_view.dart:1103)
  ```

- `lib/model/scholar.dart:55` 原文是：

  ```dart
  bool get isGrs => !username!.startsWith('3');
  ```

  **`username!`** —— 而用户描述的"老毛病"状态正是 `username == null`
  （**"显示已登录但没有学号"**）。于是学业页 build 直接抛
  `Null check operator used on a null value`：整页空白，
  又因为刷新心跳每 20 秒重建一次 → **每 20 秒崩一次**。
- **所以 11.6 的"老毛病"和 11.7 的崩溃是同一个现场**：
  那个半坏的登录态直接触发了这个空断言。
- 修法：学号为空时按最常见的**本科**口径返回 false。这样页面会正常走到它本来就有的
  「未登录 / 下拉刷新以获取数据」空状态，而不是整页崩掉。
- 顺带说明：这只治"崩"，不治"为什么会出现半坏登录态" —— 那件事见 11.6 的下一步。

## 11.9 ★ 小组件"点了没反应"的真凶：后台被电池优化限制（已定位 + 已解决）

这一条值得单独记，因为**根因不在小组件，也不在桌面**。

### 现象
- 桌面点方框：进 App 看，待办**确实完成了**；但**小组件画面纹丝不动**（连重绘的迹象都没有）。
- 而在 App 里操作 → 小组件的画面**会**跟着变。

### 定位过程（探针逐层切开）
1. `onAction 收到勾选` ✓、`queued=true` ✓、`update/updateAll 已请求` ✓ —— **动作与数据全对**；
2. 画面仍不变 ✗ —— 于是排除"勾选没生效""快照没更新"这两层；
3. 抓系统日志发现：点击后**只有 `AppWidgetServiceImpl.resetAppWidgetProviderInfo`**（系统收到请求 ✓），
   而 **Glance 的 `androidx.glance.session.SessionWorker` 一次都没跑** ✗ ——
   那是个 **WorkManager 任务**；
4. **决定性实验**：`adb shell dumpsys deviceidle whitelist +xyz.nosig.celechron.mod`
   把它加进电池优化白名单 → 再点 → 被卡住的任务**立刻执行**：
   `I/WM-WorkerWrapper: Worker result SUCCESS for Work [ ... tags={ androidx.glance.session.SessionWorker } ]`
   → 小组件画面**当场变成正确的条数** ✓✓✓

### 结论
**小组件的重画依赖 Glance 的会话任务（WorkManager），而 App 被系统"电池优化/后台限制"
管着时，这个任务在后台根本不会被调度** → 数据更新了、画面永远不重画。
App 在前台时任务能立刻跑，所以现象就是"**进 App 就正常、在桌面点就没反应**"。

**这和"闹钟不响"是同一个根因**（我们那个「闹钟可靠性」面板讲的就是这件事）。
→ 待办：把这条也写进那个面板 + 更新日志里说明（见 BACKLOG）。

### 本轮同时做的两件事
- **`LazyColumn` → 普通 `Column`**（`TodoWidget.kt`）：Glance 的 LazyColumn 是适配器型集合
  （日志里的 `lazyCollection=2`、`ListAdapterCallbackTrampoline`），换掉它让小组件变成
  一份普通 RemoteViews，重画链路更短更可预测。代价：**失去滚动**，超出高度会被裁
  （列表最多 6 条，小组件本身可拉伸）。
- **定向 `update(glanceId)` + `updateAll` 两条都发**，并把两处 SharedPreferences 写入
  从 `apply()` 改成 `commit()`（排除"写还没落就重画"）。
- **探针保留**：`adb logcat -s ElychronWidget` 能看到"收到勾选 / queued / 推快照的条数与 id"，
  下次同类问题一步就能定性，不用再从零猜。

### ★ 后续决定：**尘封这个小组件**（2026-09-15 深夜，用户拍板）

定位清楚之后用户的要求是「**实在修不好就尘封**」，而结论是：

- 数据链路完全正确 ✓（探针已证明）；
- 但"画面刷新"依赖 WorkManager 任务能被调度 ✗，而这要求**每个用户手动去系统里
  允许后台运行**（本机实测加白名单后确实好了 ✓）—— 对大多数用户就是"点了没反应"，
  体验不可控，代价太大。

所以：

- `AndroidManifest.xml` 里那段 `<receiver>` 已**注释掉**（验证：装到手机上后
  `dumpsys package | grep -c TodoWidgetReceiver` = 0 ✓，桌面上的实例随之消失 ✓）；
- Dart 侧加了 `TodoWidgetMessenger.enabled = false` 开关，`update/pendingCompletionIds/
  acknowledgeCompletions` 全部直接返回，不再做无用功；
- **代码与资源全部保留在树里**，复活步骤写在 manifest 的注释和 `BACKLOG-DEPRECATED.md` #28 里；
- 两个 PR 的回复里也向作者如实说明了这件事（PR #4）。

## 11.11 ★ 「更新后半坏登录态」的成因与修复（已修，含成因）

用户原话（反馈过很多次的老毛病）：

> 基本上每次推送更新的时候登录态会变成一种诡异的样子，显示"已登录"但是没有学号，
> 同时学业页面刷新不出来。退出登录后再次重新登陆时不会保存上次的账号密码，
> 重新登陆之后一切恢复正常。

### 成因：两套存储的存活期不一样

- **`isLogan`（以及课程、成绩等）跟着 `Scholar` 存进 Hive 数据库** —— 覆盖安装后还在 ✓；
- **`username` / `password` 存在系统密钥库**（`FlutterSecureStorage`，见
  `database_helper.dart` 的 `getScholar/setScholar/removeScholar`）——
  某些 ROM / 机型在覆盖安装后**读回 null（且不报错）** ✗。

于是 `Scholar` 从数据库恢复时无条件 `isLogan = true`（`scholar.dart` 恢复路径的最后一行），
App 就"认为自己登录着"：不做重新登录、但每次刷新都必然失败 →
用户看到的就是那个"诡异的样子"。**学业页还因此每 20 秒崩一次**（`Scholar.isGrs` 里的
`username!`，见 11.8）——**11.6 与 11.7/11.8 是同一个现场**。

### 修复

在 `main.dart` 注入 scholar 之前做一次纠正（**注入之前**，全 App 都会看到纠正后的状态）：

```dart
final credentialsMissing = username.isEmpty || password.isEmpty;
if (restoredScholar.isLogan && credentialsMissing) {
  restoredScholar.isLogan = false;      // 别再去跑注定失败的自动刷新
  restoredScholar.sessionInvalid = true; // 设置页会给出「重新登录」入口（已有该 UI）
}
```

并打一行 `debugPrint`，把"凭据缺了哪一项"记进日志，方便下次直接确认是不是同一回事。

### 还差一步（需要用户拍板）

「记住的账号密码」（`mod_last_*`）**也存在同一个密钥库里** —— 所以密钥库读不出来时，
它同样读不出来，这就是"退出重登时不会预填"的直接原因。
要让它真正挺过更新，只能**换一个不受密钥库影响的存储**（App 私有目录 / 数据库），
而那意味着**密码以可解形式落在应用私有存储里**（虽然只有本机、只有 App 自己能读，
但终究不如密钥库）。这是个**安全与便利的取舍**，等用户决定：
- 要"永远能预填" → 把 `mod_last_*` 一并镜像到数据库（并在文档/设置里讲清楚）；
- 只要"更安全的失败" → 保持现状，但登录页在预填失败时明确提示"上次的账号没能读出来，
  请手动输入"。

### ★ 用户拍板：改成"永远能预填"（2026-09-15 深夜，已实现）

用户选择前者：**另存一份到数据库，永远能预填**。实现：

- 新增独立盒子 `dbAccount`（`DatabaseHelper.accountBox`），键 `lastUsername` / `lastPassword`；
- `rememberAccount` **两处都写**：系统密钥库（首选，加密）+ 数据库（回退副本）；
- `rememberedAccount` **先读密钥库，读不到再退回副本** —— 正常情况下用的仍是加密存储，
  只有在密钥库"失忆"时才用到副本；
- `forgetAccount` 两处一起清（退出登录**不**调用它，仍然保留预填）。

**取舍写在代码注释和这里，别以后当惊喜发现**：数据库那份是**可解形式**，
存在应用私有目录里，只有本机能读，但终究不如密钥库。安全与便利之间用户选了后者。

## 11.12 课程挂载（资料 / 评论 / 待办）—— 数据层已完成

用户要求「课程允许挂载图片、评论以及待办」，并以为脚手架已搭好。
**实际情况：此前只有设计（`BACKLOG-DEPRECATED.md` #24），一行代码都没有**，本轮从零开始。

按用户当初拍板的三条决定实施（评论/资料挂"课程总体"、走折中路径、待办也可挂上来）：

- `lib/model/course_mount.dart`：`CourseMount`（键 = 课程代码，附件 + 评论）。
  **不新增 typeId、不写 adapter**，直接存 Map（与 `CourseIdMap` 同一招）→ 零 schema 风险；
  读取**逐项容错**：某一项坏了就丢那一项，绝不让课程详情页崩。
- `lib/mod/course_mount_store.dart`：读写 + 空挂载自动删记录 +
  纯函数 `tasksForCourse()`（跳过已删除/已作废，保留已完成）。
- `Task.courseId`（`@HiveField(26)`，只追加）："挂待办"走字段而不是冗余存两份关系。
  ⚠️ 加字段同时改了**四处**：字段、构造函数、`copy`/`copyWith`、
  `database/adapters/deadline_adapter.dart` —— 少一处就会"编辑一次链接就没了"。
- 测试 6 条（`test/course_mount_test.dart`）：序列化往返、空挂载、脏数据容错、筛选口径。

### 界面层（同一轮做完，真机看过）

- **课程详情页**（`page/scholar/course_detail/`）新增三个区块，各自是独立小组件
  （`course_mount_sections.dart`），所以那个页面仍是 StatelessWidget、改动面最小：
  - **资料**：附件列表（图片/doc 图标 + 大小 + 删除）＋「添加图片 / 文件」，
    复用待办那套 `pickAttachments`（先问图库还是文件管理、复制进应用目录）与
    `openAttachment`（系统分享面板打开，比 file:// 可靠）；
  - **评论**：输入框 + 发送，最近的在上面，可逐条删除；
  - **相关待办**：`tasksForCourse()` 查出来的列表（完成项打勾划线）＋
    「新建待办并挂到这门课」。
- **新建待办页**新增可选的**「课程」**字段：课表里有哪些课就列哪些（同一门课跨学期去重），
  默认「不挂课程」；没课表时不显示这一行（不给空选择器）。课程挂上后进入那门课的
  「相关待办」。
- **真机验证**：三个区块渲染正常 ✓；从课程页点「新建待办并挂到这门课」
  → 新建页的课程字段**预填为「百万立方未来世界」** ✓✓。
- ⚠️ **评论的"打字"没能在真机上验**：这台机器的输入法有问题（连 `adb shell input text`
  都进不去），所以只做到"代码 + 单测 + 界面渲染"这一层。用户自己敲一条就知道了。

### ★ AI 生成待办带上课程（已完成，按用户要求"只在明确提到课程时才传"）

关键设计：**让模型回课程名，而不是课程代码** —— 课程代码是教务内部的东西，
模型不知道，硬要它回只会得到编造的字符串。所以：

- 提示词里给一份**当前课表（课程名清单）**，要求"**只能从这里原样挑一个**"；
- 规则写死：**默认留空**，只有原文明确出现课程名（「高数作业」「计概的实验报告」）
  才填；**不要因为"看着像作业"就随便挑一门课**（挂错课比不挂更糟）；
- 拿不到课表时，提示词直接告诉模型"courseName 一律留空"；
- 我们这边用纯函数 `resolveCourseId()` 把名字落到代码上：
  归一化后完全相等 → 命中；一边包含另一边（模型爱写简称）→ 命中；
  都命中不了 → **null（不挂课程）**，并在 `warnings` 里如实说"课表里没有「X」"，
  而不是静默丢弃（静默会让用户以为 AI 漏了，其实是我们故意不硬套）。
- 匹配用的课程清单与新建待办页的选择器**是同一份**（`courseChoices()`），
  两边不会出现"选择器里有、AI 匹配不到"的不一致。

### ★ 统计页新增「本月按课程」（已完成）

- `FocusStats.byCourse()`：按 `FocusSession.courseId` 聚合（同一门课的多次专注累加），
  同样按**开始时间**归档到月份。
- 口径写在卡片副标题里：**只算挂到课程上的专注，合计 XX；自由专注不计入这里**
  —— 免得用户拿它去和总时长对不上。
- 课表里已经没有的课程代码 → 显示「已不在课表里的课程」（上学期归的课会落这里），
  不显示空白、也不编名字。
- 测试 4 条（并入 `focus_stats_test.dart`）：累加与排序、自由专注与上个月不计入、
  兜底名、一条归属都没有时返回空列表（界面据此不渲染）。

### ★ 专注归属（课程挂载的第三件，已完成）

用户 2026-09-14 拍板的两条口径都落实了：**按「开始时间」判定** + **做成开关**。

- `FocusSession` 追加 `@HiveField(11) String? courseId`（只追加、不插队），
  同时改了 `copy` 相关的序列化与 `database/adapters/focus_adapter.dart`，
  并进了 `toJson/fromJson`（导出/同步/备份带着它走）。
- 规则是纯函数 `courseIdForFocusStart()`（`mod/course_mount_store.dart`），优先级：
  1. **待办自带课程归属 → 直接继承**（用户建待办时明确选过，比按时间猜准）；
  2. 否则 **开始时间**落在哪一节的 `[startTime, endTime)` 里就算那门课
     —— 半开区间保证连续两节课的接缝只命中后一节，一次专注永远只记一门课；
  3. 只认 `PeriodType.classes`；考试 / 日程 / 虚拟占位都不算；
  4. 命中不了就是自由专注（null），**不硬塞给某门课**。
- 开关：`kFocusAttributeToCourseKey`（默认**开**），设置页在「专注」那组里
  新增一行**「专注自动计入课程」**（`settings_mod_section.dart` 的 `_FocusCourseTile`）。
- 归属失败（没登录 / 课表还没抓到）**绝不影响专注本身** —— 整个判定包在 try 里，
  失败就退回"只继承待办的课程归属"。
- 测试 13 条（`test/focus_attribution_test.dart`）：接缝（下课那一刻开始算不算）、
  课间空档、跨天不误命中、冲堂时只取一门、考试/日程混入、待办优先、无课表数据、
  没有课程代码的课时。**全套 534 条全绿。**
- 为什么不用"重叠比例"：一次专注会被拆成两半记到两门课上，统计页就没法看了
  （用户明确选了这个口径）。


## 11.10 新用户反馈：密码框弹不出键盘（已做缓解，需回访确认）

- 反馈原文：「想问问大家现在登录的时候有没有也遇到输学号正常但输密码键盘会跳不出来」
  「小米17pm，现在我尝试粘贴密码上去，但显示连不上教务网」。
  → 「连不上教务网」是另一件事（我们已修过，先不管）；
  → 关键线索是**粘贴能贴进去**：说明输入框本身能聚焦、能收文本，
  卡住的是"向系统要输入法"这一步。
- 判断：这类国产 ROM 对**密码类输入框**会挂自己的"安全输入法"，
  那个键盘起不来时，系统就什么都不弹。
- 本轮改动（`lib/page/option/login_page.dart`）：
  ① 密码框的输入类型显式声明为 `TextInputType.visiblePassword`
  （不再让引擎按 `obscureText` 推成 `textPassword`）；
  ② 关掉 `autocorrect` / `enableSuggestions`；
  ③ 加一个「显示密码」眼睛开关 —— 真弹不出来时切成明文一般就能正常输入，
  也能让用户自己确认密码有没有打错。
- 实现细节：`LoginForm` 由 `StatelessWidget` 改成 `StatefulWidget` 来承载这个开关。
  它只在 `option_view.dart` 里内联构造（两处），外部不碰它的控制器，所以改造是安全的；
  顺手把两个 controller 的 `dispose` 补上了。
- ⚠️ **我们没有小米设备，这条只能等反馈者回访确认**。
  如果眼睛开关管用，说明判断成立；如果明文也弹不出来，那就要换方向查（例如 ROM 的
  输入法白名单 / 悬浮窗权限）。

---

# 十二、2026-09-16：一次真实数据事故的完整记录（「App 打不开」→ 数据抢救）

> 这一节必须写，而且必须写清"为什么"：这个 bug 是**我们自己**引进的，
> 现场表现又是最吓人的那种（App 完全打不开、数据看起来全没了）。
> 以后任何人碰 Hive adapter，都应该先读这一节。

## 12.1 事故是怎么发生的（我的锅，写明白）

- **起因**：给待办加「课程归属」时，`Task` 加了 `@HiveField(26)`，
  adapter 的 `read`/`write` 也都加了 —— 但**头部那个"字段计数"字节忘了同步**
  （声明 **23** 个字段、实际写了 **24** 个）。`FocusSession` 同样犯了（11 → 12）。
- **后果（关键）**：Hive 写入时**不校验**计数，所以每次保存待办都会安静地写下一个
  "多一对字节"的坏帧 —— 一切正常，直到**读取**：
  列表读取器把多出来的那个 `26` 当成了**下一个待办的 typeId**
  → `HiveError: Cannot read, unknown typeId: 26` → `openBox` 抛错
  → `main()` 在 `runApp` 之前就死了。
- **用户看到的**：进程活着、日志里没有任何异常、first frame 永远不来；
  系统侧记为 `AppBootFail` + 一条 ANR。也就是「Elychron 打不开了」。
- ★ **教训（一句话）**：Hive 的字段计数是**手写**的。加字段必须同时改三处 ——
  **① 模型的注解 ② adapter 的 `read`/`write` ③ 头部那个 `writeByte(n)`**。
  而且还必须是**追加**（新字段 id 排在最后），一旦重排或插队就是同一类事故。

## 12.2 修复：先让 App 能打开，再谈数据

1. **把计数改对**：`deadline_adapter.dart` 23→24、`focus_adapter.dart` 11→12。
2. **开机自我修复** `openBoxResilientImpl`：开机先记一笔"正在开 X"、开好了再记一笔
   "X 开好了"；下次启动若发现"记了没完成"，说明上次就是死在 X 上
   → **把它整份留档（`.damaged-<时间戳>`）再挪走**。**数据一条不删。**
   - 为什么不用"先试开、失败再修"：**Hive 会缓存"打开失败"的 future** ——
     第二次 `openBox` 拿到的是同一个失败结果，这条路根本不成立（我第一次修复就这么白忙的）。
3. **锁超时兜底**：`openBox` 拿不到锁时会**无限等待**（另一个 isolate 握着，或上次被杀留下死锁）。
   现在最多等 8 秒，超时就删掉 `.lock` 重试一次。
   理由：启动这一刻我们这一侧没有别的写手，**"让用户彻底打不开"的代价远大于极小概率的并发写**。
4. **密钥库保险丝** `secureStorageOrNull`：`init()` 里原本有一个**没有 try、没有超时**的
   `await secureStorage.readAll(...)`，平台侧不返回，`main()` 就永远走不到 `runApp`。
   现在最多等 3 秒，超时或抛错都当"读不到"。密钥库再坏也不能让 App 打不开。

## 12.3 数据抢救（★ 32 条待办全部恢复）

- **兼容读取**（真正的关键）：`DeadlineAdapter.read` 读完正常字段后，如果缓冲区里还有剩，
  就**偷看下一个字节**；是 26 就把多出来的那一对吃回去。
  → 于是"坏帧"变成了"能读的帧"，数据本身从来就没丢过。
- **抢救流程** `salvageTasksFromBackupOnce`（在 `main()` 里 `runApp` **之后不 await** 地跑）：
  找出最新的 `.hive.damaged-*` → 复制成临时盒子 `dbsalvage`
  → **让 Hive 自己 `openBox` 读出 `deadlineList`**（CRC、类型注册、列表还原都交给它）
  → 写回主盒子 → `deleteBoxFromDisk` 删掉临时盒子。
- **三条安全前提**：① 只做一次（`optionsBox` 里的标记）；
  ② **当前待办列表是空的才动手** —— 有数据就一条都不碰（而且**不设标记**，下次还有机会）；
  ③ 只读备份，**绝不修改、绝不删除**。
- **结果**：**32 条全部回来**（待我处理 8 / 我已处理 23），
  完成时间、提醒、地点、子待办、状态都在，手机上逐条看过。
- ★ **踩坑记：一次性标记位咬了我三次**（① 盒子非空误判 ② 只扫尾部 200 帧 ③ 上一版用掉了标记）
  → 标记键从此**带版本号** `salvagedTaskBox_v4_20260916`，抢救逻辑每改一次就 +1。
  以后凡是"只跑一次"的自愈/迁移逻辑，**一律照这个规矩来**。
- 另一条坑：一直找不到 `dbDeadline.hive` —— **Hive 落盘时会把盒子名转成全小写**，
  文件其实叫 `dbdeadline.hive`。第一版自救因此永远"文件不存在"，静默什么都不做。

## 12.4 诊断手段（留着，但收进了开关）

- **启动路标** `[boot] 1…8` / `[boot] 4.1…4.5`：发布版**默认不打**，
  由两个文件各自的 `_bootProbesEnabled` 控制（排查时改成 `true` 重新构建，这种包不要发）。
- **异常日志照旧原样打**（自愈、删锁、留档、抢救结果）—— 它们只在真出事时出现，
  是现场最有用的东西，**不受开关控制**。
- 取日志的正确姿势（这台设备的坑）：**前缀一律 ASCII**（设备 shell 的 grep 不匹配中文），
  而且 logcat 转得飞快，必须**开机几秒内**抓：
  `adb logcat -d --pid=$(pidof xyz.nosig.celechron.mod)`。
- **代码整洁**：写"自己按帧扫描修复"的两版死代码（含那个会把整个盒子读进内存的
  `readAsBytes` 版本）已删除，教训以注释形式留在 `database_helper.dart` 里 ——
  因为它俩都是**看起来对、实际会把启动拖死或根本扫不出东西**的写法。

## 12.5 还没做的

- 数据抢救留下的备份**先别删**（`dbdeadline.hive.damaged-*` / `.unreadable-*`），
  等正常用几天确认没缺东西再清。
- 发布包要**重新构建**并重算 `RELEASE_NOTES.md` 里的 SHA-256（事故前那版包已经不算数了）。
- ⚠️ 顺手补的一条：「**闹钟可靠性**」面板里加了一句普适说明 ——
  "这条白名单管的不只是闹钟，后台刷新/后台同步同样受它限制"。
  桌面小组件那个"点了没反应"的根因就是它（见 11.9），但小组件现在是尘封状态，
  面板里**不点名小组件**（否则用户会去找一个不存在的东西），等解封再加回去。

## 12.6 真机复验（2026-09-16 夜，最终那版正式包）

- 把**重建的正式包**（`versionCode=9`，SHA-256 `b835218c…6914ec`）直接用 `adb install -r` 装到
  HUAWEI OCE-AN50 上（同一个正式密钥 → 覆盖安装、数据保留），然后冷启动：
  - ✅ 能打开、首帧正常（没有再出现 AppBootFail / ANR）；
  - ✅ 「接下来」显示真实数据：「看习概群」截止 23:59、倒计时「还有 52 分钟」算得对，
    课表事件（游泳/数学分析/程序设计…）也都在 → **抢救回来的数据在新包里读得出来**；
  - ✅ **启动路标一声不响**：`_bootProbesEnabled = false` 生效，
    `logcat --pid` 里只剩 Impeller 那一行，没有 `[boot] 1…8`；
  - ✅ 没有异常日志（异常路径那几条日志本来就只在真出事时打）。
- ✅ 顺手在真机上**点开「闹钟可靠性」面板确认**新加的那句确实渲染出来了
  （在语义树里查到该节点的真实 bounds，不是"只写进代码没显示"）
- 这一步的意义：**发布前至少要有一次"用最终二进制 + 真数据"的验证**，
  而不是只信 `flutter test` 和「本地能编过」。

---

# 十三、2026-09-16（第二轮）：用户报的「点成绩卡片就卡死」+ 两条小改进

## 13.1 ★ 「点击成绩卡片任意位置会卡死」—— 已修，成因完整记在这里

**用户原话**：「全版本点击成绩卡片任意位置会卡死」。

**真机复现**（HUAWEI OCE-AN50 / Android 12，正式包，一摸一样）：

1. 点「学业」页的**成绩卡片** → 立刻一条
   `RangeError (length): Invalid value: Valid value range is empty: 0`；
2. 之后**点什么都没反应**，约半分钟后系统弹
   「**"Elychron"无响应。是否将其关闭？**」；
3. `am_anr`：`Input dispatching timed out … Waited 5000ms for MotionEvent`；
4. dropbox 里的 ANR 报告：**主线程 state=R、utm=23.5 秒、进程 CPU 116%**，
   而且栈全在 `libapp.so`（Dart AOT）里 —— **不是死锁，是主线程被烧住了**。

### 直接原因：在空列表上取下标

成绩页构建时读的是 `semestersWithGrades[semesterIndex.value]`，
而这个列表 = 「**有成绩的学期**」。新生一门成绩都没有 → 列表是空的 → 必然越界。
（这也解释了为什么"全版本都这样"：这段代码来自上游，一直如此，只是**没有成绩的人**才会碰到。）

### 放大器（关键，和"具体哪一行写错"无关）

GetX 4.7.3 `get_rx/.../rx_interface.dart` 的 `notifyChildren`：

    RxInterface.proxy = observer;      // 先设全局代理
    final result = builder();          // ← 这里抛错
    ...
    RxInterface.proxy = oldObserver;   // 夭折：永远不会执行

于是 **`RxInterface.proxy` 被永久留在一个构建失败的 Obx 上** →
之后全 App 任何一次 Rx 读取都挂到这个"死观察者"上 →
反复触发它 setState → 它每次重建都再抛一次 → **主线程 100% 空转** → 5 秒后 ANR。
（`lib/page/scholar/scholar_view.dart` 顶部早就记过同类事故：「build 出错 → 错误组件又出错 → …」
把主 isolate 烧死；这次是它的另一个入口。）

### 修法（三层，缺一不可）

1. **不让它抛**（治本）：
   - `GradeDetailController` 新增 `safeIndex` / `hasGrades`，所有下标都走它；
   - `getYearStats` / `getYearMajorGpa` / `getPairedSemesterIndex` 全部挡越界，越界返回 0；
   - 学期名 `substring(2,5)` / `substring(7,11)` 改成按长度安全取（脏数据不再崩）。
2. **正常的空状态**：一门成绩都没有时显示「还没有成绩」卡片
   —— 这是**正常状态，不是错误**，不该弹错误也不该崩。
3. ★ **兜底解毒剂**（`main.dart`）：给 `FlutterError.onError` 加一句 `RxInterface.proxy = null`。
   只要有**任何** builder 抛错，就地断掉上面那条放大链路。
   GetX 我们改不了，但它的 `proxy` 是**公开静态字段**。
   这一条和"哪一行写错了"无关，是防止**下一次**别的地方写错又把 App 烧死。

### 同一类坑的另一半（顺手一起修了）

`CourseDetailPage` 的构造函数原来是：

    course = _scholar.value.semesters
        .firstWhere((e) => e.courses.containsKey(courseId))   // ← 没有 orElse
        .courses[courseId]!;

只要那个 courseId 不在课表里就抛 `StateError: No element`，而且是在
`Navigator.push` 的路由构建里抛的 —— 后果和上面完全一样（页面打不开 + Obx 中毒 + 卡死）。
**哪些课会找不到？**最典型的就是**成绩卡片**点进去：军训、体育、通识课这些
**没排进课表**的课照样有成绩。现在改成找不到就显示「课表里没有这门课」。
（顺带：`createSessionCard` 原来在 build 里对 `course.sessions` **就地排序**，
等于构建时改模型数据，已改成先复制。）

## 13.2 专注：暂停时可以放心离开，回来接着做（用户要求）

- 用户原话：「专注模式暂停状态下应当可以切换到其他页面，方便修改待办之类的」。
- 以前：从专注页返回 = **结束这次专注**（弹「结束这次专注？」确认），
  想中途去改一条待办只能先把专注结束掉。
- 现在：
  - **暂停状态下直接按返回就离开**，这次专注**不结算**；
  - 还在跑（没暂停）时按返回，弹窗从「继续 / 结束」变成 **「暂停并离开 / 继续专注 / 结束」**；
  - 「专注」首页出现一张 **「有一次专注还没结束」** 卡片：显示「叫什么 · 已专注多久 · 多久前暂停」，
    两个按钮：**继续** / **结束并结算**；
  - 专停页暂停时也会明说一句「现在可以直接返回，这次专注不会结束」，否则用户不敢按。
- 为什么暂停着离开是安全的：**暂停本来就不计时**（引擎只在 working/resting 累加），
  所以离开多久都不影响时长；会话记录本来就每 10 秒落库一次。
  「这一段还剩多久 / 暂停前是工作还是休息」是引擎内部状态、会话里存不下，
  单独存一份（`lib/mod/focus_suspend.dart`，存在 optionsBox 里）。
- ⚠️ **刻意不动 Hive 表结构**：给 `FocusSession` 加字段就得同步改 adapter 的字段计数字节，
  2026-09-16 那次「App 打不开 + 待办全没」正是漏改那个字节造成的（见第十二节）。
  这种一次性的界面状态，不值得冒那个险。
- 结算逻辑从 `_FocusPageState._settle` 挪到 `settleFocusSession()`（同一份代码，
  专注首页的「结束并结算」也要用），**逻辑一字未改**。
- 新增 8 条单测：`test/focus_suspend_test.dart`（引擎"接回来"的三种情形 + 存档读写与脏数据）。

## 13.3 子待办上限 5 → 7（用户要求）

- `AiTaskDraft.maxSubtaskCount`：5 → **7**。
- 只影响 **AI 拆解**（提示词里那句"最多 N 条"和一个截断循环都用这个常量，改一处即生效）；
  **手动加子待办本来就没有上限**。
- 没同步涨的：标签仍是最多 3 个、图片最多 3 张 —— 用户只提了子待办。
- ⚠️ **没在真机上验证过**：它要真的调一次 AI 才会走到（我们没在测试机上花这个钱），
  改的是一行常量、两个引用点，靠单测与读代码保证。用户下次用 AI 拆子待办时留意一下能不能出到 6、7 条。

## 13.4 真机复验（2026-09-16 夜，第二轮）

装上新包（覆盖安装，数据保留）后逐条走：

- ✅ **成绩卡片**：点下去打开的是「还没有成绩」页 —— **没有 RangeError、没有 ANR**，
  进程 CPU **0.0%**（修复前同一个动作是 **116% 空转**）；返回键能正常退回学业页；logcat 里 RangeError/ErrorWidget 计数为 0。
- ✅ **专注暂停后离开**：
  开始专注 → 点「暂停」（页面上出现新写的那句"已暂停，不计时。现在可以直接返回…"）
  → **按返回直接离开，没有再弹「结束这次专注？」**
  → 专注首页出现「有一次专注还没结束：『专注』· 已专注 32 秒 · 刚刚暂停」+「继续 / 结束并结算」
  → 点「继续」回到专注页：**剩余 44:27、已专注 32 秒 原样接回**（不是从 45:00 重来）
  → 再「结束并结算」→ 确认框 → 结算后那张卡消失。
- ✅ **免打扰已还原**（`settings get global zen_mode` = 0）。
- ✅ 测试留下的那条 32 秒专注记录已从「专注记录」里长按删除，手机上的统计数字回到测试前。
- 测试：544 → **552** 条（新增 `test/focus_suspend_test.dart`）；`flutter analyze` 0 error / 17 warnings。

## 13.5 还没做的（这一轮没碰）

- `#29.8` 的另一半：**成绩页本身**在"有成绩"时点某门**没排进课表**的课的卡片
  —— 代码路径已经堵住（显示「课表里没有这门课」），但**没有真数据可以验证**
  （测试机一门成绩都没有）。
- 子待办 5→7 的端到端验证（见 13.3 的 ⚠️）。

---

# 十四、2026-09-17：教程能放图片了（+ 手把手引导的评估与 v1.4.2 计划）

## 14.1 教程配图：从"框架支持"到"真的能用"

- **之前的状态（说实话）**：教程框架里**早就有** `TutorialImageStep`，但那更像"留了个位置"——
  仓库里一张图都没有，用户点进去只看到「这张图还没放进来」的占位框。
  等于**能力有、内容没有**。
- 这一轮补上了两件事：

### ① 显示方式（`lib/tutorial/steps/step_renderers.dart`）

- **限高**：按屏幕宽度等比缩放，但高度上限是**屏幕高度的 46%**。
  手机截图是 1080×2376 这种竖长条，按宽度铺满要占两屏多，用户得一直往下拉才能看到"下一步"。
- ★ **点图放大**：右下角一个「点图放大」角标 → 全屏、可**捏合/双击**缩放。
  教程里最常见的需求就是"看清那个按钮到底在哪"，这一条不能省。
- **解码时转圈**（截图几百 KB，不给反馈会以为没响应）；图缺失时仍是明确的占位框。
- 步骤多了一个可选的 `title`（图上面的标题）。
- ⚠️ 实现坑：本仓库用的 Flutter 版本里 **`Image.asset` 没有暴露 `loadingBuilder`**
  （命名构造函数里写死成 null），要转圈只能用 `Image(image: AssetImage(...))`。
- ⚠️ **真机一看就发现的问题**：「点图放大」那个角标第一版压在图片右下角 ——
  短图（比如 1080×455 的横条截图）会被它挡住正文那一行字。现在挪到**图片外面**右上角，
  零遮挡。这就是为什么这类改动必须上真机看一眼，光"能编过"不算数。
- ⚠️ 另一个坑：**只写 `- assets/` 时，测试用的资产包不会收进新建的子目录**
  （app 包是好的），导致"图片能不能读出来"这条测试误报。
  已在 `pubspec.yaml` 里把 `assets/tutorial/`、`assets/tutorial/tasks/`、`assets/tutorial/data/` **显式列出来**。

### ② 真的放了两张图（都是真机截图，且**不含任何个人数据**）

| 图 | 用在哪 | 截的什么 | 为什么这样裁 |
|---|---|---|---|
| `assets/tutorial/tasks/types.png` | 待办教程 | 新建页的**四种类型**（活动/截止/提醒/备忘）+ 那行解释 | 只截表单顶部 —— 表单里本来就没有个人数据 |
| `assets/tutorial/data/export.png` | 数据教程 | 设置 → **数据**（导出/导入/复制反馈信息/导入 iCal） | **故意避开上面那行「已登录: 学号」** |

- 红线写进了 `assets/tutorial/README.txt` 和 `docs/TUTORIAL.md`：
  **这些图会跟着 GPLv3 源码公开，绝不能出现姓名、学号、成绩、待办标题、聊天内容**。
- 同时把"怎么自己做一张图"写成了流程（adb 截图 → 裁掉状态栏/导航栏/个人数据 → 放进目录）。

### ③ 一条会红的测试

`test/tutorial_test.dart` 新增：**真的用 `rootBundle` 去加载每一张被引用到的图**，
并断言"教程里至少得有一张图"。以后路径写错、文件忘了放、图被误删，测试直接红 ——
不再出现"框架支持图片、实际一张都没有"这种状态。

## 14.2 手把手引导（在真实页面上高亮按钮 + 气泡）：**工程量偏大，按用户拍板挪到 v1.4.2**

评估写在 `docs/BACKLOG-DEPRECATED.md` 的「v1.4.2 计划」里（该清单已于 2026-09-25 归档，只读），这里记结论：

- 要点：目标控件定位（`GlobalKey` + `RenderBox.localToGlobal`，还要处理"还没构建出来"）、
  位置**实时跟随**（滚动/旋转/键盘/刷新）、遮罩**挖洞**（`Path.combine` + 深色模式 + 安全区）、
  跨页跳转的等待与重试、**找不到目标要能优雅跳过**、打断（返回/切后台/被杀）处理、
  与 `TutorialStore` 的进度体系打通；而且**几乎没法自动化测**，主要靠真机逐页走一遍，
  **每加一个功能就得重走一遍**。
- 估：**约 600~1000 行 + 逐页真机验证**，比自己写一篇"一步一页"的教程高一档。
- 1.4.1 里已有的近似物：`TutorialActionStep` —— "讲完一步 → 一键跳到那个页面自己试"，
  成本几乎为零，先把这条用好。

## 14.3 v1.4.2 计划已建（用户要求"其他没做的也堆到 v1.4.2"）

`docs/BACKLOG-DEPRECATED.md` 顶部有 **「★ v1.4.2 计划」**（该清单已于 2026-09-25 归档，只读），分三档收口：

- **A. 只等条件成熟的**：手把手引导、海宁校区课表节次（等数据）、
  "有成绩时点没排进课表的课"（缺真数据）、AI 子待办 5→7 的端到端验证、
  ★ 专注「该休息了」在后台/锁屏不响（用户点名）、课前提醒的投递方式；
- **B. 用户提过、没排期的**：每节课自动生成复习、翻页再快一点、长按登录看接口连通、
  小组件解封、课程实体剩余、P5 残留字段清理、专注按标签统计、备份自动清理、导航时间服务、AI 后续；
- **C. 要单独开一轮的**：局域网同步、坚果云/WebDAV、Windows 客户端、上架商店、CI、教程内容扩充。

**1.4.1 这条线的口径**：只收**修 bug + 小改进**；
凡是"要新设计、要逐页真机验证、要等外部数据"的，一律排到 v1.4.2 —— 免得发布被无限拖住。

> **★ 2026-09-17 用户澄清口径**：上一轮说的"教程还没写完"指的是**教程内容**（还没写几篇），
> **不是**"手把手引导"那套架构；**v1.4.1 与 v1.4.2 分两次发布**，1.4.1 只是等教程内容补齐。
> 同时清理了项目目录：只留 v1.4.0 正式包归档在 `releases/`，v1.4.1 的成品包都删了
> （`build_apk.ps1` 已改成构建后自动归档到 `releases/` 并打印 SHA-256）。

---

# 十五、2026-09-17（第二轮）：四条真实反馈的修复

## 15.1 ★ 半学期（秋/冬）课程混淆 —— 找到机制了

- **用户反馈**：「最近老是有人反馈秋冬半学期课程冲突，但我去问他们的时候又说好了。
  这让我怀疑是不是 zdbk 连不上导致的。但 eta 作为备用来源，它分不清半学期吗？」
- **结论（三条，都有证据）**：
  1. **eta 分得清** —— 它每一条都带 `xxq`（形如 `秋冬` / `秋` / `冬`），
     `eta.dart` 正是读它来定半学期的；只有缺失/写法不认识时才退化成"两半都上"。
  2. **这台机器上 eta 根本没数据**：真机诊断日志里
     `halfDiagEta：共 0 条；两半都上 0 门；xxq 缺失 0 条`（4 次刷新都是 0 条）——
     课表要么来自 zdbk，要么来自**缓存**。
  3. **真凶在 zdbk 那一路 + 合并逻辑**（见下）。
- **机制（真机日志与代码对上号了）**：
  - 「1|秋」「1|冬」是**分别查**的；行内 `xxq` 读不出来时，代码按"本次查询的季节"猜
    （`Session.fromZdbk` 的 requestedSeason 兜底）→ 秋那次全猜上半、冬那次全猜下半；
  - 两次查询**都会返回同一门课**，而 `Course.completeSession` 原来是
    `a || b` 无脑合并 → **猜出来的秋 + 猜出来的冬 = 两半都上** →
    用户看到"秋冬的课混在一起、还报冲突"；
  - 下次刷新 `xxq` 又能读了 → **自己就好了**，这正好解释"问当事人时已经正常了"。
  - 诊断日志还显示教务**经常返回 HTTP 921（限流）**，课表于是走缓存 ——
    一旦缓存是在"坏数据"那一次写下的，错误会一直留在手机上直到下次成功刷新。
- **修法**：
  - `Session` 新增**不落库**的 `halfGuessed` 标记（行内读不出来、靠查询季节猜的才算）；
  - `Course.completeSession` 改成三条规矩：**两边都确定 → 保持原来的"或"**
    （长学期课拆成秋+冬两条是正常的）；**一边确定一边猜 → 以确定的为准**；
    **两边都是猜的 → 退回"或"**（宁可多显示也别丢课）；
  - 诊断日志补上「其中靠猜的 N 条」，下次再有人反馈，一眼就能指认；
  - 8 条回归测试：`test/half_semester_merge_test.dart`。
- ⚠️ **没做**（记在 BACKLOG）：一门课**所有**行都读不出 `xxq` 时仍只能"两半都上" ——
  那时任何自动判断都是猜。要去掉这个尾巴得等教务修字段，或引入周次做二次推断
  （本科这条现在没有周次）。

## 15.2 专注归属：功能是好的，问题是**看不见**

- **用户反馈**：「当现在有课的时候，自由专注不会自动计入当前课程？」
- **真机实测（就在上课时段里做的）**：17:10 正在上「程序设计与算法基础 16:15–17:50」，
  当场开了一次自由专注 → 结束后「本月按课程」里出现 **程序设计与算法基础 31 秒** ✓
  —— **归属本身是好的**（测完那条记录已删除）。
- **真正的问题是三处都看不出来**：专注页当时一声不吭；专注记录只有名字和时长、没有课程；
  课程页**完全没有专注的影子**。用户点进"当前这门课"看不到任何东西，
  自然得出"根本没计入"的结论。
- **修法（三处都补上）**：
  1. **专注页**：开始就写一句实话 —— 「本次专注会计入《X》」 /
     「本次专注不归属课程（开始时课表里没有课）」/「专注自动计入课程：已关闭」；
  2. **专注记录**：每条后面带上课程名（`专注 · 程序设计与算法基础`）；
  3. **课程详情页新增「专注」区块**：这门课一共专注多久、几次、最近五次是什么时候
     （按 `FocusSession.courseId` 查，不存冗余）；
  4. 顺手改掉统计页那句会被误读的说明：原来是
     「只算挂到课程上的专注…自由专注不计入这里」，读起来像"自由专注永远不算课程"；
     现在写「自由专注只要开始时正在上课，也会算到那门课上」。

## 15.3 长按登录 → 看接口连通性（用户要求）

- 新增面板 `lib/mod/login_connectivity.dart`：一屏列出
  **校历 / 课表 / 考试 / 成绩 / 主修 / 作业 / 实践素质拓展** 每条的状态
  （实时成功（含耗时）/ 走缓存 / 没连上 / 未参与）+ 汇总「成功 N · 走缓存 N · 失败 N」，
  没连上的单独点名。
- 数据**直接复用现成的诊断日志**（不额外发请求）；「重新检查」会真的跑一次刷新再回到面板；
  「完整日志」跳到「设置 → 测试日志」看每条请求的网址与返回码。
- 两个入口：**设置页「已登录」那一行长按**（副标题写了"长按可查看各接口是否连通"，
  否则没人会去长按一个看起来只是显示状态的列表项）、**登录页的登录按钮长按**
  （登录不上时最想知道的就是这个，而且不用先登录进 App）。

## 15.4 深色模式：翻卡片时"变白再恢复"（用户反馈）

- **现象**：深色模式下翻「接下来 ⇄ 日历」那张卡，整张卡会先变白再恢复。
- **原因**：翻页效果里有一条「页边」（铺满卡片的圆角矩形，靠偏移 3dp 在卡片外侧露出薄薄一条），
  而卡片的各个面是**页面内容**（背景透明）。翻转时页边颜色会从卡片底下透出来 ——
  而 `CupertinoDynamicColor` **不会自己解析**（普通 BoxDecoration 拿到的是"浅色那一套"），
  于是深色下透出来的仍是 242,242,247（≈白）。
- **修法**：build 里显式 `CupertinoDynamicColor.resolve(widget.paperEdgeColor, context)`
  再上色（App 的深色模式跟着 `brightnessMode` 走，CupertinoTheme 亮度是对的，所以解析有效）；
  坑写进注释，免得以后被"优化"回去。

---

# 十六、2026-09-17（第三轮）：校历与课表"老是连不上"的系统性修法

> 起因是用户的一个问题：**「为啥校历和课表总是难以连接上？」**
> 查清之后用户拍板："你提的建议全做"，于是有了这一节。
> 结论先说：这两件事**不是同一个毛病**；在"一周一次 + 随包内置"之后，
> 它们从"每次刷新都可能失败"变成了"基本不再打扰远程"。

## 16.1 先回答那个问题：两条完全不同的病因

**课表 = 被学校限流（HTTP 921）**：

- 一次刷新有 **7 个模块并发**（校历/课表/考试/成绩/主修/作业/实践）；
- 光「课表」一个模块，就要按 `1|秋` `1|冬` `2|春` `2|夏` **分别查 4 次**（还要探测未来学年）；
- 加起来**一次刷新向教务打 9 个以上请求**，教务的反爬于是返回 **HTTP 921**（学校自己的码，不是标准 HTTP 码）；
- 后台刷新（WorkManager）+ 打开 App 的自动刷新还会各打一套；
- 真机诊断原文：一次刷新里 **4 条「服务端返回 HTTP 921」**，课表「使用缓存」。

**校历 = 压根不在浙大服务器上**：

- 它来自**上游 Celechron 的第三方静态站** `http://calendar.celechron.top/`，**明文 HTTP**；
- 实测还发现：那一站 **HTTPS 握手直接失败**（`未能创建 SSL/TLS 安全通道`），只能走 HTTP；
- 于是在"第三方域名 + 明文 HTTP + 8 秒超时"三重影响下时好时坏；
- 未来学期的 key 返回 404（NoSuchKey）是**正常状态**，不是故障。

**为什么 eta 兜不住课表**：eta 只在 zdbk **返回空**时才启用，而这台机器上 eta 返回 **0 条**
（诊断：`halfDiagEta：共 0 条`），等于没有兜底 → 直接落缓存。
## 16.2 校历：随包内置 + 每周一次 + 比对取新（用户拍板）

- **随包内置**：把公开的校历 JSON 直接打进包里（`assets/calendar/<学年学期>.json`）——
  离线可用、首次安装就有、也不必每次刷新去敲那个不靠谱的站。
  当前带了 `2025-2026-1 / 2025-2026-2 / 2026-2027-1` 三份。
- **每周才试一次远程**：记的是"上次**尝试**"而不是"上次成功"
  （对着长期挂掉的站不该每次都重试）；纯函数 `isCalendarRemoteUpdateDue` 可单测。
- **比对差异、按新的来**：连上了就跟本地那份比，内容变了才覆盖，诊断里写明
  「已按新的覆盖 / 与本地一致，未变化 / 首次获取」。
- **降级顺序**：同学期缓存 → **随包内置** → 本地推算（每一级都记来源）。
- **候选地址**（①→④依次试；前一个"连不上"或"没有这份配置"才换下一个）：
  1. 上游 HTTPS（现在握手失败，留着等上游修好 —— 一周才试一次，成本极低）
  2. 上游 HTTP
  3. **Gitee 镜像**（HTTPS；仓库里就有 `assets/calendar/`，raw 地址天然可用，国内更快）
  4. GitHub raw 镜像
- ⚠️ **踩坑记**：下载 JSON **不能用 Invoke-WebRequest 的 content 字符串** ——
  它按响应头猜编码，而那一站没给 charset，于是中文全变成 `ä¸­ç§è`。
  必须用 `curl -o`（或 `-OutFile`）存**原始字节**。现在有一条测试专门盯这个
  （断言内置 JSON 里含「中秋」「国庆」）。

## 16.3 被限流要能自愈：921 退避重试

- `_fetchWithRetry` 原来只对"超时 / socket / 连接关闭 / 需要重登"重试一次，
  **921 不在任何重试名单里** —— 一次被限流就整块落缓存（真机 grep 确认过一行都没有）。
- 现在把 921 / 429 /「请求过于频繁」归为可重试：**退避 3 秒再试一次**，
  诊断里记一行 `operation=rateLimitBackoff`；本科与研究生两个 spider 都加了。
## 16.4 少打一点：教务请求串行化 + 启动刷新节流

- `Zdbk._withSitePermit` 原来是"最多 3 并发"，现在是**一次只放一个**，
  且两个请求之间至少隔 **300 毫秒**。代价：9 个请求多花 2~3 秒；收益：不再被限流。
- **启动自动刷新加最小间隔**：最近 5 分钟内成功更新过就跳过
  （后台刷新与前台刷新不再叠着打）；下拉刷新随时可以手动来一次。
- 连通性面板现在会**点名**："这次有 N 个请求被教务限流（HTTP 921）——学校那边让你慢一点"，
  并说明"校历用的是随包内置那一份"。

## 16.5 「校历还能从哪拿」——调研结论

| 来源 | 能不能用 | 说明 |
|---|---|---|
| 上游 `calendar.celechron.top` | 现在是主源之一（但常挂） | 明文 HTTP、HTTPS 握手失败、**不在任何公开仓库里**（GitHub 代码搜索只搜到 App 自己的解析代码） |
| **我们自己的仓库镜像** | ✅ **已启用** | 同一份 JSON 就在 `assets/calendar/` 里，raw 地址天然是 HTTPS 源；改一次全部设备可更新 |
| 浙大官方 PDF / 网页（本科生院、教务处、国际校区） | ⚠️ 人工 | 权威但是 PDF/图片，机器解析脆弱；适合"一年两次人工誊一份" |
| zdbk 自己的通知页 | ⚠️ 人工 | 例如「关于做好 2026-2027 学年秋学期开学工作的通知」（`xwck_ckLoginNews`），开学日期在正文里 |
| 从课表反推 | ✅ 已有兜底 | `buildSafeDefaultCalendarConfig`：能推出开学/周历，**推不出节假日与调休** |
| eta / 学在浙大 | ❌ 没找到可用的校历接口 | 而且这台机器上 eta 返回 0 条 |

**建议的长期口径**：以"**仓库镜像 + 随包内置**"为主，celechron.top 降为第二来源；
每年两次（秋、春）人工从官方 PDF 誊一份 JSON 进 `assets/calendar/` —— 成本极低、稳定性最高。
真要再省事，可以做一个小工具：把官方 PDF 上的"开学日期 / 节假日 / 调休"填进模板并校验。

## 16.6 验证与测试

- 新增 `test/calendar_bundled_test.dart` **11 条**：内置三份都能真的读出来、
  能通过 `decodeAndValidateCalendarConfig` 校验、2026-2027-1 就是这学期（9/14 开学、11/9 转下半）、
  中文没乱码（编码护栏）、没有内置的学期返回 null、每周节流的四种边界、四个候选地址的顺序与协议。
- `flutter analyze` 0 error / 17 warnings；测试 **561 → 572**。
- 真机复验见 16.7 —— 里面有**一个没解决的发现**。
## 16.7 真机复验（2026-09-17 晚）—— 以及一个**没解决**的发现

装上这一版后长按「已登录」，面板变成：

- 「最近一次刷新：刚刚（前台刷新）｜成功 5 · 走缓存 1 · **失败 1**」
- 逐条：校历 走缓存 / **课表 没连上** / 考试 实时成功（3.0 秒）/ 成绩（2.4 秒）/
  主修（2.1 秒）/ 作业（2.9 秒）/ 实践素质拓展（4.1 秒）
- 新加的那句提示**生效了**：「这次有 **5 个请求被教务限流**（HTTP 921）——学校那边让你慢一点…」
- 「没连上的：课表」「走缓存的：校历」也各自点名了

**★ 诚实记一笔：串行化并没能解决限流。** 这一次刷新仍然撞了 **5 次 921**，课表还是失败了。
把代码翻出来数一遍，原因就清楚了：

- 课表是**按学年 × 学期**逐个查的：`for (season in ['1|秋','1|冬','2|春','2|夏'])`；
  而学年的范围是「入学年 → 当前年（+探针年）」——
  26 级新生 = 2 个学年 × 4 次 = **8 次**，高年级（23 级）则是 4 个学年 × 4 次 = **16 次**！
- 再加成绩 / 主修 / 考试 / 作业 / 实践，一次刷新对教务就是 **13~21 个请求**。
- 也就是说：**教务的限流更像是"一个时间窗内的请求条数"，不是"同时并发几个"** ——
  所以"串行 + 300ms 间隔"（16.4）只能让波形好看一点，**条数没减，效果有限**。

**下一步该做的（记在 BACKLOG，等实施 + 真机验证）**：**历史学期不必每次刷新都查**。
- 过去学年的课表/成绩**基本不会变**，只在"本地没有这份数据"或"每周一次"时才去拉；
- 当前学年 + 探针学年仍然每次都查（这两块才需要新鲜）；
- 预期：一次刷新的教务请求从 13~21 降到 6~9，正好落回限流阈值以内。

这条改动会碰刷新主链路，**必须配真机验证**（确认历史学期不会因为跳过而消失），
所以没有塞进这一轮 —— 宁可少改一点，也不要一边修限流一边把课表弄丢。

> ✅ **2026-09-17 深夜：用户拍板后已按上面的口径实施完毕，见 16.8。**

## 16.8 ★ 后续（2026-09-17 深夜，用户拍板之后实施）

用户原话：「按照你说的来吧，减少对历史学年的探查来减少请求次数。同时，可以在后台计算一下时间，
每隔一段时间发一个请求，这样虽然刷新会变得很慢，但是有缓存兜底，后台只需要静默刷新就行。」

于是做了六件事（**全部真机验证过**）：

1. **历史学年只吃缓存**（`preferCache`）：过去学年的课表基本不变，有缓存就直接用
   （诊断记 `cacheFirst`），没有缓存才联网 —— 新装用户不会因此缺数据。
   对高年级是最大的一笔：23 级原本一次刷新要查 4 个历史学年 × 4 个学期 = **16 次**，现在 **0 次**。
2. ★ **"已知为空"的学期，当天不再重复问**（`skipKnownEmpty`）：新生的 26-27 学年里
   `1|秋``1|冬` 有数据，而 `2|春``2|夏` 与整个探针学年都还没开放（返回 0 行）——
   一次刷新的 8 个课表请求里**有 6 个是明知故问**。现在当天问过是空的，当天就不再问
   （第二天再确认一次）。**真机实测：一次刷新跳过 4 个请求**（日志里 4 条 `skipKnownEmpty`）。
3. **探针学年只探第一个学期**：探针只为"下学年开没开"，问 `1|秋` 就够了，
   其余三个学期等它变成当前学年再查（每天再省 3 个请求）。
4. ★ **探针学年被限流不再拖垮整个课表**：真机复现过一个很坑的现象 ——
   探针学年撞 921，面板上「课表」直接变成"没连上"，可实际上当前学年的 22+21 条课都拿到了。
   现在探针学年的 921 归入"没问到"，不算失败。
   **真机验证：失败 1 → 失败 0，课表从"没连上"变成"走缓存"。**
5. **后台慢节奏**：相邻两个教务请求的最小间隔 —— 前台 **300 毫秒**、后台 **15 秒**
   （`Zdbk.foregroundRequestGap` / `backgroundRequestGap`）。后台刷一次约 6~9 个请求
   ≈ 1.5~2.5 分钟，静默完成 —— 正是用户说的"刷新很慢但没关系，有缓存兜底"。
6. **启动刷新 5 分钟节流**：最近 5 分钟更新过就跳过启动自动刷新（真机日志能看到
   `startupRefreshSkipped`），不再和后台刷新叠着打。

顺手修的：**连通性面板重启后读不到历史** —— 原来只读**本进程内存**里的日志，
所以 App 刚起来时面板显示"还没有刷新记录"；现在改用 `recentText()`（含落盘那份）。
真机验证：重启后打开面板，看到的是「最近一次刷新：1 分钟前（20:09）」。

**新增测试** `test/timetable_fetch_policy_test.dart` **11 条**：把"哪些请求不用打"的规则
抽成纯函数（`TimetableFetchPolicy`）逐个钉死 —— 今天/昨天、历史/当前/未来学年、
缓存优先时不吃"已知为空"、标记键不串味、历史学期的校历不再试远程。
测试总数 572 → **583**。

---

# 十七、教程内容开始落地（2026-09-17 深夜）

用户手写的 `docs/GUIDE_V1.4.1.md`（"零、配置相关 / 一、基本操作 / 二、课程相关 / 三、专注模式"）
开始搬进 App。**文字照抄用户的稿子**，图片按稿子里 `【图片】（提示…）` 的要求逐张准备。

## 17.1 这一轮进去了两篇

| 教程 | 步数 | 图片 |
|---|---|---|
| **开始之前：几项配置** | 15 步 | **用户给的 6 张**：DeepSeek 官网 3 张 + Elychron 设置 3 张（直接打进包里） |
| **基本操作：手势与翻页** | 14 步 | **我在真机上拍的 5 张**：右滑完成 / 左滑删除 / 翻转中的卡片 / 三条线 / 横 V |

- 图片放 `assets/tutorial/setup/` 与 `assets/tutorial/basics/`，pubspec 里**显式列出**
  （只写 `assets/` 时测试用的资产包收不到新目录，这是老坑）。
- 教程中心现在共 **4 篇**（另有《待办：四种类型怎么选》《数据：搬家、备份与导入》）。
- **真机验证**：教程中心列出 4 篇、进度条 `1 / 15`、图片正常渲染、「点图放大」提示都在。

## 17.2 拍截图的两条规矩

1. **不能出现任何个人数据**（这是公开仓库）。所以待办那几张用的是我自己建的
   「示例任务：××」（用分享入口建的），拍完就删；画面也裁到只剩要讲的那一块。
2. 稿子里怎么写就怎么拍 —— 例如「尽量展示划到一半，露出两种颜色」，
   是用 `input motionevent DOWN/MOVE` 把卡片拖到一半、**按住不放**截的图；
   「同时包含小圆和正在旋转的卡片」则是在点下小圆的**那一瞬间**连拍抓帧。

## 17.3 还没拍的图（下一轮）

已完成右滑恢复（1）· 日历折叠 / 展开（2）· 课程详情页的三块（1）· 专注模式 6 张
（专注页 / 选择待办 / 命名弹窗 / 工作中 / 暂停效果 / 暂停后首页的黄色提示卡）。
等这些拍完，「一、基本操作」的图就齐了，「二、课程相关」「三、专注模式」两篇也能直接成篇。

> ✅ 这批图**已经拍完并上线**（含最后找到的日历折叠把手）；用户随后提的三条反馈也一并改掉了，见 17.4。

## 17.4 用户的三条反馈（2026-09-17 深夜，已全部改掉）

> 原话：「第一，不要改我的文案。虽然很抱歉，但是你写的一股子人机味儿……
> 第二，图片跟文字不能放在同一步里面吗？分开看会真的真的很难受
> 第三，你自己看看这个界面的截图。为什么这些条目的边上都有一圈灰色的东西？」

**① 文案一字不改。** 四篇教程（配置相关 / 基本操作 / 课程相关 / 专注模式）
全部改成**用户原稿的原文**：标题用他加粗的那几个（`配置环境` `DeepSeek密钥配置` …），
句子照抄，**我自己编的小标题和图注全部删掉**。
同一节的后半页把标题留空 —— 渲染器对空标题直接不画标题行，这样就不必为了"每步一个标题"
替他造句。注册表完整性检查也跟着放宽（原来要求每步都有标题）。

**② 图片和文字放在同一步。** `TutorialImageStep` 加了：
- `body`：图**上面**的正文段落（一段话配一张图）；
- `assets`：同一个步骤里的图片改成**列表**（原稿里"点三条线切换"那句下面就跟着两张图，
  现在不用硬拆成两步）。
渲染顺序：标题 → 正文 → 图（一张或多张）→ 图注。真机验证：
「当待办未完成时右滑待办，可以直接完成」下面紧跟着那张半划截图 ✓。

**③ 灰圈找到了。** 教程中心和教程播放器的 `CupertinoPageScaffold` **没给页面底色**，
默认是纯白；而 `CupertinoListSection` 会自己往每个区块下面画一块浅灰圆角背景 ——
**白底衬灰块**，于是每条看起来都套了一圈灰。设置页一直写着
`backgroundColor: CupertinoColors.systemGroupedBackground`，所以那边从来没有这个问题。
两个页面补上这一行即可：现在一个分组一张白卡、行间只有细线，和设置页完全一致。

顺带把「一、基本操作」缺的最后两张图补上了：日历**折叠**那根横线、折叠后的 **V 形图标**
（真机找到了把手：月历下方正中那根小横线，点一下折成一屏一周）。

> ✅ 2026-09-18：用户又报了六个问题，见**第十八节**（含"分享进来打断专注"与"图片分享不弹窗"两个真凶）。

---

# 十八、2026-09-18：用户报的六个问题（全部处理完）

## 18.1 课程资料：图片能看大图、能重命名

- **缩略图**：课程详情「资料」里的图片附件现在直接显示 46×46 缩略图
  （以前一律是同一个图标，得一个个点开才知道哪张是哪张）；文件被清理掉时自动退回图标。
- **点一下看大图**：图片在应用内全屏预览（可捏合放大、拖动），不用跳出去看再跳回来。
- **长按重命名**：长按任意附件弹出菜单：重命名 / 用其它应用打开 / 删除。
  重命名只改**显示名**（`TaskAttachment.name`），后缀自动保留、不动磁盘上的文件名
  （改磁盘名要动文件系统还可能撞名，而用户要的就是"列表里那行叫什么"）。

## 18.2 ★ 专注不该被"外面分享进来"打断（真凶找到了）

用户现象：「外面通过分享进入 Elychron 会把专注打断」。

真凶不是分享本身，而是**专注会话的"僵尸"处理**：
App 被系统杀掉后，那条未结算的会话一直躺在库里，下次打开专注页就被
`FocusPage._settleStaleSessions()` 当成僵尸**结算成"未正常结束"**。
于是用户眼里就是"分享一下，专注没了、还被记了一笔异常"。

修法（`mod/focus_suspend.dart` 新增 `adoptStaleFocusSessions`）：
打开专注页时，把**最新**那条未结算会话转成"暂停中" —— 专注首页出现继续卡片，
点一下原样接着做，**既不结算也不记异常**；更老的僵尸会话才按最后记录结算。
另外多一句说明：「上次专注（X 分钟）中途被系统打断了，已经保留在这里，可以继续」。

**真机验证**：开始专注 → 25 秒后 `am force-stop`（模拟系统杀进程）→ 重开 App →
专注页显示「有一次专注还没结束 · 已专注 30 秒 · 刚刚暂停」+ 那句说明 ✓，
点继续能恢复到暂停态接着计时 ✓。

## 18.3 ★ 有暂停的专注时点"开始专注"，不再强制打断上一次

以前这里是**无脑开新的一次**，旧的那条就成了"未正常结束"（用户点名的现象）。
现在先弹一个三选一：「继续上一次」/「结束并开始新的」/「取消」；
选"结束并开始新的"时，上一次按**正常结束**结算（用户认了那段时间）。

**真机验证**：有暂停会话时点开始专注 → 弹窗三个选项都在 ✓。

## 18.4 ★ 分享：图片不弹窗的真凶有两个

用户现象：「分享功能出问题了，图片分享进来没有自动弹出弹窗」。查到两条原因，都修了：

1. **只读 `EXTRA_STREAM`**：Android 13+ 的相册、以及不少新版 App 分享图片时
   把 URI 放在 **`clipData`** 里，`EXTRA_STREAM` 是空的，我们拿到 0 个 URI，
   于是什么都不弹。现在两处都读，并按 URI 去重。
2. **读不出来就悄悄丢掉**：附件复制失败时以前是「?: continue」，
   整条分享被丢干净、连一句话都没有。现在**如实上报**：
   - 全都读不出来 → 弹「这个分享读不出来」并说明原因
     （真机验证：分享一个没有读权限的文件，弹窗如实说"对方应用没有把 1 个文件的读取权限给过来"）；
   - 部分读不出来 → 正常弹目的地选择，选项副标题会注明
     「有 N 个文件读不出来，已跳过」。

顺带修掉一个连带问题：分享拦截原来用"库里有没有未结算会话"判断"是不是在专注"，
被系统杀掉后那条会话会一直留着，**分享从此被无限期攒着**（这就是用户遇到的"什么都不弹"）。
现在改成读**运行时状态**（`mod/focus_runtime.dart`：专注页真的在计时才算 running）。

## 18.5 ★ 更新检查改成"全部源取并集"

用户要求：「务必要保证任何情况下有更新版就会提醒，不是说比如 github 似连非连就不看其他网站了。
全部检查取并集。」

以前是**串行 + 第一个能通就 break**，一个半通的 GitHub（有应答但是旧数据）能把
已经发新版的 Gitee 挡在后面。现在：

- **四个源并行查**（总耗时约等于最慢那个，不再是 4 × 8 秒）：
  GitHub API / Gitee API / gh-proxy 反代 / kkgithub 反代；
- 所有应答里**取版本号最大的那个**（`Fuse.newestVersion`，纯函数，5 条单测锁住），
  并按那个源给下载入口；
- 谁都不应答时安静跳过，下次启动再试。

## 18.6 第一次打开 App：引导一句"这里有教程"

用户要求：「没打开过教程的 app 第一次打开默认弹出一个弹窗，引导进入教程」。
判据（`TutorialStore.shouldShowIntro`，纯函数 + 5 条单测）：
**没弹过 + 没进过教程中心 + 没看过任何一篇** 才弹，弹过就记一笔不再打扰。
点「去看看」直接进教程中心，点「以后再说」也不再弹。
（已经看过教程的老用户不会被这个弹窗打扰。）

## 18.7 这一轮的测试与验证

- 测试 **583 → 593**（新增 5 条并集单测 + 5 条引导判据单测）；analyze 0 error / 17 warnings。
- 真机验证：18.2 / 18.3 / 18.4 三条都在用户手机上跑通（含"杀进程后接住专注"这种极端路径）。
- ⚠️ **18.1（课程资料图片预览与重命名）没能在真机上走完整流程**：
  这台机器的系统相册选择器没有可点的"完成"按钮，自动化点不到；
  代码 analyze 干净、沿用同一套弹窗与卡片组件，等用户自己点一次确认。
- 顺带踩到并记录：改 Kotlin 之后 Gradle 报了三个假的 `Unresolved reference`
  （指向另一个文件里明明存在的顶层函数），删掉 `android/.gradle` 与 `android/app/build`
  再构建即可，这是本项目已知的 Kotlin 增量缓存问题。

---

# 十九、发布记录：GitHub 已发，Gitee 被令牌卡住（2026-09-18）

## 19.1 GitHub Release 已发布 ✓

- 地址：https://github.com/Elyyyyyyyyxer/Elychron/releases/tag/v1.4.1-elychron.1
- 标签 `v1.4.1-elychron.1` 已推；正文就是 `docs/RELEASE_NOTES.md`；附件
  `Elychron-v1.4.1-elychron.1-arm64.apk`（28,817,986 字节，与本地归档一致）
- 复核过 `releases/latest`：现在指向 v1.4.1 ✓ —— 也就是说 **App 里的更新检查马上就能认到**。
- 怎么发的（免得下次再摸）：本机**没装 gh**，走的是 GitHub REST API +
  凭据管理器里的 token（`git credential fill` 取，40 位）：
  `POST /repos/Elyyyyyyyyxer/Elychron/releases` 然后
  `POST uploads.github.com/…/assets?name=…`

## 19.2 ★ 一个坑：Windows Schannel 的吊销检查会把 GitHub API 全掐掉

第一次调 API 时 curl 返回空、连 `api.github.com` 都是 `000`，带 `-S` 才看到真话：

    curl: (35) schannel: next InitializeSecurityContext failed:
    CRYPT_E_NO_REVOCATION_CHECK (0x80092012) - 吊销服务器无法验证证书是否已吊销

**修法：给 curl 加 `--ssl-no-revoke`**（`gitee.com` 不受影响，只有 GitHub 这边这样）。
以后在这台机器上用 curl 访问 GitHub API 都得带这个参数。

## 19.3 ★ Gitee 为什么一直没有发行版，以及怎么修好的（用户问的）

**先给结论：以前不是忘了发，是根本推不上去；这一轮用户给了私人令牌，已经全部发好 ✓**

- Gitee 发行版：https://gitee.com/P3RF3CT/elychron/releases/tag/v1.4.1-elychron.1
  （APK 已附：`Elychron-1.4.1-elychron.1-arm64.apk`，与 GitHub 上那个是同一个文件）
- Gitee 代码也补齐了：`git push gitee main` 把落后的 **100 个提交**一次快进推平
  （`gitee/main` 现在与本地一致），tag `v1.4.1-elychron.1` 也推上去了。
- 本仓库的 `gitee` remote 已配好，以后发版两行命令：
  `git push gitee main --tags` + 建发行版（命令见下）。

诊断过程（留给下次）：

1. **Gitee 已经不接受账号密码**：本机凭据库里 Gitee 存的是账号密码（`P3RF3CT`，14 位）
   - `git push gitee main` → `fatal: Authentication failed`；
   - Gitee OpenAPI 用同样的凭据 → `401 Unauthorized: Access token does not exist`。
   现在 Gitee 的 git push 与 API 都要**私人令牌**（access token）。
2. 所以 **Gitee 上的代码比本地落后 100 个提交**（`ahead 100 / behind 0`，是一次干净的快进，
   不是分叉）；v1.4.0 的 tag 倒是早在能推的时候推上去了，
   但**发行版一个都没有** —— 建发行版只能靠令牌或网页手动做。
3. 我自己的文档里其实也记过一笔：「Gitee 发行版还没建（需要与 GitHub 同 tag、同 APK），
   等用户发话再发」—— 当时就没发。

**发版时的三步（都已验证可用）**：

1. 令牌存进凭据库（**不要**写进命令行或 URL）：把 protocol / host / username / password
   四行加一个空行（换行用 PowerShell 的 [char]10 拼）管道给 `git credential approve`。
2. `git push gitee main --tags`。
3. 建发行版 + 传附件（Gitee OpenAPI，curl 即可）：

       POST /api/v5/repos/P3RF3CT/elychron/releases
            参数：access_token / tag_name / target_commitish=main / name / body 取自 docs/RELEASE_NOTES.md
       POST /api/v5/repos/P3RF3CT/elychron/releases/<id>/attach_files
            参数：access_token / file=@releases/<apk>

Gitee 的下载链接会 302 跳转，属正常；实际拉下来的内容确认过是 APK（ZIP 结构）。



## 17.5 用户对截图的七条修改（2026-09-17 深夜，全部改完）

> 原话见下面每条的引用。**七条里有六条是重拍/重裁，第七条是真 bug。**

| # | 用户意见 | 处理 |
|---|---|---|
| 1 | 基本操作第 7 步「旋转到一半太丑了，还是在平面的时候截图吧」 | 重拍成**平面状态**的「接下来」页（标题 + 小圆 + 第一张卡片） |
| 2 | 第 8 步「只展示图标会让人不知道在什么地方，适当放一点其他部分出来」 | 两张都重裁：三条线那张带上「接下来」标题与卡片，横 V 那张带上学年学期标题与周次行 |
| 3 | 第 10 步「这里展示展开状态的日历最好」 | 重裁成**整月展开**的日历 + 下方那根横线把手 |
| 4 | 第 3 步「最顶端左侧似乎略有毛刺」 | 重拍（毛刺来自上一个裁切框进了标签栏底边），新图只留卡片本身 |
| 5 | 专注模式第 1 步「把我手机的状态栏和导航栏截掉」 | 重裁：去掉状态栏与底部系统导航栏（y 96–2210） |
| 6 | 第 8 步「下面的按钮都被截掉一半啦，保留行不行？还有缺少了上面的图标」 | 重拍：整页入画 —— 顶部「专注」标题 + 大圆 + 提示卡 + **继续 / 结束并结算按钮完整保留** |
| 7 | 第 9 步「标题、正文没有左对齐」 | **真 bug**，见下 |
| 8 | （追加）第 8 步图二与第 10 步「暴露我的课表了，请把所有的字抹掉」 | 两张都重裁到**不含任何课程名**：第 8 步的课表图只留学期标题 + 横 V 图标 + 周次行；第 10 步的日历图只留整月与折叠把手（下面那截带课名的列表裁掉） |

### 第 7 条是个真 bug（不是截图问题）

`TutorialPage` 里那个 Column **没写 crossAxisAlignment**，默认是 **center**；
而步骤内容是按**自身宽度**居中的 —— 长段落刚好占满整行看不出问题，
短一点的步骤（「你专注了吗？」）整块就被推到屏幕中间去了。
加上 `crossAxisAlignment: CrossAxisAlignment.stretch` 即可。

真机验证（同一屏）：「你专注了吗？」`x=66..419`、「rt，好好利用专注模式吧！」`x=66..579`，
和底部按钮左边缘 `x=66` 完全对齐 ✓。











