# 多端同步（设计 / 准备稿）

> 状态：**准备阶段**，先对齐范围再动手。相关 backlog：第 1 项（面板美化）、
> 第 2 项（附件同步）、第 3 项（WebDAV 跨网）、第 4 项（Windows 客户端）、
> 第 8 项（同步快速入口）。

---

## 一、家底：现在已经有什么（实测清点）

| 文件 | 行数 | 干什么 |
|---|---|---|
| `lib/mod/lan_sync_server.dart` | 258 | 手机上的 HTTP 服务：`GET /`（面板）、`POST /pair`（配对拿 token）、`GET/POST /bundle`（取/交数据）|
| `lib/mod/lan_panel.dart` | 312 | 浏览器面板（单个 HTML+JS 字符串）：查看 / 打钩 / 删除 / 新建 / 导出 / 导入 |
| `lib/mod/lan_sync_page.dart` | 182 | App 内的入口页：开服务、显示地址与配对码 |
| `lib/utils/data_sync.dart` | 179 | **数据契约** `DataBundle`：`format='celechron-mod'`、`exportedAt`、`tasks`、`tombstones`、`tags`、`tagColors`、`reminderMode`、`alarmTheme`；合并结果 `DataMergeResult`（added/updated/removed）|
| `lib/utils/data_backup.dart` | 91 | 文件导出/导入（同一份契约，走文件而不是网络）|
| `lib/model/tombstone.dart` + `lib/mod/database_mod.dart` | — | **删除墓碑**：合并时判断「这条是被删掉的」而不是「对方新增的」|

**已经有底子的地方（很重要）**
- ✅ 数据契约是**版本化 + 传输无关**的（`DataBundle` + 墓碑），加新传输方式是**加法不是重写**；
- ✅ 已经有**两条**通路：网络（`/bundle`）与文件（导出/导入）；
- ✅ 合并逻辑有 `added/updated/removed` 统计，能如实告诉用户改了什么；
- ✅ 配对机制（token）已经有了。

**现在的缺口**
- ❌ 只同步 **待办 / 标签 / 少量设置**：`dbFocus`（专注记录）、附件文件、其它设置**都不在里面**；
- ❌ 面板是「手机当服务器」的单向心智：PC 端本质是遥控器，**不是对等端**；
- ❌ 没有**设备身份**：看不出「这条是哪台设备改的」，也没有「同步到哪些设备」的概念；
- ❌ 没有**实时推送**：现在是一次性的取/交，不会自动感知对方的变化；
- ❌ 冲突策略是**整条 last-write-wins**（靠 `updatedAt`），没有分字段合并；同一秒两边改会丢一边。

---

## 二、已拍板（2026-09-12）

| # | 问题 | 决定 |
|---|---|---|
| 1 | 目标形态 | **先做 A：手机 ⇄ 电脑（浏览器面板）**；B（Windows 客户端）保留为长期选项，先不动；C（手机互联）/ D（跨网络）按下面第 7 条走 |
| 2 | 同步范围 | **「除本科教务网拉下来的数据之外，所有用户数据」**（详见下面的数据清单）|
| 3 | 冲突策略 | **墓碑优先** ✓ |
| 4 | 设备身份 | **要**（`deviceId`，首次运行生成、存本地、随包上传）|
| 5 | 自动 / 手动 | **打开面板自动拉 + 改完自动推**（实时推送先不做）|
| 6 | 安全边界 | 按建议：token 每次开服务重生成 + 面板显示已连接设备 + 文档写明「只在可信 Wi-Fi 用」，**不做应用层加密** |
| 7 | 传输路线 | **先做同一局域网**（现有这套）；**坚果云 / WebDAV 路线保留**。**最终两条都要留着，让用户按情况自由切换** |

### 关于「为什么不直接把 App 装到电脑上」（用户问过，记下来）

Flutter 跨平台跨的是 **UI 层与 Dart 运行时**，不是整个 App。所以：
1. **APK 是 Android 二进制，Windows 跑不了** —— 以前能靠微软的 WSA（Windows 子系统 for Android），
   但**它已在 2025 年 3 月停止支持**；剩下的安卓模拟器体验与「顺手的桌面工具」是两回事；
2. **就算为 Windows 重新编译，插件层也过不去**，实测统计（BACKLOG 第 4 项）：
   - `workmanager`（上游自建 fork）**只支持 Android/iOS** —— 11 处
   - `flutter_local_notifications` **不支持 Windows** —— 18 处
   - `MainActivity.kt` / `ECardWidget.kt` 里的校园卡小组件、全屏闹钟、分享接收
     —— **这些功能在 Windows 上本身就不存在**，整层要重写
   - 另有 `Platform.is` 23 处、`PlatformFeatures` 抽象 11 处 —— 说明代码从没打算跨端跑
3. 真想要桌面端，**得先回答「在电脑上干什么」**：
   - 只是「看和改待办」→ **浏览器面板就是最优解** ✓（零安装、任何电脑能开、改完自动同步）
   - 「电脑上常驻提醒 / 离线重度用」→ 才值得上 Windows 客户端（工作量比面板大一个量级）
   - 好消息：`hive` / `flutter_secure_storage` / `path_provider` / `file_picker` /
     `share_plus` / `table_calendar` **都支持 Windows** ✓ 基础不是从零开始

### 数据清单（按「用户数据 vs 学校数据」划线）

**要同步的（用户自己产生的）**

| 数据 | 现在 | 说明 |
|---|---|---|
| 待办（含子待办/评论/附件条目/标签）| ✅ 已有 | 核心 |
| 删除墓碑 | ✅ 已有 | 双向同步的地基 |
| 标签库 + 标签颜色 | ✅ 已有 | |
| 提醒方式 / 默认提前量 / 闹钟配色 / 亮度模式 | 部分已有 | 都是小标量，一起带 |
| 课程代码自定义映射 | ❌ | **是用户手动配的** → 要同步 |
| **自定义日程（学生组织例会那种）** | ✅ **已有** | 2026-09-25 加：`DataBundle.userEvents` + `userEventTombstones`（见下）|
| **专注记录 `dbFocus`** | ❌ | 要同步（统计页多端一致才有意义）|
| 专注参数（工作时长/休息时长/休息提醒）| ❌ | 要同步 |
| **附件文件本体** | ❌ | 要同步，但**单独一个阶段**（sha1 命名 + 懒下载，见 S3）|

**不同步的（从学校服务器拉的，可重新获取）**

课表 / 考试安排 / 成绩 / 实践分 / 校园卡余额 / 校历缓存 —— 同步它们只会带来**陈旧与冲突**，
每台设备各自去学校拉一次最干净 ✓

**需要你单独拍板的两类敏感数据**（我倾向都**不**同步，理由写在下面）

| 数据 | 我的建议 | 理由 |
|---|---|---|
| 教务网账号密码（`FlutterSecureStorage`）| **不同步**，每台设备各登一次 | 凭据跨设备传输是最容易被质疑的点，而收益只是「少登一次」；局域网是明文 HTTP |
| AI 的 DeepSeek key / 高德 key | **不同步**，各自填 | 同上；而且每台设备可能想用不同的 key |
| 诊断日志 | 不同步 | 是本机排障用的临时数据 |

> 如果你坚持要同步凭据，那第 6 条的「不做应用层加密」就必须推翻（明文 HTTP 传密码不可接受），
> 我会改成「先做一次端到端加密的密钥交换」—— 这是个明显更大的工程。

### 自定义日程是怎么进同步包的（2026-09-25）

功能本身见 [FEATURES.md](FEATURES.md) 的「自定义日程」一节。这里只记同步侧的口径：

| 项 | 值 |
|---|---|
| 包里新增的键 | `userEvents`（每条是 `UserEvent.toMap()` 的形状）、`userEventTombstones`（`{uid: 删除时刻毫秒}`）|
| 契约版本 | **没动**（仍是 `version = 2`，`format` 仍是 `celechron-mod`）—— 只加字段，老备份照常导入，老客户端读到未知键会忽略 |
| 合并口径 | 同 uid 比 `updatedAt`（新的赢）、**墓碑优先**、删完之后又编辑过的会复活、合并幂等。实现是 `lib/mod/user_event_merge.dart`（纯逻辑，有单测）|
| 存储 | 独立盒子 `dbUserEvent` / `dbUserEventTombstone`，值存 Map/JSON，**不新增 Hive typeId** |

**为什么只改了 `DataBundle` 与 `DataBackup.applyMerge` 就够**（免得以后以为漏了）：
局域网同步的两个方向（当服务器收推送、当客户端拉取）都走 `mergeIncomingBundle`，
而它内部调的正是 `DataBackup.applyMerge(bundle: incoming)`。
所以导出/导入与局域网同步**共用同一个载体与同一条合并路径**，
`lan_sync_client.dart` / `lan_sync_server.dart` 一行都不用改。

**墓碑为什么带删除时刻**（而不是只存 uid 集合）：没有时刻就无法判断
「删完之后又编辑过的该不该复活」。这与待办的 `TaskTombstone` 是同一口径。
墓碑默认保留 **180 天**，之后由 `UserEventStore.pruneUserEventTombstones` 清掉
（一台设备删过、另一台半年没上线的情况本来也不会再看见那条日程）。
清理的调用点在 `CalendarController.loadUserEvents()`：日程页建起来时顺手清一次，
幂等、代价很小；**不清的话墓碑会随删除次数无限长下去**。


---

## 三、建议的路线（每阶段都能单独交付、单独验证）

### S1：把「同步什么」补齐 + 设备身份（地基）
- `DataBundle` 加 `deviceId`、`focusSessions`、`focusWorkMinutes/focusRestMinutes/...`
- 契约**只加字段不改老字段** → 老备份文件仍能导入 ✓
- 测试：契约往返 + 合并（含墓碑、含专注记录）

### S2：面板升级成真编辑器（BACKLOG 1）
- 能改描述 / 子待办 / 标签 / 优先级 / 地点；**改步骤时间与清单分组**
- 自动拉取 + 改完自动推；显示「最后同步来自 X」
- 这是你实际用得最多的界面，值得一次做扎实

### S3：附件同步（BACKLOG 2）
- 附件按内容 sha1 命名 → 同一个面板服务加 `/attachment/<sha1>` 端点
- PC 端**懒下载**（点开才拉），手机端上传
- 需要一个「附件清理」策略（孤儿文件多久删）

### S4：跨网络 —— **两条路线都留，用户自由切换**
- 抽 `SyncTransport` 接口：`LocalHttpTransport`（现在这套，同一 Wi-Fi）
  / `WebDavTransport`（用户自配坚果云应用密码）
- **最终两条都保留**，设置里让用户选当前用哪条（或「都启用」），
  数据层完全复用 S1 的契约 ✓
- 界面要写清两者差别：局域网=**免配置、只在同一 Wi-Fi**；坚果云=**在外面也能同步、但要自己配一次**

### S5（可选）：Windows 桌面端（BACKLOG 4）
- 只有前面都稳了、并且你真的需要「电脑上常驻」再上

---

## 四、风险与坑（先记下来）

| 风险 | 说明 / 对策 |
|---|---|
| **半同步状态**：同步到一半断了 | 契约里有 `exportedAt` 与墓碑，重跑一次是幂等的 ✓ 但仍要测「中断后重连」 |
| **时钟不一致**：两台设备时间不一样 → `updatedAt` 比较出错 | 用「谁的时间更晚」只在**两端都改过**时用；并显示改了哪条，不静默丢 |
| **大附件**：几百张截图 | 懒下载 + 只按需拉；不整包传 |
| **扫到就改**：同一 Wi-Fi 里别人拿到 token | 每次开服务重新配对；面板显示连接设备；文档写清 |
| **学校接口限流** | 注意：**课表/成绩不进同步**就是为了避免「两台设备各拉一次还互相覆盖」|

---

## 五、下一步

等你在这六件事上拍板（尤其 **1 目标形态**、**2 同步范围**、**5 自动还是手动**），
我就把 S1 的设计细化成可实施的任务清单，然后开工。
