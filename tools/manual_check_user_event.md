# 真机验收：日程自定义（学生组织例会）

> 这份是**动手清单**，照着走一遍即可。测试测不出来的部分（布局、颜色、手感）
> 全靠这一遍。做完请把**哪一步不对**告诉我，越具体越好（截图最好）。
>
> 相关：`SPEC.md`（需求与拍板）、`docs/BACKLOG.md` #1（进度）。

---

## 为什么必须真机看

`dart analyze` 只保证编译得过，保证不了：

- 课表格子挤不挤（13 行 × 7 列本来就紧，日程卡片只有 9px 字号）；
- 粉色够不够显眼、和课程的时段色阶会不会撞；
- 编辑页会不会溢出、滚不滚得动、滚轮好不好按；
- 点下去会不会报错（编译过的代码运行时照样能崩）。

---

## 一、把手机接上（一次性）

1. 手机：**设置 → 关于手机 → 连点「版本号」7 次**，打开开发者选项；
2. **设置 → 开发者选项 → 打开「USB 调试」**；
3. 用数据线连电脑（不是只能充电的那种线）；
4. 手机上会弹「**允许 USB 调试吗**」→ 勾选「始终允许」→ 确定。

验证（在你的 PowerShell 里跑）：

```powershell
C:\Android\Sdk\platform-tools\adb devices -l
```

- 看到一行 `xxxxxxx  device ...` → 成功；
- 只有 `unauthorized` → 手机上那个弹窗没点，或点了拒绝（拔插一次会重弹）；
- 什么都没有 → 线/口的问题，换个口试试，或换根线。

> 我在沙箱里查过：手机（`R5GL50QSPBE`）**现在连着**，可以直接装。

---

## 二、装上去（一条命令）

```powershell
cd D:\neochron
flutter run --release
```

它会编译 → 安装 → 启动 → 并把日志接到这个终端（Ctrl+C 退出，不影响手机上已装的应用）。

为什么用 `--release` 而不是默认的 debug：

- **与你现在手机上那个应用同签名**。`android/app/build.gradle` 里 release 走的是
  `signingConfigs.release`，而 `android/key.properties` 不在本机 → **自动退回 debug 签名**，
  正好和手机现存版本一致 → **可以直接覆盖安装，不用卸载**（待办/专注记录都留着）；
- 布局与性能是真的（debug 版启动形态不同，看排版没意义）；
- 日志会打到这个终端，出红字我能直接定位。

> 注意：`flutter run`（不带 `--release`）走的是 debug 构建，它的包名带 `.debug` 后缀，
> 会**另装一个应用**、数据也是空的。别用它验收。

**如果你更想直接拿到文件自己装**：

```powershell
cd D:\neochron
flutter build apk --release --target-platform android-arm64 --no-tree-shake-icons
C:\Android\Sdk\platform-tools\adb install -r build\app\outputs\flutter-apk\app-release.apk
```

（`-r` = 覆盖安装，保留数据。如果它报签名不一致，就先导出数据再卸载重装，见 `docs/MIGRATION.md`。）

> ⚠️ 你上一轮测的那个 APK 还是旧的（里面没有四处修复，课表仍是 13 行）。
> 这一轮的 APK 是 **2026-09-25 16:30 之后**打的，包含：四处修复（`b3b74f3`）
> + 颜色接线（`e901c94`）+ 第 5 步并回主线（`f41a7d3`）。**必须重装再看。**

---

## 三、看什么（按顺序，每步都能判断对错）

### ⓪ 先复验上一轮报的 4 条（`docs/PROBLEMS_SPOTTED.md`）

这四条是上一轮你没通过的地方，先确认都好了：

- [x] **1** 编辑页「重复」那行的值（每周）**真的靠右**了；
- [x] **2** 「第一次发生在」「重复到」不再莫名其妙省略号（内容放得下就应显示全）；
- [x] **3** 点「去掉结束日期」后**那一行不会消失**，而是变成「设置结束日期」，
      还能再点回来；
- [ ] **4** 课表那一面是 **15 行**，且能看到粉色的「学生会例会」。

### ① 新建一条例会

待办/日程页 → 右上角 `+` → 应弹出选择：**「待办（要完成的事）」/「日程（到点就发生，比如例会）」**
→ 选**日程**。

编辑页（这里要看的就是"排版对不对"）：

- [x] 顶部是大字号输入框，提示语是「比如：学生会例会」；
- [x] 「怎么填时间」那一行有 **按节次 ⇄ 按时刻** 的切换；
- [x] 「第一次发生在」能点开日历弹窗，**日历上有课程/待办的小圆点**；
- [x] 「重复」点开是**待办那套弹窗**（每周重复 / 每 N 周 / 无限重复 / 终止于某天）；
- [x] 新建时「重复到」**已经预填了本学期的最后一天**（这是 SPEC D11）；点「去掉结束日期」会变成「一直重复」；
- [x] 颜色那行有 4 个色点，**第一个是粉色**，选中的那个圈更粗；
  **换一个颜色（比如天依蓝）→ 月视图小圆点、当天列表那个小方块、「接下来」左边那条竖线
  都要跟着变；但课表里那张卡片必须仍然是粉色**（SPEC.md R3 定的，两类不共用调色板）；
- [x] 地点、备注能输入；
- [x] **页面能滚到底**（最下面有一句关于"不跟放假走"的说明）；
- [x] 右上角「保存」在标题为空时是灰的。

填：标题「学生会例会」、按时刻 **19:00-20:30**、重复**每周** → 保存。

### ② 日历那面

待办/日程页的日历（不是课表）：

- [x] 今天/下一次那一天（比如下一个该发生的日子）的列表里出现这条日程，
      **用你给它挑的颜色**标记（没挑过就是粉色）；
- [x] 点它能打开详情（应显示名称、时间、地点）；
- [x] 月视图的那一天有标记点。

### ③ 课表那面（**这轮的重点**）

右上角那个列表图标切到课表：

- [ ] 课表现在是 **15 行**：左侧时间列**最下面一格是 22:10**（第 15 行）；
      倒数第二格 21:20（第 14 行）。**只数到 20:30 的话那是第 13 行**，
      下面还有两行 —— 20:30 是旧版（13 行）的最后一格，所以很容易误判，看最底下那格才准；
- [ ] **在你那条日程的星期几那一列**出现一张粉色卡片「学生会例会」，
      落在**第 11～13 行**（19:00-20:30 横跨第 11、12、13 节）；
      颜色是爱莉粉 `#FFA6C9`（与课重叠时会变成不透明 + 橙色角标）。

      > ⚠️ **别去周一那一列找。** 这份清单原来写的是「周一那一列」，**那是我写错了**：
      > 按第 ① 步的填法，「第一次发生在」预填的是**今天**，而今天 2026-09-25 是**周五**，
      > 所以卡片在**周五**那一列（第 5 列）。你照我写的去找周一，当然什么都没有。
      > 这一轮白跑是我文档的错，不是功能的错。
      >
      > 2026-09-25 用逐像素截图核实过：卡片确实在**周五**那一列、y 1491..1782
      > （第 11-13 行），颜色 `#FFA6C9` 叠 85% 白底 = `#FFB3D1`，宽 119px。
- [ ] 卡片**叠在课程卡片上面**（不是并排）：SPEC.md D10 定的就是「叠加显示 + 小角标」，
      不自动挪时间。只有**两条自定义日程**撞在同一行区间时才会各占一半宽度。
- [ ] 字体虽小但**读得出**；地点那行也在；
- [ ] 如果那条日程与课重叠，卡片有**橙色三角角标**；
- [ ] **整体不挤**：15 行比原来的 13 行更矮，课程卡片里的字还看得清吗？

> 2026-09-25 用户拍板把课表从 13 行扩到 15 行（真实校历就是 15 节）。
> 这一改动**只有你真机能判断好坏**：如果太挤，告诉我，我把行高下限再调大
> （现在是 460，矮屏上会变成可滚动）。

### ④ 「接下来」那一面

顶部那个小圆点翻到「接下来」：

- [ ] 这条日程出现在列表里，右边的类型标签是**「日程」**；
- [ ] 显示「还有 N 分钟 / 进行中」这类倒计时。

### ⑤ 编辑与删除

- [ ] 在**日历**里点这条日程 → 应打开**编辑页**（标题写着「编辑日程」，右上角多一个垃圾桶图标）；
- [ ] 改标题或时间 → 保存 → **日历与课表都跟着变**；
- [ ] 点垃圾桶 → 确认弹窗 → 删除后 **日历、课表、「接下来」三处同时消失**；
- [ ] 点左上角 ✕ → 取消，什么都不该变。

> 注：这一段是 2026-09-25 补的 —— 在那之前**点日程卡片什么都不会发生**
> （tap 只服务待办），所以这一步当时是做不到的。

### ⑥（可选）导出与同步

设置 → 数据 → **导出数据** → 得到一份 JSON；用文本编辑器搜 `userEvents`，
应该能看到你刚建的那条。导入同一份文件应提示"新增 0 条"（因为已经在了）。

跨设备（如果你有第二台）：设置 → 局域网同步（`kLanSyncEnabled = true`，
入口是开着的）→ 配对后日程应该能过去，**在一端删掉的不会在另一端复活**。

---

## 四、出问题怎么反馈

把这三样给我，我基本能直接定位：

1. **哪一步**不对（照上面的编号/勾选项说）；
2. **截图**（布局问题必须看图，口头描述我猜不准）；
3. `flutter run` 那个终端里的**红字**（运行时报错都在那里）。

---

## 做完之后我会做的事

- 你验收过了 → **#1（日程自定义）就算完成**：七步都已提交（步 5、6、7 在
  `7f76ec0` / `8da5017` / `f916681`），四处修复在 `b3b74f3`，颜色接线在 `e901c94`。
- 你说还有问题 → 我按同样的流程修（一个分支、一个提交、跑全量测试、给你复验）。
- 之后要不要开新条目（比如提醒投递、只删某一次、导出 iCal），由你在
  `docs/BACKLOG.md`「二、已发现但没排期」里挑。
