# Neochron —— Celechron 的非官方改版（第三代）

> **这是 Celechron 的非官方修改版（fork），不是官方发布。**
>
> - 项目渊源：**Celechron**（官方，面向浙大学生的时间管理器）
>   → **Elychron**（[Elyyyyyyyyxer/Elychron](https://github.com/Elyyyyyyyyxer/Elychron)，由 **Tixer** 维护）
>   → **Neochron**（本项目）
> - 上游项目：[Celechron/Celechron](https://github.com/Celechron/Celechron)
> - 原项目版权归 **nosig** 及 Celechron 全体贡献者所有，遵循 **GPLv3**
> - Neochron 基于上游 **Celechron v1.3.0**（commit `ceab2a4`）；上一代 **Elychron** 的改动一并继承，
>   **那些改动的功劳属于其原作者**，本仓库只是在它们的基础上继续做
> - 许可协议：**GPLv3**（继承上游，见 [LICENSE](LICENSE)）。本仓库即为对应源码，
>   只要分发本程序的二进制，你就有权获得这份源码。
> - 独立 `applicationId`（`xyz.nosig.celechron.mod`），**可与官方版共存**，装它不会覆盖官方版
>
> Neochron 不是 Celechron 官方版本，与官方项目无关。遇到问题请提到**本仓库** Issues，
> **不要去打扰上游作者**（也不要打扰上一代作者）。

## 下载与安装

到 [Releases](https://github.com/SpecialJudge/Neochron/releases/latest) 下载 APK 直接安装。

- 只提供 **arm64** 包（近年的校园机型都支持）
- 与官方 Celechron **包名不同，可以共存**，装它不会覆盖官方版，两边数据互不影响
- 首次安装需要在系统里允许「安装未知来源应用」
- **校验**：每个版本的 APK SHA-256 与签名证书指纹写在对应 Release 说明里。
  本项目当前的签名证书指纹（SHA-256，`CN=Neochron`，2026-09-25 生成）：

  ```text
  1ecde30744d28360749269a24b3e87719fcde710ab5f283d12db9dbdf5deb31c
  ```

  核对方式：`apksigner verify --print-certs <你的.apk>`；详见 `docs/RELEASE.md`。
  > 上一代 Elychron 那串 `b2cc4256…a771c8` 属于**上一代维护者的密钥**，
  > 与本项目无关，**不要**拿它来校验本项目的包。

## 一、为什么会有这个改版

Celechron 可以查课表、成绩、考试、作业，也允许自定义日程。v1.3 之后数据稳定性已经大幅改善，
但仍有两类问题：

1. **教务不可用时课表就没了**：本科教务在选课 / 筛课期间会「请求成功但返回空表」，
   而空结果会覆盖本地缓存 —— 刷新一下课表就空了、课时变 0；
2. **日程功能与成熟工具差距明显**：只有笼统的「任务 / DDL」，没有子任务、标签、优先级、
   专注统计；提醒只有通知一种形式。

于是 **Elychron（上一代）** 做了三件事：**换掉课表数据来源并保住缓存**、**重写整套待办与时间语义**、
**补上提醒、专注、AI 整理这些日常真正会用到的能力**；因为与原版差异已经很大，不适合合并回上游
（会造成版本混乱、合并成本极高），所以选择独立发行。

**Neochron（本项目，第三代）** 在 Elychron 的基础上继续：把「**日程**」从待办里独立出来
（学生组织例会这类"到点就发生"的事，见下一节），并把应用名、签名与仓库都收拢到自己名下。

## 二、更新内容

> 本节里「已有功能优化 / 新增功能 / 与官方版的对照」记的是**上一代 Elychron** 打下的底子，
> 本仓库全部继承并继续维护；「Neochron 这一代」单独列**这一代自己做的改动**。

### Neochron 这一代（2026-09）

**自定义日程（学生组织例会那种）—— 这一代的主功能**

- 在日程页 `+` 里可以直接新建**日程**（与「待办」并列，不再是待办的一种）；
- 支持**只这一次 / 每周 / 每两周 / 每 N 周**，配一个截止日
  （新建时默认填本学期最后一天，也可以清掉改成"一直重复"）；
- 时间可以**按节次**填，也可以**按具体时刻**填（如 19:00-20:30）；
- 同时出现在**四处**：日历当天列表、**课表那一面**（粉色卡片）、「接下来」、当天列表；
- 课表里与课程重叠时**叠加显示并带橙色角标**，不自动挪时间；
- 可以**选颜色**（4 套预设，默认爱莉粉 `#FFA6C9`）：月视图、当天列表与「接下来」跟着变，
  **课表里一律固定粉** —— 课程走的是按小时变色的色阶，共用调色板会撞色；
- 与待办一样参与**导出 / 导入 / 局域网同步**，删掉的不会被同步回来；
- **归属学期**：上学期建的例会不会跑到下学期那张课表上。

**这一代的其它改动**

- 应用名 **Elychron → Neochron**（桌面名、应用内标题、关于页、教程与各处提示文案）；
- 仓库迁到本项目自己的地址：更新检查、关于页源码链接、校历镜像都指向这里；
- 改用**本项目自己的正式签名密钥**（不再用 debug 签名，指纹见上一节）；
- 课表从 13 行扩到 **15 行** —— 真实校历就是 15 节，此前第 14、15 节（21:20 之后）
  的课程与日程都画不出来。

### 已有功能优化

- **增设 eta 获取数据渠道（issue #174）**：本科教务不可用期间（譬如选课），自动改用
  **智慧研工**取课表，确保仍能正常拿到数据
- **空数据不再覆盖缓存（issue #174）**：教务明确返回空课表时不再把它写进缓存，
  而是沿用上一次成功解析的课表
- **校历多级回退**：远程配置 → 同学期缓存 → 本地推算；上游校历接口挂掉时只影响
  假期与考试周标注，课表照常显示
- **开学前也能看课表**：学期还没开始时直接显示新学期课表并提示开学日期，
  不再是干巴巴一句「无学期信息」
- **诊断与测试更有用**：刷新结果会写明「接口返回几行 / 解析入库几条 / 能显示几条」，
  出问题不用猜；一键导出**脱敏**日志，密码、Cookie、学号都会自动隐藏
- **重写时间设置模块**：时间设置现在可以打开日程表，按日历选日期；
  同时支持更复杂的重复逻辑（新增「每周工作日，跳过双休日」等），自定义自由度更高

### 新增功能

**待办：四种时间语义 + 子待办行程表**

- **时间语义细化**：把原来笼统的日程 / DDL 统一为「待办」后，再细分为
  **活动 / 截止 / 提醒 / 备忘**四种时间语义，各自的提醒时间、逾期判定、日历表现都不同：
  - **活动**：完整起止时间，提醒默认开始前 30 分钟（等效于原版的「日程」）
  - **截止**：只有截止时刻（等效于原版的「DDL」）
  - **提醒**：只有一个提醒时刻，用于便捷通知
  - **备忘**：无时间、无提醒，充当备忘录
- **新增子待办**：一条待办可以挂多个子待办，分为两类 ——
  **清单类**没有时间、默认提前显示（`带雨伞`、`带校园卡`）；
  **日程类**有起止时间，可自动排序形成日程表
- **新增附件 / 优先级 / 标签**：待办可加附件；优先级参与分类；
  标签库便于批量整理与查看
- **新增评论**：长程任务里记录实时情况，方便数字留痕
- **新增待办页筛选器**：按时间语义、创建时间、标签等筛选，不用疯狂翻页
- **删除增加二次确认**：防止手滑
- **合并「接下来」与「日程」**：屏幕顶部圆形按键翻页切换，默认打开「接下来」

**AI 整理（默认关闭，自带 Key）**

- **全自动日程转换**：把通知、聊天记录、邮件甚至**一张截图**整理成结构化待办 ——
  标题、描述、类型、时间、地点、标签、子待办一次到位
- **自动拆解子待办**：复杂任务拆成可执行步骤；「着装：白色院衫、带校徽、带雨伞」
  会拆成若干**清单类**子待办，行程时间则写成**日程类**子待办
- **默认关闭**：开启需填自己的模型服务商 Key（当前版本仅支持 DeepSeek），
  内容只发给你自己配置的服务商

**待办页：钉钉风格的重做**

- **分类页签**：待我处理 / 优先处理 / 我已处理 / 星标
- **标签体系**：可新建、删除、**拖动排序**、**每个标签单独配色**（10 色盘）；标签筛选可多选

**提醒与闹钟**

- **提醒方式可选**：**通知**（横幅 + 响铃）或**全屏闹钟**
  （全屏弹出 + 循环系统闹钟铃声 + 震动，可延迟 10 分钟或划掉）
- **四套闹钟配色预设**（天依蓝 / 爱莉粉 / 清新绿 / 静谧紫）：设置里可预览真实页面
- **新增闹钟可靠性自检页**：全屏闹钟授权、通知渠道重要度、电池优化白名单检查，
  没开的可以直接跳转设置（国产 ROM 杀后台是闹钟不响的头号原因）
- **可选系统时钟提醒**：重点事项可以**手动**设为系统闹钟
  （待办详情 → 右上角 ⋯ → 设为系统闹钟）。
  ⚠️ 系统闹钟**一次性**、且**不会随待办完成 / 删除而撤销**，要改要删得自己去「时钟」应用操作 ——
  所以它只做手动入口，不做成常驻提醒方式

**专注模式**

- **新增专注页**：进入工作 / 休息循环，可以指定待办，也可以自由新建临时任务进行专注；
  专注页可查看详细统计，用于统计各事务的真实时间消耗
- **工作 / 休息自动交替**（默认 1 小时 / 15 分钟，可改），工作段走完弹通知提醒你起来走走
- **专注记录**：今天 / 本周 / 本月时长、最近七天趋势、按任务分布

**其它**

- **跨应用联动**：其它应用分享图片 / 文件 / 文字进来即可新建待办
- **待办 JSON 导出 / 导入**：按 `uid` + 更新时间合并（不是覆盖），删除留墓碑，便于数据迁移

### 与官方版的对照

| 能力 | 官方 Celechron | Neochron |
|---|---|---|
| **时间语义** | 两种（日程 / DDL） | **四种**：活动 / 截止 / 提醒 / 备忘，各自的提醒锚点、逾期判定、日历表现都不同 |
| **自定义日程** | ❌ 无 | ✅ **独立于待办的日程**：每周 / 每两周 / 每 N 周 + 截止日，按节次或按时刻填；出现在课表、月视图、当天列表与「接下来」四处，可选颜色 |
| **子待办** | ❌ 无 | **清单类 + 日程类**：每步可带起止时间与提前量，也可只做打钩项 |
| **附件 / 优先级 / 标签 / 评论** | ❌ 无 | ✅ 均有（标签库可配色、拖动排序） |
| **专注计时** | ❌ 无 | 工作 / 休息自动交替 + 今日 / 本周 / 本月统计、按任务分布 |
| **AI 整理** | ❌ 无 | 文字或**截图** → 结构化待办，自带 Key、默认关闭 |
| **提醒方式** | 只有通知 | 通知 **或全屏闹钟**（循环铃声 + 震动 + 延迟），四套配色；另有可靠性自检与系统闹钟入口 |
| **课表数据源** | 本科教务单源 | **双来源**：教务拿不到时改用智慧研工；两个都拿不到时**沿用上一次的课表**，不清空 |
| **开学前看课表** | 显示「无学期信息」 | 显示新学期课表，并提示开学日期 |
| **数据备份** | ❌ 无 | ✅ JSON **导出 / 导入**（按 `uid` + 更新时间合并，删除留墓碑） |
| **出问题的排查** | 诊断日志 | 诊断日志 + **刷新数字**（接口返回几行 / 解析入库几条 / 能显示几条） |
| **电脑上操作待办** | ❌ 无 | ✅ **局域网直连网页端**：手机当服务器、电脑浏览器访问，配对码 + 本地 token，只服务私有网段 |
| **包名** | `xyz.nosig.celechron` | `xyz.nosig.celechron.mod` —— **可与官方版共存** |

> 上表每一行都能在源码里对上：上游 1.3.0 的 `Task` 模型**没有** priority / attachment /
> tags / subtask / reminder 字段，仓库里也**没有** `lib/mod/` 目录、
> 更没有标签管理、筛选面板、时间面板、闹钟主题这些文件 —— 它们都在本仓库里。

## 三、数据迁移

**从上一代 Elychron 迁过来**（最容易踩的坑，先说这个）：

- 两者**包名相同**（都是 `xyz.nosig.celechron.mod`），系统把它们当**同一个应用**，
  所以安装时显示的是"更新"而不是"安装"，而且**两者不能共存**；
- 但**签名不同**（上一代用其维护者的密钥，本项目用自己的密钥），所以**覆盖安装会被拒绝**
  （`INSTALL_FAILED_UPDATE_INCOMPATIBLE`）；
- **版本降级也会被拒绝**：如果已装的是 versionCode 更大的版本，装旧版会报"已安装了较新版本"
  （`INSTALL_FAILED_VERSION_DOWNGRADE`）。Android 只比 **versionCode**
  （`pubspec.yaml` 里 `版本号+` 后面那个整数），不看 versionName；
- 正确步骤：打开 **Elychron** → 设置 → 数据 → **导出数据** → **卸载 Elychron** →
  装 Neochron → 设置 → 数据 → **导入数据**。反过来想回 Elychron，同样要走这一套。

**从官方版迁过来**：课表、成绩、考试、作业、素质拓展、校园卡 **登录同一个学校账号就有**，
不用搬。要手动处理的只有你自己手写的待办 —— 官方版**没有数据导出功能**，这部分只能重建：
建议在待办页截图，再用 AI 整理识别成待办，比手打快得多。（官方版包名是
`xyz.nosig.celechron`，**可与 Neochron 共存**，不需要卸载。）

备份与恢复的细节（导出文件里有什么、合并规则、什么时候该导出）见
[docs/MIGRATION.md](docs/MIGRATION.md)。

## 四、已知问题

- 只提供 **arm64** 包
- **自定义日程的提醒还没做**：这一期只保证"看得见"，到点不会响；
  另外**「只删某一次」也还没做**，目前只能整条删。两项都记在 [docs/BACKLOG.md](docs/BACKLOG.md)
- **自定义日程不跟放假 / 调休走**：它是"自然周几 + 截止日"，中秋国庆放假那天的例会
  **不会自动消失**，要自己删那几条或改截止日。这是刻意的取舍 —— 放假期间也可能有活动
- **局域网同步还比较新**：入口在 设置 → 数据 → 局域网同步 —— 手机当服务器，
  同一 Wi-Fi 下的另一台设备用浏览器访问（配对码 + 本地 token，只服务私有网段）。
  **请在可信网络里使用**：同一 Wi-Fi 的设备拿到配对码就能读到这些数据
- **跨网络同步（坚果云 / WebDAV）** 只有预留配置项，功能未实现
- **校历配置**仍依赖上游的公开只读接口，挂了会回退到缓存 / 本地推算，只影响假期与考试周标注
- **AI 功能**需要自备模型服务商的 Key
- **系统闹钟**设出去之后撤销不了，得自己去「时钟」应用删

## 五、后续开发方向

- **自定义日程的提醒**（提前 N 分钟），以及**只删某一次**（不删整条）
- 多端同步：允许跨不同设备、网络同步与管理数据（局域网同步已实现，跨网络同步尚未）
- 接入导航 API：更精确地规划预留提醒时间
- 软件风格自定义化、UI 动画优化

> 完整清单（含"已发现但没排期"与"明确推迟"两类）见 [docs/BACKLOG.md](docs/BACKLOG.md)。

## 六、构建

```bash
flutter pub get
flutter build apk --release --target-platform android-arm64 --no-tree-shake-icons
```

> ⚠️ Windows 上**项目路径不能含非 ASCII 字符**（Gradle 会直接拒绝），
> 中文目录下请用 junction / 软链映射到纯英文路径再构建。
> `--no-tree-shake-icons` 是为了绕开 Windows 应用控制策略对 `font-subset.exe` 的拦截。

签名：跑一次 `tools/setup_signing.ps1` 生成自己的 keystore，它会同时写好
`android/key.properties`（该文件已 gitignore）；没有它时会退回 debug 签名以便本地调试。
⚠️ keystore 与密码**必须备份两处** —— 丢了就再也发不出能覆盖升级的包。
发版的完整清单见 [docs/RELEASE.md](docs/RELEASE.md)。

## 七、隐私

**没有自己的服务器，不收集、不上传任何数据。** 会联网的场景只有：拉取你自己的教务数据、
课表兜底来源（智慧研工）、只读的公开校历接口、检查更新，以及你主动开启的 AI
（局域网同步走的是你自己的局域网，不经过任何服务器）。逐条说明见 [PRIVACY.md](PRIVACY.md)。

## 八、反馈

- 问题与建议：[Issues](https://github.com/SpecialJudge/Neochron/issues)
- 报问题时请附上 **设置 → 诊断与测试 → 导出并分享 TXT** 的日志（已自动脱敏，
  密码 / Cookie / 学号都不会带出去），比截图有用得多
- 上手教程见 [docs/FEATURES.md](docs/FEATURES.md) ｜ 版本说明见
  [docs/RELEASE_NOTES.md](docs/RELEASE_NOTES.md) ｜ 与上游的跟版流程见
  [docs/UPSTREAM.md](docs/UPSTREAM.md) ｜ 完整改动记录见 [MOD_NOTES.md](MOD_NOTES.md)

## 九、许可

GPLv3。你可以自由使用、修改、再分发，但**必须**：保留原版权与许可证声明、
标注这是修改版、以同样的协议发布、并提供完整源码（即本仓库）。
不得附加「禁止商用 / 禁止转载」之类的额外限制。
详见 [LICENSE](LICENSE)。
