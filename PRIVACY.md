# 隐私说明

Neochron（Celechron 的非官方修改版）**没有自己的服务器**：不收集、不上传、不分享任何用户数据。

## 数据存在哪

| 数据 | 位置 | 说明 |
|---|---|---|
| 待办、标签、设置、课表/成绩缓存 | 手机本地 Hive 数据库（应用私有目录） | 卸载应用即删除 |
| 附件 | 应用私有目录 | 选择或分享进来的文件会被复制一份 |
| 浙大账号与密码 | 本机系统密钥库（Android Keystore / iOS Keychain） | 只用于登录**你自己的**学校账号 |
| 诊断日志 | 应用缓存目录 | 自动脱敏（见下），最多保留 2000 行 |

## 会联网的场景

全部为你主动使用或学校数据刷新所必需，**没有任何一条会把数据发给我们**（我们没有服务器）。

1. **拉取你自己的教务数据**：直连浙大官方服务器
   —— 统一身份认证 `zjuam.zju.edu.cn`、本科教务 `zdbk.zju.edu.cn`、研究生院 `yjsy.zju.edu.cn`、
   学在浙大 `courses.zju.edu.cn`、素质拓展 `sztz.zju.edu.cn`、校园卡 `elife.zju.edu.cn`。
2. **智慧研工 `eta.zju.edu.cn`**：当本科教务返回空课表时，作为课表的**兜底来源**读取你的课表。
   用的是同一套学校账号体系，同样直连学校服务器。
3. **校历配置**：只读请求上游项目提供的公开接口 `calendar.celechron.top`，
   只取学年学期日期这类**公开信息**，不含任何用户数据；请求失败会依次回退到
   同学期缓存 → 本地推算，不影响功能可用性。
4. **检查更新**：每天最多一次请求 GitHub API
   （`api.github.com/repos/SpecialJudge/Neochron/releases/latest`），
   只读取版本号与更新说明；异常时静默跳过。**这一条会向 GitHub 暴露你的 IP**。
5. **关于页的贡献者列表**：打开时请求 GitHub API 读取上游仓库的贡献者名单，同样是公开信息。
6. **AI 整理（默认关闭）**：你需要自己填模型服务商的 Key（当前支持 DeepSeek）。
   开启后，待办标题、描述、时间等文本会发送到**你自己配置的**服务商。
   我们不经手、也不中转这些内容；关掉开关即不再发送。
7. **局域网同步（入口已开放）**：手机当服务器，同一局域网内的另一台设备用浏览器访问；
   数据只在**你自己的设备之间**走，不经过任何第三方。见下一节。

## 局域网同步会做什么

> 入口在 **设置 → 数据 → 局域网同步**。下面是这个功能的行为说明，也便于你审计仓库里的实现。

这是「手机当服务器、电脑浏览器访问」的本地直连功能，**数据只在你的设备之间走**。

- **默认关闭**；只有你打开设置里的「局域网同步」页面、并把开关打开时才监听。
- 手机在**当前局域网内**监听一个 HTTP 端口；**只服务私有网段**来源
  （回环 / 链路本地 / 10.x / 192.168.x / 172.16–31 / fc00::/7），公网地址一律拒绝。
- 电脑需要输入手机上显示的 **6 位配对码**换取一个会话 token；token 只通过
  请求头 `X-Lan-Token` 传递（不走 URL，避免留在浏览器历史与代理日志里）。
- 网页端会把 token 存在**你自己电脑浏览器的 localStorage** 里，方便下次免配对。
- 传输内容就是你自己的待办与设置；**不经过任何第三方服务器**。
- 关掉开关、或离开该页面即停止监听。

> 同一 Wi-Fi 下的其他设备若拿到配对码，也能读到这些数据 —— 请在可信网络里使用。

## 诊断日志

- 日志用于自查刷新失败、接口异常这类问题，**自动隐藏**密码、Cookie、Session、ticket、
  学号等敏感字段（有单元测试守着这条）。
- 只有你主动点「导出并分享」时才会离开设备，去哪里由你决定。

## 权限用途

| 权限 | 用途 |
|---|---|
| 网络 | 登录学校系统拉取数据、检查更新 |
| 通知 | 待办到期提醒、闹钟 |
| 精确闹钟 | 让提醒在设定时刻准时触发 |
| 震动 | 闹钟模式下震动 |
| 日历读写 | 把课表同步到系统日历（可选，默认关闭） |
| 前台服务 / 后台刷新 | 让课表与作业在后台更新（可在设置里关闭） |
| 分享接收 | 从其它应用接收分享内容 |

局域网同步复用「网络」权限，不额外申请任何权限。

## 未实现的功能

「跨网络同步（坚果云 / WebDAV）」目前**只有预留的配置项，功能未实现**，不会发送任何数据。

## 联系方式

有问题请提到 [本仓库 Issues](https://github.com/SpecialJudge/Neochron/issues)。
